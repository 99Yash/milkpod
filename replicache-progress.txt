Replicache PR review progress log.

Populated per-iteration by loop.sh. Format for each task:

## Task N: <title>

Issues found and fixed:
  1. path/to/file.ts:LINE — <what was wrong> — <what you did>

Issues noted (no fix needed):
  1. path/to/file.ts:LINE — <why acceptable>

Library improvements made:
  - <replaced hand-rolled X with library primitive Y>

Checklist verification:
  - [checklist item] — [evidence]

---

## Task 1: Verify row-version triggers fire on INSERT + UPDATE with distinctness guard

Issues found and fixed:
  1. packages/db/src/migrations/0033_sync_row_version_triggers.sql — `bump_row_version()` had no no-op guard. Combined with `lifecycle_dates.updatedAt` using Drizzle's `$onUpdate(() => new Date())`, every `.update()` call auto-bumps `updated_at`, so even a semantic no-op UPDATE (e.g. momentUpdate called twice with identical payload) would satisfy a vanilla `WHEN (OLD.* IS DISTINCT FROM NEW.*)` clause and bump `row_version`, emitting a redundant `put` on the next CVR diff. Fixed by adding migration 0038_sync_row_version_distinctness_guard.sql which replaces the function with a body-level guard: compare `to_jsonb(NEW) - 'row_version' - 'updated_at'` vs OLD's equivalent, and short-circuit when they're IS NOT DISTINCT FROM. Exclude the metadata columns so Drizzle-generated timestamp bumps don't count as a change. Also updated _journal.json (idx 38) and copied 0037_snapshot.json → 0038_snapshot.json (schema unchanged; new snapshot id + prevId chain).

Issues noted (no fix needed):
  1. packages/db/src/migrations/0033_sync_row_version_triggers.sql:10-22 — Trigger is `BEFORE UPDATE` only (no INSERT). Confirmed intentional and correct: the in-migration comment explains it, and pull.ts step 6 relies on presence-in-current vs presence-in-prev (not row_version) to detect new rows. A new row's row_version defaults to 0, and the CVR diff emits `put` because `prevSnapshot.{table}[id]` is undefined, independent of row_version value. Adding `BEFORE INSERT` would be redundant.
  2. packages/api/src/modules/replicache/pull.ts:141-183 — Pull publishes three entity types: assetMoments, assetComments, notifications. All three schemas have `row_version integer NOT NULL DEFAULT 0` (comments.ts:42-43, moments.ts:54-55, notifications.ts:46) and all three have a bump trigger (0033 for moments/comments, 0037 for notifications). Cross-reference complete — no synced entity is missing a trigger.

Library improvements made:
  - None applicable — the fix is a Postgres trigger function, not a library call. Verified the `to_jsonb(record) - 'col'` pattern is idiomatic (confirmed via mcp__grep__searchGitHub in kuestcom/prediction-market audit trigger).

Checklist verification:
  - BEFORE INSERT OR UPDATE — UPDATE covered by 0033/0037. INSERT deliberately omitted; CVR diff emits puts on presence, not row_version. Design documented in 0033 header comment.
  - No-op UPDATE guard — 0038 adds body-level guard excluding updated_at + row_version. Handles Drizzle $onUpdate auto-bump case where a simple WHEN clause would not.
  - Every synced table has a trigger — assetMoments (0033), assetComments (0033), notifications (0037). All three match the entities emitted by pull.ts.
  - Appropriate data types — rowVersion is integer (not bigint; 2^31 updates per row is effectively infinite for this domain). jsonb body column on notifications uses `.$type<>()`-style narrowing via NotificationType discriminants at the serialization layer (service.ts).
  - Idempotent DDL — 0033 uses CREATE OR REPLACE FUNCTION + DROP TRIGGER IF EXISTS. 0037 does the same. 0038 reuses CREATE OR REPLACE FUNCTION — safe to re-run.
  - Migration safety for zero-downtime — 0038 is a function replacement only; no table locks, no column changes. Safe on large tables.
  - pnpm check-types: clean (8/8 tasks green).

---

## Task 2: Verify backfill migrations are idempotent

Issues found and fixed:
  - None. Both migrations are already idempotent by design.

Issues noted (no fix needed):
  1. packages/db/src/migrations/0031_backfill_asset_member_owners.sql:3-6 — `INSERT ... SELECT ... ON CONFLICT ("asset_id", "user_id") DO NOTHING` on the composite PK. Safe to re-run. Uses the media_asset's `created_at` for both created_at and updated_at on the membership row so the backfilled rows look chronologically consistent with the asset; this is harmless for sync because the BEFORE UPDATE row_version trigger (0033) only fires on UPDATE, not INSERT — see Task 1 notes. Also, since `created_at` on asset_member is set from the pre-existing media_asset.created_at rather than NOW(), the `updated_at = created_at` invariant holds and won't later be bumped by Drizzle's `$onUpdate` unless someone actually updates the row.
  2. packages/db/src/migrations/0035_backfill_orphan_asset_member_owners.sql:7-13 — Doubly idempotent: a LEFT JOIN WHERE `am.user_id IS NULL` filter, plus `ON CONFLICT DO NOTHING` as defense-in-depth. Safe to re-run.
  3. Gap coverage claim verified: 0031 was added in commit 16ce298 ("refactor: replicache sync"), which introduced the asset_member table. `AssetService.create` didn't begin seeding memberships until a later commit (85b4937 / 1297719 — migration 0035 was added in 85b4937). Any asset created via the old (pre-seeding) create path between 0031 and 85b4937 would be missing its owner membership; 0035 closes that gap. Confirmed by reading the journal timestamps and current service.ts:170-173 which now inserts the owner membership inside the same transaction as the asset insert with `.onConflictDoNothing()`.

Library improvements made:
  - None. `INSERT ... ON CONFLICT DO NOTHING` IS the idiomatic Postgres/Drizzle primitive here; no library abstraction would be cleaner for a pure-SQL migration. (Noted for completeness: the runtime `AssetService.create` seeding also uses Drizzle's `.onConflictDoNothing()` which is the library primitive version of the same pattern.)

Checklist verification:
  - Idempotent (safe to re-run) — 0031 ON CONFLICT DO NOTHING; 0035 LEFT JOIN ... WHERE IS NULL + ON CONFLICT DO NOTHING. Both verified.
  - Migration safety for zero-downtime — both are additive INSERTs into asset_member; no schema mutation, no column changes, no table locks. Safe.
  - No raw string concatenation (SQL injection surface) — pure static DDL / DML, no parameter interpolation.
  - Appropriate data types — role is the 'asset_member_role' enum (defined in 0030); assetId/userId are text (matching media_asset.id / user.id); timestamps preserved from source.
  - PK integrity — 0031 and 0035 target the composite PK (asset_id, user_id); ON CONFLICT clause correctly references both columns.
  - Gap analysis — 0035's claim that it covers assets created between 0031 and the AssetService.create seeding change is verified by the commit sequence (0031 in 16ce298; seeding in 85b4937; 0035 also in 85b4937 as the compensating backfill). No orphan windows remain because the now-seeding create path uses `.onConflictDoNothing()`, so re-runs are harmless.
  - Ordering / independence — 0035 runs after 0034 which constrains asset_invite roles to non-owner; 0035 inserts into asset_member not asset_invite, so no CHECK interaction. Independent.
  - pnpm check-types: clean (8/8 tasks green, full-turbo cache hit; no source changes).

---

## Task 3: Pre-deploy check for 0034 CHECK constraint

Issues found and fixed:
  - None. No code change required (task scope is pre-deploy operational verification).

Issues noted (no fix needed):
  1. packages/db/src/migrations/0034_asset_invite_role_not_owner.sql:7-9 — `ALTER TABLE ... ADD CONSTRAINT ... CHECK (role IN ('editor','viewer'))` is a plain (validating) constraint add. Postgres takes an ACCESS EXCLUSIVE lock and validates ALL existing rows immediately. In a large-table deploy this could stall writes for the duration. Here it's acceptable: asset_invite was introduced in 0030 of this same branch, so the table is empty on first deploy, and the expected steady-state row count is small (pending invites per asset). For future reference, the safer pattern for already-populated tables is `ADD CONSTRAINT ... CHECK (...) NOT VALID; ... VALIDATE CONSTRAINT ...` which keeps the validation out of the DDL lock window. Not changing here because (a) the current situation is safe, and (b) retrofitting two-phase validation once data is small gains nothing.
  2. packages/db/src/migrations/0034_asset_invite_role_not_owner.sql — Uses `ADD CONSTRAINT` without `IF NOT EXISTS` (syntax not supported on ADD CONSTRAINT in Postgres < 17 anyway). Drizzle's journal prevents re-application so this is fine; noting for the record.
  3. Independence from 0035 verified: 0034 targets table `asset_invite`; 0035 targets table `asset_member`. Different tables, different columns, no foreign key between them other than both referencing `media_asset`. No ordering constraint between 0034 and 0035 — they could run in either order without behavioral change. Current journal ordering is 0034 → 0035, which is what `_journal.json` idx 34/35 shows (entries copied verbatim: 0034 when=1776866713941, 0035 when=1776867555730).
  4. API-side safety verified: `packages/api/src/modules/asset-members/service.ts:10` defines `AssetInviteRole = Exclude<AssetMemberRole, 'owner'>`, and `AssetMemberService.invite(..., role: AssetInviteRole)` is the only code path that INSERTs into asset_invite. `packages/auth/src/signup-hooks.ts` only SELECTs and DELETEs from asset_invite. No code path inserts role='owner', so the CHECK is pure defense-in-depth against direct DB writes — it should never reject a row authored by this application's runtime.
  5. CHECK syntax compatibility with pgEnum column: `role IN ('editor','viewer')` compares the `asset_member_role` enum column against string literals. Postgres implicitly casts the literals to the enum via `unknown → enum` resolution; this is the standard idiom used throughout Drizzle's own generated migrations. No CAST is needed.

Pre-deploy verification (REQUIRED before running 0034 against any environment that may have seen manual DB writes):

    -- Expected: 0
    SELECT count(*) FROM asset_invite WHERE role = 'owner';

    -- If > 0, resolve offending rows before applying 0034. Either:
    --   DELETE FROM asset_invite WHERE role = 'owner';
    -- or downgrade:
    --   UPDATE asset_invite SET role = 'editor' WHERE role = 'owner';
    -- Then re-run `SELECT count(*)` — must return 0 — and only then `pnpm db:migrate`.

    Rationale: 0034's `ADD CONSTRAINT ... CHECK` validates every existing row at
    DDL time. One `role = 'owner'` row → migration fails → deploy blocked.

Expected result across all current environments: 0. asset_invite only exists in environments where migration 0030 (Apr 22 2026 onward) has been applied, and the only runtime insert path (AssetMemberService.invite) is TS-narrowed to exclude 'owner'. Any non-zero result would indicate a manual DB write outside the application and must be investigated before deploy.

Library improvements made:
  - None. Pre-deploy check is an operational verification, not a library call site.

Checklist verification:
  - Migration safety for zero-downtime — ADD CONSTRAINT takes ACCESS EXCLUSIVE briefly; table is effectively empty in prod, so the lock window is negligible. Noted NOT VALID alternative for future large-table CHECKs.
  - No raw string concatenation (SQL injection surface) — pure static DDL.
  - Appropriate data types — CHECK targets the `asset_member_role` enum column; string literal comparison is idiomatic Postgres.
  - No ordering dependency — 0034 and 0035 target different tables, confirmed by reading both migrations.
  - Defense-in-depth rationale matches schema narrowing — confirmed asset_invite schema uses `.$type<'editor' | 'viewer'>()` (asset-members.ts:55) and service.ts enforces the same at the TS API boundary.
  - pnpm check-types: clean (8/8 tasks green, full-turbo cache hit; no source changes).

---

## Task 4: Verify indexes on new FK columns and hot query paths

Approach: enumerated every query that touches the new tables (`asset_member`, `asset_invite`, `notification`, `replicache_client_group`, `replicache_client`) across `packages/api`, `packages/auth`, and sanity-checked FK columns without app-level WHEREs. Cross-referenced each WHERE / ORDER BY / JOIN predicate against the indexes declared in the schemas and the 0030/0032/0036 migrations.

Issues found and fixed:
  - None. Every application-level WHERE on the new tables is already covered.

Issues noted (no fix needed):
  1. packages/db/src/schema/notifications.ts:57-60 — `notification_recipient_row_version_idx` on `(recipient_id, row_version)` is unused today. The pull handler (pull.ts:182-184) selects notifications with `WHERE recipient_id = ?` only; `row_version` is read into JS and diffed against the CVR snapshot in memory. No query filters or orders by `row_version`. Kept (not removed) because (a) Task 14 in this plan explicitly calls for a prepared hot-path pull query that would filter by row_version, at which point this composite index becomes load-bearing; (b) removing it would require a new migration with rollback risk; (c) notification write volume is low (bounded to membership-lifecycle events per asset), so write-amplification cost is negligible. Same pattern exists on `asset_comment_asset_row_version_idx` / `asset_moment_asset_row_version_idx` (migration 0032) for identical reasons — out of scope for this task but noted for consistency.
  2. packages/db/src/schema/replicache.ts:5-12 — `replicache_client_group.user_id` has no index. Not queried by any app code (only the PK `id` is used for lookup in pull.ts:105 and push.ts:84). The `ON DELETE cascade` from `user.id` would perform a seq scan on user deletion — acceptable because user deletion is administratively rare and the table stays small (one row per browser profile, not per event). If a future feature needs "invalidate all client groups for user X on password rotation" an index should be added then.
  3. packages/db/src/schema/asset-members.ts:30-32,55-57 — `asset_member.invited_by` and `asset_invite.invited_by` are both un-indexed FKs (`ON DELETE set null` and `ON DELETE cascade` respectively). Neither is queried in a WHERE by application code. Acceptable because user deletion is rare and these tables remain small.
  4. packages/db/src/schema/notifications.ts:35-37 — `notification.actor_id` FK has no index. Queried only as a LEFT JOIN driven from `notifications` (pull.ts:183, service.ts:169); the join is `user.id = notifications.actor_id`, so the hot path uses `user`'s PK — no index on the FK side is needed.

Library improvements made:
  - None. Drizzle's `index()`, `primaryKey()`, and `unique()` builders are already the library primitives here, and all indexes declared match actual query patterns. No opportunity to replace hand-rolled index definitions with a higher-level abstraction (Drizzle has none for this).

Checklist verification (per `review-prompt.md` Queries & Data Access + Task 4 scope):
  - Every FK column queried in a WHERE has an index:
      - notifications.recipient_id — `notification_recipient_created_idx`, `notification_recipient_unread_idx` (partial), `notification_recipient_row_version_idx`
      - assetMembers.assetId (via PK prefix) and assetMembers.userId — PK + `asset_member_user_idx`
      - assetInvites.assetId (via UNIQUE prefix), assetInvites.email — UNIQUE(asset_id,email) + `asset_invite_email_idx`
      - replicacheClient.clientGroupId — `replicache_client_group_idx`
      - replicacheClientGroup.id, replicacheClient.id — PKs
  - `notifications (userId, createdAt DESC)` exists — verified as `notification_recipient_created_idx` on (recipient_id, created_at); Postgres backward-scans for ORDER BY … DESC.
  - Partial index on `(userId) WHERE readAt IS NULL` for unread counts — verified as `notification_recipient_unread_idx` with `WHERE "notification"."read_at" IS NULL`. Used by `markAllRead` (service.ts:201-212), `markReadByResource` (service.ts:219-237), and `markRead`'s single-row filter (service.ts:181-197).
  - `asset_member` PK covers `(assetId, userId)` — confirmed; used by `getRole` (both columns), `list`/`listMemberUserIds` (assetId prefix), and `AssetService.list`/`search`/`listPage` join + WHERE (assetId + userId).
  - `asset_member` userId-only lookup has an index — `asset_member_user_idx` (declared at schema line 37, created in migration 0030:30). Used by `getAccessibleAssetIds` (authz.ts:10) and `AssetService.list`/`search`/`listPage` when driven from `mediaAssets` join + userId condition.
  - Replicache CVR / client-group tables are indexed on what pull queries by — `replicacheClientGroup` queried only by PK `id` (pull.ts:105, push.ts:84); `replicacheClient` queried by PK `id` (push.ts:70) and by `clientGroupId` (pull.ts:281, covered by `replicache_client_group_idx`).
  - Pagination on list endpoints — not in scope for Task 4; the notifications list currently returns all user rows unbounded (deferred to Task 21 per plan).
  - Appropriate data types — `row_version` integer, `cvr_version` integer, `last_mutation_id` integer, `created_at`/`updated_at`/`read_at`/`expires_at` timestamp, `body` jsonb, text for ids/emails/roles via enum. All correct.
  - Drizzle relational query API check — the pull + service queries use explicit `.innerJoin`/`.leftJoin` rather than `db.query.table.findMany({ with: ... })`. Acceptable here because (a) the queries return custom projections (selected subset of columns with aliases), not the default entity shape relational queries produce, (b) the per-asset scoping uses `inArray(assetId, assetIds)` with a precomputed list — fine for the current cardinality. Not a Task 4 concern; noted for Task 6 / 15 if revisited.
  - pnpm check-types: clean (8/8 tasks green, full-turbo cache hit; no source changes).

---

## Task 5: Verify `$type<>()` on new jsonb columns

Approach: enumerated every `jsonb(` declaration in the schema directory and filtered to columns that are new on this branch (per `git diff main --`). Cross-referenced each against the discriminated union it serializes to in the API layer.

Issues found and fixed:
  1. packages/db/src/schema/notifications.ts:44 — `body: jsonb('body').notNull().default({})` had no `.$type<>()`. Selects of `notifications.body` into `NotificationService.serialize` arrived as Drizzle's default (`unknown`/loose JSON), forcing `row.body as { role: ... }` and `row.body as { fromRole, toRole }` assertions in the serializer (service.ts:133, 139) with no compile-time guarantee that the in-flight row shape matched. Fixed by (a) defining `NotificationBody` as a discriminated union in the schema module — `{ role }` | `{ fromRole, toRole }` | `Record<string, never>` — using literal role strings (`'editor' | 'viewer'`) rather than importing `AssetRole` from `@milkpod/api`, which would invert the package-layering order. (b) Applied `.$type<NotificationBody>()` to the column. The `.notNull().default({})` chain still works because `{}` satisfies the `Record<string, never>` branch of the union. Confirmed via `pnpm build` in `packages/db` and full-repo `pnpm check-types` (8/8 green). Call sites that read `.body` (pull.ts:175, service.ts:161) now receive `NotificationBody` instead of `unknown`; the existing `as` casts in the serializer remain because they narrow one union branch to a specific shape (a type guard by `row.type`), not because the source is untyped. Updating those to `isNotificationOfType`-style guards is a separate refactor, not a Task 5 concern.

Issues noted (no fix needed):
  1. packages/db/src/schema/moments.ts:50 — `scoreBreakdown: jsonb('score_breakdown')` has no `.$type<>()`. Out of scope: the column exists on `main` (verified via `git show main:packages/db/src/schema/moments.ts`) — only `deletedAt`, `rowVersion`, and the matching row-version index are new on this branch. Task 5 is explicitly "new jsonb columns". Noting it as a latent hygiene item; a future sweep or a Task 30 regression sidebar could strongly-type it once a canonical shape is settled (moments/service.ts builds the payload; grep showed no external consumer that would benefit today).
  2. packages/db/src/schema/comments.ts:37 — `evidenceRefs: jsonb('evidence_refs').$type<{ transcriptSegmentIds: string[]; visualSegmentIds: string[] }>()`. Already typed. The new columns on comments.ts on this branch (`deletedAt`, `rowVersion`) are not jsonb — no action.
  3. packages/db/src/schema/asset-members.ts and replicache.ts — no jsonb columns. Nothing to check.
  4. packages/api/src/modules/notifications/service.ts:59 — `type: input.type as NotificationType` is a redundant cast (input is already the discriminated union `RecordInput` whose `type` field is structurally identical to `NotificationType`). Pre-existing; not introduced or caused by this task. Dropped in favor of keeping scope tight.
  5. packages/api/src/modules/notifications/service.ts:91-148 — `serialize(row)` declares `body: unknown` on its public signature despite `notifications.body` now being typed. Intentionally kept permissive: `serialize` is the serialization boundary between DB rows (where a newer-server `type` could write a body shape this deploy doesn't know about) and the public discriminated union (`Notification`). The per-branch `as` inside each `case` is a local narrowing tied to the discriminant check, not an unsafe cast of untyped input. Tightening would force every caller to first narrow — net loss. Kept as-is.

Library improvements made:
  - Used Drizzle's built-in `.$type<>()` on `jsonb()` — the library primitive for type-safe JSON columns. Confirmed the API by reading `node_modules/.pnpm/drizzle-orm@0.44.7*/node_modules/drizzle-orm/column-builder.d.ts:167-172` (the official `$type<TType>(): $Type<this, TType>` declaration with inline JSDoc example `json('details').$type<UserDetails>().notNull()`). No hand-rolled alternative (e.g. `as NotificationBody` on the select, or a runtime Zod reshaper) was written — the Drizzle primitive covers the compile-time need and the DB already enforces JSON validity at the storage layer.
  - The NotificationBody discriminated union type lives in the schema module (co-located with NotificationType) rather than in `@milkpod/api`, preserving the package-layering direction: `packages/db` has no dependency on `packages/api`. Exported via the `schemas.ts` barrel (unchanged — `export * from './schema/notifications'` picks up the new type automatically).

Checklist verification (per `review-prompt.md` Queries & Data Access + TypeScript patterns + Library Leverage):
  - `$type<>()` used on jsonb columns for type-safe access — notifications.body now typed; comments.evidenceRefs was already typed; replicache + asset-members have no jsonb; moments.scoreBreakdown pre-existing and out of scope.
  - Discriminated union over notification type — NotificationBody is a true TS discriminated union (three branches), matching NotificationType's three discriminants one-for-one. Adding a new NotificationType literal forces a compile error in any `switch (row.type)` that doesn't add a branch (e.g. service.ts:128-148) AND forces the body union to be extended — two-sided contract.
  - No `any` types introduced — NotificationBody uses literal string types (`'editor' | 'viewer'`), `Record<string, never>`, and structural object types. `unknown` retained only on `serialize(row: { body: unknown })` as a conscious boundary choice (see issues-noted #5).
  - Appropriate data types — `body jsonb DEFAULT '{}'::jsonb NOT NULL` matches the `Record<string, never>` branch; `row_version integer`, `read_at timestamp` unchanged. No migration generated / needed: `.$type<>()` is a TS-only refinement, not a DDL change (verified by running `pnpm --filter @milkpod/db build` — no migration drift warnings).
  - pnpm check-types: clean (8/8 tasks green across @milkpod/{db,ai,auth,api}, server, web). @milkpod/db built first, then downstream consumers resolved the new type from `dist/` per CLAUDE.md workflow.

---

## Task 6: Audit pull.ts for CVR determinism and per-user visibility filter

Approach: audited pull.ts + cvr.ts + authz.ts against three criteria — (a) non-deterministic sources inside CVR/patch construction, (b) ORDER BY stability on every query whose output order flows into the patch or persisted snapshot, (c) visibility filter correctness for the initial (cold) pull. Grepped for `Date.now|Math.random|new Date(` under modules/replicache/; inspected every `.select()` for ORDER BY; re-read the WHERE clauses against authz.ts.

Issues found and fixed:
  1. packages/api/src/modules/replicache/pull.ts:142-161, 167-184, 276-282 — None of the three hot-path queries (moments, comments, notifications) and the clients LMID query had an ORDER BY. Iteration order of result rows feeds directly into the patch array (`patch.push` in a `for (const m of currentMoments)` loop) AND into the snapshot's insertion order (`nextMoments[m.id] = ...`), which is JSON-serialized and read back on the next pull. Two pulls on identical (cookie, clientGroupID, userID, DB state) could therefore produce different patch array orders, breaking the task's "deterministic output" requirement. The snapshot keyed by id is lookup-insensitive to order, but (a) the patch array order IS observed by Replicache's client-side JSON log / debugger diffs, and (b) `Object.entries(prevSnapshot.moments)` in the del-loops iterates insertion order, which only stays stable if the write side is stable. Fixed by adding `.orderBy(asc(assetMoments.id))`, `.orderBy(asc(assetComments.id))`, `.orderBy(asc(notifications.id))`, and `.orderBy(asc(replicacheClient.id))`. Added `asc` to the drizzle-orm import. A one-line comment in the moments section explains the determinism rationale so future contributors don't drop the ORDER BY as "unnecessary". Verified API signature via `node_modules/.pnpm/drizzle-orm@0.44.7*/node_modules/drizzle-orm/pg-core/query-builders/select.d.ts:527-541` — `.orderBy(...columns)` accepts `asc(col)`/`desc(col)` wrappers exactly as used.
  2. packages/api/src/modules/replicache/pull.ts:247-248 — `value: n as unknown as Record<string, unknown>` is a double-cast through `unknown`, the specific anti-pattern flagged by review-prompt.md TypeScript §2 ("No `as unknown as T`"). Notification is a tagged union with a fixed set of flat + nested-object fields, so a structural serializer is cheap. Fixed by adding `serializeNotification(n: Notification): Record<string, unknown>` alongside the existing `serializeMoment`/`serializeComment` helpers — fresh object literal, no cast, TypeScript widens literal → `Record<string, unknown>` via the fresh-literal rule (same mechanism that makes `serializeMoment`'s return assignable). Fields enumerated explicitly so adding a new Notification type / field forces a compile error here (see isNotificationOfType in types.ts:144 for the discriminant helper).
  3. packages/api/src/modules/replicache/pull.ts:210-214, 242, 264-267 — `nextSnapshot.notifications![n.id]` and `nextSnapshot.notifications![id]` used `!` non-null assertions on an optional field even though `nextSnapshot` was locally initialized with `notifications: {}`. Flagged by review-prompt.md TypeScript §2 ("No unjustified non-null assertions"). `notifications` is optional in `CVRSnapshot` for backwards-compat with pre-deploy Redis snapshots (valid), but the local `!` assertion leaks that concern into every write site. Fixed by refactoring `nextSnapshot` assembly: declare three local `Record<string, ...>` accumulators (`nextMoments`, `nextComments`, `nextNotifications`), populate them in the put-loops, then build `nextSnapshot` once at the end as a single object literal. Same pattern `currentLmids` already used for the clients map — this harmonizes the style. Zero `!` assertions in the final code.

Issues noted (no fix needed):
  1. packages/api/src/modules/replicache/pull.ts:138-184 — Queries inside `db().transaction` are serialized by the underlying pg/postgres-js driver even when wrapped in Promise.all (single connection per tx). `Promise.all` would not speed them up here, would lose the per-query error localization, and would obscure read-order intent. Kept sequential `await`s — correct pattern for the tx-per-pull model.
  2. packages/api/src/modules/replicache/authz.ts:6-12 — `getAccessibleAssetIds` returns assetIds in indeterminate order. That's fed to `inArray(assetMoments.assetId, assetIds)` in the WHERE; Postgres treats IN-list order as irrelevant (sets, not sequences) so result order depends only on the moments query's ORDER BY. The assetIds order leaks nowhere else. No need to sort. Task 8 will re-audit authz.ts for coverage, so leaving the query shape as-is.
  3. packages/api/src/modules/replicache/pull.ts:181-184 — Notifications are NOT filtered by `assetIds`. That's correct for the bell: a user dropped from an asset still receives the `asset.member.removed` notification (membership revoked → no longer in asset_member → notification would be hidden if scoped by assetIds). Scoping only on `recipientId = userId` is the right initial-pull filter for user-owned notifications. Cross-referenced with NotificationService.record (service.ts:49-68) — notifications are always written with the recipient as the read-access key, never the asset's member list.
  4. packages/api/src/modules/replicache/pull.ts:152-154 — `prevSnapshot` cold-sync fallback has `notifications: {}`. Unused except for the `prevSnapshot.notifications ?? {}` line (line 271) reading it. Could drop from the literal (since `?? {}` catches undefined), but keeping it documents the expected shape. Minor, not worth touching.
  5. packages/api/src/modules/replicache/cvr.ts:30-40 — `notifications?` and `clients?` are optional in `CVRSnapshot`. Kept optional because (a) Redis-persisted snapshots from a prior schema (before notifications were added) may still be within the 12-hour TTL window at deploy time, (b) the `?? {}` fallback in pull.ts is the intentional boundary, (c) making them required would force either an eager migration or a runtime cast. Local `Record<string, ...>` accumulators now eliminate the read-side `!` assertions (see fix #3) without touching the type. No change.
  6. packages/api/src/modules/replicache/pull.ts:79 — `createdAt: m.createdAt instanceof Date ? m.createdAt.toISOString() : m.createdAt` — the ternary handles both typed `Date` (from a direct tx query) and serialized-string (from a subsequent JSON round-trip). Not a non-determinism source; the branch is taken on row shape, not on runtime clock. Confirmed.

Library improvements made:
  - Used Drizzle's `asc()` builder for ORDER BY rather than `sql\`\"id\" asc\`` raw templating. Verified the primitive in `node_modules/.pnpm/drizzle-orm@0.44.7*/node_modules/drizzle-orm/pg-core/query-builders/select.d.ts:540-541` — `.orderBy(...columns: (PgColumn | SQL | SQL.Aliased)[])` signature accepts `asc(col)` directly. No raw SQL needed.
  - Replaced `as unknown as Record<string, unknown>` with an explicit serializer fn (fresh literal) — same pattern as existing `serializeMoment`/`serializeComment`. Preserves TypeScript's structural-type narrowing for the call site and makes schema-evolution compile-visible.
  - Refactored snapshot assembly to use three local `Record<string, ...>` accumulators, eliminating all `!` assertions. This is the pattern Drizzle's own result mappers use (accumulate in a typed Map/object, assemble at the end) — matches the "extract shared patterns into typed utilities" review guideline.

Checklist verification (per `review-prompt.md` Queries & Data Access + TypeScript patterns + Library Leverage + Security):
  - No `Date.now()`, `Math.random()`, `new Date()` inside CVR/patch construction — grepped `packages/api/src/modules/replicache` and confirmed all `new Date()` hits are in push.ts / server-mutators.ts (write path — Task 7 scope), not pull.ts / cvr.ts. Pull handler is pure derivation from DB state + prev snapshot.
  - Every ORDER BY has a stable tiebreak — the four queries with loop/patch-affecting output (moments, comments, notifications, replicacheClient) now ORDER BY the primary id column (text ids, unique by PK). ids are single-column PKs so the PK ORDER BY is itself a total order — no additional tiebreak needed.
  - Initial pull visibility filter — `getAccessibleAssetIds(userId)` returns the user's `asset_member` rows' asset ids. Moments/comments filtered via `inArray(assetId, assetIds)` (nested scoping: user → accessible assets → their moments/comments). Notifications scoped via `eq(recipientId, userId)` (user-direct scoping). No global firehose. Verified by reading authz.ts:1-12 and pull.ts:164-213.
  - Cold sync emits `clear` — confirmed at pull.ts:247 (`if (isColdSync) patch.push({ op: 'clear' });`). isColdSync is true iff `prev == null` which is true iff (cookie is null) OR (cvr cache miss/corrupt). Matches Replicache protocol expectation.
  - Del emission on visibility loss — covered by the `!nextMoments[id]` / `!nextComments[id]` / `!nextNotifications[id]` loops at lines 284-299. A revoked membership removes the asset from getAccessibleAssetIds → its moments/comments drop from `currentMoments` → they're absent in nextMoments → del ops are emitted. Task 9 deep-audits this; today's refactor did not regress it.
  - No `any` types introduced — serializeNotification fully typed (Notification → Record<string, unknown>). imports add `asc`, `CVRRow`, `NotificationCVRRow` — all exported from existing modules, no new type.
  - No unjustified `!` — all removed via local-accumulator refactor.
  - No `as T` / `as unknown as T` — serializeNotification replaces the one cast; no new casts added. Verified via `rg 'as unknown as|as Record<' packages/api/src/modules/replicache/pull.ts` → zero hits.
  - Parameterized queries / ORM builders — all queries use Drizzle builders; only raw SQL is the `pg_advisory_xact_lock(hashtext(${lockKey}))` call which uses `sql\`\` template tag with `${lockKey}` interpolation → parameterized (confirmed by Drizzle docs: `sql\`\${x}\`` → `$1` bind, not string concat). No injection surface.
  - Appropriate data types — id columns text, rowVersion integer, createdAt timestamp. PatchOp value is `Record<string, unknown>` which matches Replicache's ReadonlyJSONObject expectation.
  - No N+1 — still single query per entity type (moments, comments, notifications, authors, clients). Author hydration batched via `inArray(userTable.id, [...authorIds])`. ORDER BY does not change query shape.
  - pnpm check-types: clean (8/8 tasks green). @milkpod/api and downstream (server, web) rebuilt; no regressions.

---

## Task 7: Wrap push mutations in a transaction; verify mutation-ID monotonicity

Approach: audited push.ts against the three plan criteria — (a) single outer transaction wrapping LMID reads + mutator writes + LMID advances, (b) duplicate-mutation dedup semantics (<= skip silently), (c) LMID advance on mutator failure so the client rebases rather than re-queuing forever. Cross-referenced the LMID upsert against Drizzle's `setWhere` support and verified nested transactions use savepoints.

Issues found and fixed:
  1. packages/api/src/modules/replicache/push.ts:133-161 (pre-edit) — `handlePush` was NOT wrapped in a single transaction. Each mutation opened its own `db().transaction(...)`, and `getLMID` + the error-path `advanceLMID` ran on the raw pool connection (no tx at all). This created a monotonicity race window: two concurrent pushes from the same clientID could each `getLMID → 5`, both see `mutation.id = 6 > 5`, both enter separate transactions, and the later-to-commit would overwrite the LMID with whatever value it had locally. If two requests arrived in reverse order (push2 with id=7 committed first, push1 with id=6 committed second), the LMID would regress from 7 to 6 — Replicache's client would then re-queue 7, and the server could either dedup or apply twice depending on mutator idempotence. Fixed by wrapping the entire push handler (clientGroup bind, mutation loop, all LMID reads and writes) inside a single `db().transaction(...)` with per-mutation savepoints for isolation. See fixes #2 + #3 below for the monotonicity guard and savepoint strategy.
  2. packages/api/src/modules/replicache/push.ts:53-64, 149-160 (pre-edit) — `advanceLMID`'s `onConflictDoUpdate({ set: { lastMutationId: newId } })` had no monotonicity guard. Even within a single transaction, an out-of-order LMID write would regress the row. Fixed by adding Drizzle's `setWhere: sql\`${replicacheClient.lastMutationId} < ${newId}\`` — the UPDATE only applies when the new id strictly advances the current value. Verified the API in `node_modules/.pnpm/drizzle-orm@0.44.7*/node_modules/drizzle-orm/pg-core/query-builders/insert.d.ts:61-68` (`PgInsertOnConflictDoUpdateConfig.setWhere?: SQL`) — it compiles to a standard Postgres `ON CONFLICT ... DO UPDATE ... WHERE` and keeps the upsert atomic. Unified the single `advanceLMID` helper to take `tx` as its first arg so both the error-path and success-path advances run inside the outer transaction with the monotonicity guard applied consistently.
  3. packages/api/src/modules/replicache/push.ts:133-161 (pre-edit) — No isolation between mutations. With per-mutation transactions, a crash in mutation 3 would roll back only mutation 3. But in a single outer transaction, any throw would roll back ALL prior mutations in the batch — violating Replicache's rebase contract (the client expects mutations with advanced LMIDs to be either applied or explicitly rejected, not silently reverted). Fixed by wrapping each mutator call in a nested `tx.transaction(async (subTx) => { ... })` — Drizzle's nested transaction issues `SAVEPOINT spN` / `ROLLBACK TO SAVEPOINT spN` / `RELEASE SAVEPOINT spN` under the hood (verified in `node_modules/.pnpm/drizzle-orm@0.44.7*/node_modules/drizzle-orm/node-postgres/session.js:206-220`). If a mutator throws, only its writes roll back; the outer tx stays alive, and the subsequent LMID advance commits with the rest of the batch.
  4. packages/api/src/modules/replicache/push.ts:170-184 (pre-edit) — `console.error('mutator crashed', name, err instanceof Error ? err.message : err)`. The fallback branch logs the raw `err` (which is `unknown` from the catch binding). Per review-prompt.md §Common Patterns #1 (real-world finding: `err` can contain connection strings, SQL state, stack traces), changed to `String(err)` for the non-Error path. Same pattern applied to the user-poke catch block at what is now line 227-230.
  5. packages/api/src/modules/replicache/push.ts:75-99 (pre-edit) — ClientGroup bind/create (the trust boundary check) ran on the raw pool connection, outside any transaction. Fixed by moving it inside the outer `db().transaction(...)` so the check and any subsequent LMID writes in this push see consistent state. If the clientGroup creation races another push, `onConflictDoNothing` absorbs the duplicate. Forbidden returns early with a typed discriminant (`{ forbidden: true }`) that escapes the transaction without committing any work.

Issues noted (no fix needed):
  1. packages/api/src/modules/replicache/push.ts:168-172 — The `serverMutators[mutatorName] as (tx, args: unknown, ctx) => Promise<void>` cast cannot be eliminated without a per-mutator switch statement (8 cases). The args-per-name correlation across the MutatorName union is not expressible in TypeScript: `parsed.data` is a union of all possible args shapes, `serverMutators[mutatorName]` is a union of all mutator signatures, and TS cannot correlate the two branches on the same literal name without an explicit discriminant. The Zod schema validation one line above (`schema.safeParse(mutation.args)` with `schema = mutatorArgsSchemas[mutatorName]`) provides the runtime guarantee that parsed.data matches the target mutator's args — this is the right boundary for the cast. Kept with a justifying comment.
  2. packages/api/src/modules/replicache/server-mutators.ts:38-48 — `requireEditor` uses `AssetMemberService.isEditor(assetId, userId)` which calls `db()`, not the transaction. Inside a mutator now wrapped in a savepoint of the outer tx, this auth check runs on a SEPARATE pool connection. Consequences: (a) the check doesn't see uncommitted writes from earlier mutations in this push (fine — none of our mutators modify membership), (b) the check could see a concurrent revocation that committed during this tx (also fine — at worst we apply one extra write before the next pull rebases). Out of scope for Task 7 per the plan's own task ordering — Task 8 (authz.ts coverage audit) is where this will be revisited. Leaving as-is preserves the current service API.
  3. packages/api/src/modules/replicache/push.ts:204 — `(parsed.data as { assetId: string }).assetId` is a structural cast that could be replaced with a branded discriminant on the args-schemas side. Not worth the refactor — every non-user-scoped mutator's args includes assetId by contract (enforced by the zod schemas in packages/sync/src/mutators/*.ts lines 20-35, 57-64, 86-90 for moments; equivalent for comments), the USER_SCOPED_MUTATORS set explicitly partitions the ones without, and this call site is the single consumer. A tighter type would require adding a tag to every args schema. Kept with the existing narrow.
  4. packages/api/src/modules/replicache/push.ts:123-156 — No gap detection in LMID progression. If `mutation.id > lastMutationId + 1` (client jumped forward), we silently advance to mutation.id, skipping the intermediate mutations. Per Replicache's push protocol, mutations are submitted in strict order per clientID and a gap would indicate a client-side bug. Current behavior (advance to latest, log nothing, trust the protocol) matches Rocicorp's reference row-version strategy implementation. If we later add observability, this would be a good place to emit a counter.
  5. packages/api/src/modules/replicache/push.ts:151 — `getLMID` runs once per mutation, inside the tx. For a batch of 100 mutations this is 100 SELECTs in series. Acceptable because (a) pushes are small (Task 12 will cap at 100), (b) the SELECT is on `replicacheClient.id` PK so each query is an index lookup <1 ms, (c) the read must be inside the tx anyway to be consistent with concurrent writes. A future optimization: fetch all LMIDs for the client group in one query before the loop, then track progress in a Map. Not worth the complexity today.
  6. packages/api/src/modules/replicache/push.ts:215-232 — Pokes fan out AFTER the outer transaction commits (post-`await db().transaction(...)`). Task 10 explicitly audits this; verified here as unchanged and correct. No poke is emitted inside the transaction body.

Library improvements made:
  - Replaced hand-rolled monotonicity logic with Drizzle's `onConflictDoUpdate({ setWhere })` primitive. Without `setWhere`, the alternative would have been a raw SQL `UPDATE ... WHERE last_mutation_id < $1` + separate INSERT-on-not-exists, or an application-level read-compare-write that doesn't survive concurrent transactions. `setWhere` compiles to `ON CONFLICT ("id") DO UPDATE SET last_mutation_id = excluded.last_mutation_id, last_modified = excluded.last_modified WHERE replicache_client.last_mutation_id < $newId` — a single atomic statement that Postgres guarantees is linearizable under serializable isolation.
  - Used Drizzle's nested-transaction primitive (`tx.transaction(...)`) rather than hand-rolled `tx.execute(sql\`savepoint X\`)` / `rollback to savepoint X` / `release savepoint X`. Verified the implementation at `node_modules/.pnpm/drizzle-orm@0.44.7*/node_modules/drizzle-orm/node-postgres/session.js:206-220` — Drizzle auto-generates unique savepoint names (`sp${nestedIndex+1}`), handles both the happy path (RELEASE) and error path (ROLLBACK TO + rethrow), and maintains the outer tx's liveness correctly. No chance of savepoint name collision or leaked savepoints.
  - Unified `advanceLMID` into a single tx-aware helper (was previously duplicated logic between inline tx.insert + the helper that used the pool `db()`). One call site, one monotonicity invariant — harder to skip by accident.

Checklist verification (per `review-prompt.md` Queries & Data Access + Security + Resilience + TypeScript patterns + Library Leverage + Common Patterns):
  - Transactions for atomic operations — entire push handler (clientGroup bind, each mutation's writes, LMID advance) runs in a single `db().transaction(...)`. Per-mutation isolation via nested transactions (savepoints). Verified by reading push.ts:97-211.
  - No duplicate mutations errored — `if (mutation.id <= lastMutationId) continue;` at push.ts:152-156. Silently skipped per Replicache's retry-produces-duplicates design invariant.
  - LMID monotonicity — `setWhere: sql\`${replicacheClient.lastMutationId} < ${newId}\`` on every upsert (helper at push.ts:70-74, used from all call sites). Postgres enforces this atomically via the ON CONFLICT UPDATE predicate.
  - Error path still advances LMID — push.ts:196 (`advanceLMID` after the try/catch) runs regardless of whether the savepoint rolled back. Client rebases via next pull instead of re-queuing forever. Task note: Replicache does not yet support "error record" metadata per LMID (that's a v2 protocol extension); today's behavior matches Rocicorp's reference server.
  - No raw SQL injection — `sql\`${replicacheClient.lastMutationId} < ${newId}\`` uses Drizzle's parameterized template tag (verified elsewhere: `${x}` binds `$N`, not string-concat). `newId` is a TypeScript `number`, narrowed from `mutation.id` which was validated as `t.Number()` at the Elysia route (index.ts:52).
  - No sensitive data in logs — err fallback branch now uses `String(err)` instead of raw `err` (push.ts:187, 229). Matches review-prompt.md Common Patterns #1.
  - No secrets in error responses — the only return to the client is `{}` (success) or `{ forbidden: true }` → 403 at the route (index.ts:38-42); no upstream errors bubble.
  - Proper HTTP status codes — 200 on success, 403 on forbidden clientGroup bind. (The rest is Elysia's default.)
  - Idempotency — mutation.id dedup + `ON CONFLICT DO NOTHING` on the create mutators (server-mutators.ts:72, 133) + `setWhere` monotonicity guard together make the push handler idempotent under retry.
  - Input validation — Elysia TypeBox validates the push body shape at index.ts:47-58 (pushVersion, clientGroupID length, mutations array shape). Per-mutation args validated at push.ts:139-140 against the `mutatorArgsSchemas[name]` zod schema. Unknown mutator names are logged + LMID-dropped (push.ts:124-131) rather than crashing the batch.
  - Auth / authorization — route is `.use(authMacro)` + `auth: true` (index.ts:11, 46); userId comes from the authed session and is passed into every mutator ctx. ClientGroup userId pinning enforced at push.ts:106-107.
  - Rate limiting — not in this task's scope. Task 13 covers rate-limiter registration for /api/replicache/pull and /api/replicache/push.
  - No non-null assertions — zero `!` in push.ts after the refactor (verified: `grep '!' packages/api/src/modules/replicache/push.ts` returns only logical-not operators like `if (!group)` and `if (!isKnownMutator(...))`).
  - No `any` types — one justified `any` alias `type DbTx = any` for the Drizzle tx handle (already existed in server-mutators.ts), with a comment explaining why (driver-version variance). Not introduced by this task.
  - No `as` / `as unknown as T` — one structural cast `as { assetId: string }` on parsed.data, one dispatch cast on `serverMutators[mutatorName]`. Both justified in-code with comments (see issues-noted #1, #3).
  - Exhaustive switch — no switch statements in push.ts; the dispatch is by keyed record (mutatorName → runner). Adding a new mutator name requires adding to both `serverMutators` (server-mutators.ts) AND `mutatorArgsSchemas` + `clientMutators` (packages/sync/src/mutators/index.ts), and any name in MutatorName that isn't in USER_SCOPED_MUTATORS falls to the assetId-poke branch — which will throw at runtime if the args schema lacks an assetId field. Could be made compile-visible with a branded args type; noted in issues-noted #3.
  - Library leverage — `setWhere` (Drizzle), nested `tx.transaction` (Drizzle savepoints), `onConflictDoNothing`/`onConflictDoUpdate` (Drizzle), `safeParse` (Zod). No hand-rolled alternatives.
  - Poke emission AFTER commit — push.ts:215-232 runs after `await db().transaction(...)` resolves. No poke is emitted inside the transaction body.
  - pnpm check-types: clean (8/8 tasks green across @milkpod/{env,sync,db,ai,auth,api}, server, web). No regressions.

---

## Task 8: Audit authz.ts coverage for read (pull) and write (push)

Approach: enumerated every synced entity (moments, comments, notifications — nothing else is published to the client via pull.ts), mapped each to its read-filter (pull) and write-check (push), and looked for divergence between the two sides. Confirmed every mutator in `serverMutators` re-checks permissions on the server before writing. Traced `requireEditor`'s dependency graph (lived in server-mutators.ts, delegates to `AssetMemberService.isEditor`, which calls `getRole`) to verify there was no parallel implementation.

Issues found and fixed:
  1. packages/api/src/modules/replicache/authz.ts (pre-edit was 12 lines) — authz.ts exposed only the read-side helper (`getAccessibleAssetIds`). The write-side auth primitive (`requireEditor` + `MutatorForbiddenError`) lived in server-mutators.ts, and push.ts imported the error class from there too. Plan wording explicitly calls for authz.ts to cover both sides: "don't let pull/push reimplement checks divergently". Fixed by consolidating into authz.ts: moved `MutatorForbiddenError` and `requireEditor` from server-mutators.ts into authz.ts, and documented the full entity matrix (moment / comment / notification × pull-read / push-write) in the module header so future contributors see at a glance which authz primitive applies where. Updated server-mutators.ts to `import { requireEditor } from './authz'` (dropping its own `AssetMemberService` import — the service is now reached transitively through authz) and updated push.ts to `import { MutatorForbiddenError } from './authz'` (previously from './server-mutators'). Net effect: one file, one module, one set of rules — pull.ts and server-mutators.ts can no longer drift because they both reach through the same choke point. `requireEditor` still delegates to `AssetMemberService.isEditor` so the role-threshold definition still lives in one place (the membership service); authz.ts is the Replicache-specific facade over it.

Issues noted (no fix needed):
  1. packages/api/src/modules/replicache/pull.ts:182 — Notifications are filtered inline via `eq(notifications.recipientId, userId)`, not through an authz.ts helper. Considered adding a `notificationFilter(userId)` SQL-fragment helper. Rejected because (a) the filter IS a single column comparison expressed in Drizzle's query DSL, not a decision that needs a separate query — wrapping it adds indirection without eliminating the call-site repetition; (b) the identical `eq(notifications.recipientId, ctx.userId)` clause also appears in both notification mutators (server-mutators.ts:158 and 180) — so the rule is applied in three places with the same literal-column predicate, and divergence would be a visible code-review issue (not a subtle behavior delta). Documented this design choice explicitly in the authz.ts header block.
  2. packages/api/src/modules/replicache/authz.ts:62 — `requireEditor` still calls `AssetMemberService.isEditor` which uses `db()` (the pool), not the push transaction. Means the auth check runs on a SEPARATE connection from the mutator's writes. This is safe TODAY because no mutator modifies asset membership — a user's editor status is invariant over a single push batch. If a future mutator adds or revokes memberships, the check must be revised to accept the tx and query inside it (otherwise prior mutations in the same push could "reauthorize" a user whose membership was just removed, or vice versa). Documented the invariant in authz.ts:61-65. No code change today.
  3. packages/api/src/modules/replicache/authz.ts:48-53 — `getAccessibleAssetIds` queries `assetMembers` directly rather than going through an `AssetMemberService.listUserAssetIds(userId)` method. Considered adding the method to the service for consistency. Rejected because (a) no other call site in the codebase needs "all assets for user X" as a distinct operation (`AssetService.list` uses a JOIN + WHERE against `assetMembers`, not a preliminary id fetch), (b) adding the method solely to reach through it from authz.ts is indirection without benefit. If a second consumer appears, extract then. Kept the direct query.
  4. packages/api/src/modules/replicache/server-mutators.ts — `notificationMarkRead` / `notificationMarkAllRead` enforce ownership via the WHERE clause (`eq(notifications.recipientId, ctx.userId)`) rather than an explicit `requireOwner(notificationId, userId)` check. This is the stronger pattern: the ownership check and the write are atomic in a single SQL statement — there's no TOCTOU window between "verify I own this" and "modify it". Prefer this over a separate SELECT + UPDATE that would require a transaction or optimistic-lock to stay consistent. No helper abstraction needed.
  5. packages/api/src/modules/replicache/pull.ts:164 — moments/comments filter uses `inArray(assetId, assetIds)` where `assetIds` comes from `getAccessibleAssetIds`. A sufficiently determined attacker who spoofs `assetIds` cannot — the function is called server-side with the session's `userId`, and the only path from route → handler → filter is `handlePull(user.id, body)` where `user.id` is derived from the auth middleware. Confirmed at index.ts:14 and push.ts:88 that client-supplied body fields are never substituted for the authed user id.
  6. packages/api/src/modules/replicache/index.ts:38 — push route returns `{ forbidden: true }` as a 403 when the clientGroup is bound to a different user. This is a trust-boundary check (not content-level authz), verified at push.ts:106-107. It catches the case where an attacker who has a valid session steals another user's clientGroupID and tries to push into it. The check + rejection is in the right layer.

Library improvements made:
  - Reused the existing Drizzle patterns (no library change needed): `eq`, `inArray` for WHERE clauses; `AssetMemberService.isEditor` as the canonical role-threshold query. Consolidation was a pure TypeScript module-boundary refactor — the library surface didn't need to change. Verified Drizzle's `db().select().from().where()` call pattern in `node_modules/.pnpm/drizzle-orm@0.44.7*/node_modules/drizzle-orm/pg-core/db.d.ts:281` (`transaction<T>(transaction: (tx: PgTransaction<...>) => Promise<T>, config?: PgTransactionConfig): Promise<T>`) — the tx handle has the same query-builder surface as `db()` itself, which is why the existing `requireEditor` using `db()` rather than tx is a behavioral choice (cross-connection, not a library limitation). Noted in issues-noted #2 for future follow-up.

Checklist verification (per `review-prompt.md` Security + Queries & Data Access + Code Quality + TypeScript patterns + Library Leverage):
  - Auth and authorization checks on every endpoint, not just the router level — verified by enumeration:
      - pull.ts: user-id comes from auth middleware (index.ts:14, `user.id`), clientGroup → user binding check at pull.ts:105-118, moments/comments visibility filter via `getAccessibleAssetIds(userId)`, notifications via `recipientId = userId`.
      - push.ts: user-id from auth middleware (index.ts:37), clientGroup → user binding check at push.ts:106-107, every mutator invokes `requireEditor` (moments/comments) or relies on an atomic-WHERE ownership filter (notifications).
      - Every mutator in serverMutators re-checks ownership — all 6 asset-scoped mutators call `await requireEditor(args.assetId, ctx.userId);` as the first statement; both notification mutators include `eq(notifications.recipientId, ctx.userId)` in the UPDATE WHERE.
  - Input validation on every route — pull.ts route has TypeBox validator at index.ts:25-31 (pullVersion literal, clientGroupID length, cookie nullable number). push.ts route at index.ts:47-61 (pushVersion literal, clientGroupID length, mutations array with per-mutation Object schema). Per-mutation args validated against mutatorArgsSchemas[name] at push.ts:139-140 before dispatch.
  - No sensitive data in logs — MutatorForbiddenError construction uses its own message string (`User ${userId} is not an editor of asset ${assetId}`) — ids are not secrets and don't leak tokens or PII. push.ts catch block uses `err instanceof Error ? err.message : String(err)` (verified at push.ts:187).
  - No secrets in error responses — 403 response at index.ts:38 returns `{ message: 'Client group is bound to another user' }`. No database internals, no ACL rule details, no stack traces bubbled.
  - Proper HTTP status codes — 403 for cross-user clientGroup attempt (both routes). 200 for mutator-forbidden (per design: push response is per-mutation via advanced LMID + no patch; not a route-level 403). Matches Replicache's protocol.
  - No `any` types introduced by this task — `DbTx = any` alias stayed in server-mutators.ts (see Task 7 notes for justification — driver-version variance); `requireEditor` is fully typed (`assetId: string, userId: string` → `Promise<void>`). The `MutatorForbiddenError` class extends `Error` with no type shortcuts.
  - Exhaustive switch statements — no switch over the mutator union; dispatch is by record lookup (`serverMutators[mutatorName]`). Enforced at the type level: adding a mutator requires extending `clientMutators`, `serverMutators`, and `mutatorArgsSchemas` in lockstep (packages/sync/src/mutators/index.ts and packages/api/src/modules/replicache/server-mutators.ts). Missing from any of the three produces a TS error at push.ts.
  - No magic numbers — clientGroupID min/max enforced at the TypeBox level (1, 200) in index.ts; push mutations array has no hard cap yet (Task 12 will add one).
  - Reuse existing utilities — `requireEditor` uses `AssetMemberService.isEditor` (canonical role check, shared with REST routes at asset-members/index.ts:21,46,68). No duplicate implementation.
  - Extract duplicated blocks — merged by this task: `MutatorForbiddenError` + `requireEditor` now live in one module (authz.ts) and are imported by both sides. Zero duplication.
  - Coverage matrix for every synced entity — documented in-code as the authz.ts header block:
      - moment: pull via `getAccessibleAssetIds` ∋ assetId, push via `requireEditor(assetId, userId)`
      - comment: pull via `getAccessibleAssetIds` ∋ assetId, push via `requireEditor(assetId, userId)`
      - notification: pull via `recipientId = userId`, push via atomic-WHERE `recipientId = userId`
      - assets / asset_members: NOT published via sync, authz enforced at REST (documented).
  - Library leverage — no library primitive was missed. The consolidation was a module-boundary refactor, not a library-level change.
  - pnpm check-types: clean (8/8 tasks green across @milkpod/{env,sync,db,ai,auth,api}, server, web). No regressions. @milkpod/api and downstream re-ran due to source changes; @milkpod/{db,sync,ai,auth} cache-hit.

---

## Task 9: Emit `del` patches on visibility loss

Approach: traced every way a row can leave the user's visible set (physical delete, soft delete via dismissedAt/deletedAt, membership revocation) and verified each converges on the same CVR-diff mechanism in pull.ts. Cross-checked that the del loop reconstructs the correct Replicache key for each entity (uses `row.a` for asset-scoped prefixes, bare id for user-scoped notifications). Re-read AssetMemberService.removeMember to confirm a Replicache poke fires post-commit so the revoked user pulls immediately. Ran `pnpm check-types` to verify the `DbHandle` union type plays nicely with the existing tx handle.

Issues found and fixed:
  1. packages/api/src/modules/replicache/authz.ts:46-52 + packages/api/src/modules/replicache/pull.ts:164 — `getAccessibleAssetIds` ran on `db()` (the pool) from inside the pull transaction, opening a narrow race window: if a membership revocation committed between the membership read (pool connection) and the subsequent moments/comments reads (tx connection), the current pull would return the revoked asset's moments/comments as "current" — delaying the `del` ops by one pull cycle. Fixed by adding a second parameter to `getAccessibleAssetIds` typed as `DbHandle = ReturnType<typeof db> | Parameters<Parameters<ReturnType<typeof db>['transaction']>[0]>[0]` (a union that accepts either the pool or a tx), defaulting to `db()` for backwards-compat. `pull.ts` now passes its tx, so the membership read is scheduled on the same connection as the moment/comment queries. The remaining READ-COMMITTED race (a revocation could still commit between two separate SELECTs within the tx) is self-healing: the next pull picks up the revocation via getAccessibleAssetIds and emits the correct `del` ops. Documented this tradeoff inline on both files. Alternative considered and rejected: `INNER JOIN assetMembers` on each entity query — would fully eliminate the cross-statement race per query, but requires `getTableColumns(...)` projection to keep the flat Moment/Comment result shape, and still leaves the moments-vs-comments cross-query race without repeatable-read isolation. The tx-handle change is the minimal, principled fix that improves consistency without introducing transaction-isolation complexity.
  2. packages/api/src/modules/replicache/pull.ts:283-301 — Del loop had a one-line comment ("Deletions: rows present in prev snapshot but not current") that didn't enumerate the scenarios it covers. Expanded to a block comment documenting all three paths (physical delete, soft delete via dismissedAt/deletedAt, membership revocation), how each manifests as "absent from next snapshot", and why `row.a` is necessary (to reconstruct `moment/<assetId>/<id>` keys after the user has already lost access to the asset). Also called out that notifications are user-scoped (no assetId segment in the key). This makes the CVR-diff correctness argument visible to future readers without them needing to re-audit the whole pull handler.
  3. packages/api/src/modules/replicache/pull.ts:158-170 — Added a block comment before the `getAccessibleAssetIds` call noting that assets themselves are NOT published via sync (per packages/sync/src/keys.ts — only moment/comment/notification are in `SYNC_ENTITIES`). Prevents a future contributor from thinking "why don't we emit a `del` for the asset too?" — the answer is that REST cache invalidation handles asset-level disappearance, not the CVR diff.

Issues noted (no fix needed):
  1. packages/api/src/modules/replicache/pull.ts:283-301 — The del loop correctly handles the case where `prevSnapshot.moments` contains a row whose asset the user no longer has access to. `row.a` is carried in the CVRRow record specifically so the key can be reconstructed from the prev snapshot alone, without re-querying the asset. This is the full purpose of the `a` field on CVRRow — verified by reading cvr.ts:5-10 and confirming it's populated at snapshot write time (pull.ts:254, 265).
  2. packages/api/src/modules/replicache/pull.ts:195-216 — Notifications have NO soft-delete column (no dismissedAt, no deletedAt on the notifications schema) and no mutator currently hard-deletes them (grepped `packages/api/src` for `delete(notifications)` / `DELETE FROM notification` — zero hits). In practice, notifications accumulate until the next pull reconfigures the recipientId filter (never happens — recipientId is immutable) or the server's `NotificationService.serialize` returns null for a row (only when the row's discriminant `type` doesn't match a known variant — a forward-compat scenario for a newer-server writing a type this deploy doesn't know). The del branch for notifications is therefore future-proofing; it would fire on (a) a future manual hard-delete path or (b) schema evolution where a row falls out of the known-types union. Keeping it correct by the same "absent from next" rule.
  3. packages/api/src/modules/replicache/pull.ts:164 — The `assetIds` variable still flows into `inArray(assetMoments.assetId, assetIds)` rather than being replaced by an `INNER JOIN assetMembers` predicate. The JOIN approach would eliminate the within-tx cross-statement race (a membership revoke committing between the moments SELECT and the comments SELECT). Rejected because (a) requires `getTableColumns(assetMoments)` projection to preserve the flat Moment result shape, adding boilerplate, (b) the remaining race is extremely narrow (sub-millisecond window on the same pool connection), (c) self-heals in the next pull, (d) Replicache's design tolerates eventual consistency across pull boundaries. The tx-handle change (fix #1) captures the practical benefit without the complexity. Documented this tradeoff in authz.ts's function docblock.
  4. packages/api/src/modules/replicache/cvr.ts — No changes required. The Redis snapshot store is authz-agnostic; it persists whatever pull.ts writes. The CVR diff logic (which entities to del) lives in pull.ts, not cvr.ts. Re-read cvr.ts:1-85 to confirm: pure KV-over-Redis with JSON codec and 12h TTL — no state machine concerns, no authz concerns.
  5. Same READ-COMMITTED race exists in authz.ts's `requireEditor` for push (documented in Task 8's own notes and in authz.ts:60-65). Out of scope for Task 9 (write side, not visibility-loss). Left as-is; Task 8's comment explicitly acknowledges the current behavior is safe because no mutator modifies membership.

Library improvements made:
  - Used Drizzle's `Parameters<Parameters<ReturnType<typeof db>['transaction']>[0]>[0]` pattern to derive the tx type automatically from the transaction signature — same idiom as packages/api/src/modules/podcasts/service.ts:15. This tracks the Drizzle version (0.44.7) without a manual import chain through `NodePgDatabase` / `PgTransaction<...>` generic parameters, and stays correct through driver upgrades.
  - Wrapped the tx + pool acceptance in a named `DbHandle` union type so future authz helpers can follow the same pattern. Exported implicitly via the function signature; no need to promote it to a public type.
  - No library primitive was missed for the del-emission logic itself. Replicache's row-version-based pull strategy (Rocicorp's reference) is exactly this "diff prev-snapshot against current-set" mechanism — cross-referenced with the design intent in sync-engine-plan.md and with the `row_version` trigger work from Task 1 (migrations 0033, 0037, 0038). No higher-level abstraction — the library IS the pattern.

Checklist verification (per review-prompt.md Queries & Data Access + Security + Resilience + Library Leverage + TypeScript patterns + Common Patterns):
  - Del on membership revocation — verified by tracing: `AssetMemberService.removeMember` (service.ts:262-302) deletes the asset_member row in a tx, then emits a poke via `emitReplicachePokes([userIdToRemove], assetId)` post-commit. User's client receives poke → pulls → `getAccessibleAssetIds(userId, tx)` no longer returns that assetId → `inArray(assetId, assetIds)` filter excludes every moment/comment on the revoked asset → `nextMoments` / `nextComments` omit them → del-loop emits `{ op: 'del', key: 'moment/<assetId>/<id>' }` / `{ op: 'del', key: 'comment/<assetId>/<id>' }` using the assetId captured in `row.a` of the prev snapshot.
  - CVR diff detects visibility loss (not just physical deletion) — verified: the del-loop is "for each id in prevSnapshot where not in nextSnapshot, emit del". Three distinct root causes (physical delete, soft delete, membership revoke) all manifest as "absent from next"; the mechanism is indifferent to why. See pull.ts:283-301 and the new block comment.
  - Soft deletes covered — both `dismissedAt` (legacy REST path, set by moments/service.ts:114 and comments/service.ts:79) and `deletedAt` (Replicache mutator path, set by server-mutators.ts:86 and :145) are `isNull(...)`-filtered at pull.ts:174-175 + 185-186. Soft-deleted rows fall out of current and get `del` ops.
  - Assets not synced (by design) — confirmed via packages/sync/src/keys.ts:1 (`SYNC_ENTITIES = ["moment", "comment", "notification"]` — no "asset" entity). Asset-level visibility loss surfaces via REST cache invalidation, which is outside this task's scope. Documented in-code.
  - asset_member rows not synced — same as above; no authz.ts helper publishes them. Their authz is enforced at REST boundaries (asset-members/index.ts).
  - No `any` types introduced — `DbHandle` is a precise union; `getAccessibleAssetIds` parameter is typed to accept either pool or tx; no `as` / `as unknown as T` added. Verified by reading the changed files.
  - No unjustified `!` — zero in authz.ts and pull.ts after this task.
  - No raw SQL injection — `getAccessibleAssetIds` uses Drizzle's `.select().from().where()` builder chain with `eq(...)`; the tx handle is the same Drizzle interface. No template strings, no `sql` tag with raw concat.
  - No sensitive data in logs — no new logging added; the existing path is unchanged.
  - Proper error handling — no new catch blocks added; visibility-loss is a data-path phenomenon, not an error. The pull handler's existing error surface (tx rollback on throw, Elysia's default 500) is preserved.
  - Appropriate data types — `DbHandle` union captures both handle types without collapsing their method signatures; `.select()` / `.from()` / `.where()` are statically resolved against the appropriate concrete type at call site. Result type of `rows.map(r => r.assetId)` is still `Promise<string[]>` — no signature change for the caller.
  - Reuse existing utilities — `getAccessibleAssetIds` is THE shared helper for "what assets can this user see via sync" (single caller today, but the shape is right for future REST/admin tools that want a per-user asset list without manual membership logic). Did not duplicate the query inline in pull.ts.
  - Exhaustive switch — no switch statements in the touched files. Dispatch is by object-entries iteration, which is structurally exhaustive over the CVR snapshot's entity buckets.
  - Library leverage — Drizzle's builder + tx primitives. No library API was under-used; no hand-rolled wrapper was eliminated because none existed.
  - Pokes after tx commit — verified AssetMemberService.removeMember (service.ts:293-300) fires `emitReplicachePokes` AFTER `await db().transaction(...)` resolves. Task 10 will re-audit all poke sites; this task only verified the revoke path.
  - Del key reconstruction from prev snapshot — `row.a` is present on both CVRRow writes (pull.ts:254, 265) and reads (pull.ts:287, 292). Confirmed stable across snapshot versions by reading cvr.ts:5-10 (`a` is required, not optional).
  - pnpm check-types: clean (8/8 tasks green). @milkpod/api and downstream re-ran; @milkpod/{db,sync,ai,auth} cache-hit.

---

---

## Task 10: Verify realtime poke is sent AFTER transaction commit

Approach: enumerated every poke emitter across the branch (`grep -rn "emitReplicachePokes\|replicacheEvents\.emit\|\.poke(\|pokeMembers" packages/api/src`) and traced each call against its surrounding transaction boundary. Cross-checked server-mutators.ts to confirm mutators never emit pokes from inside the savepoint. Read the whole poke bus contract (events/replicache-events.ts) to document the invariant for future contributors.

Call-site audit (one bullet per emitter, all verified post-commit):
  - packages/api/src/modules/replicache/push.ts:219 — `AssetMemberService.pokeMembers(assetId)` runs after `await db().transaction(...)` on line 98 resolves. The mutation loop collects `affectedAssetIds` into a Set inside the tx body and returns it; fan-out happens outside. Post-commit. ✅
  - packages/api/src/modules/replicache/push.ts:226 — `emitReplicachePokes([userId], '')` for user-scoped mutators (notificationMarkRead / notificationMarkAllRead). Same post-commit block. ✅
  - packages/api/src/modules/asset-members/service.ts:169 — `NotificationService.poke(existingUser.id, assetId)` in `AssetMemberService.invite` fires after the `await db().transaction(...)` on line 154-168 resolves. The transaction only contains the `assetMembers` insert + `NotificationService.record(tx, ...)` write — no poke inside. Post-commit. ✅
  - packages/api/src/modules/asset-members/service.ts:294 — `emitReplicachePokes([userIdToRemove], assetId)` in `AssetMemberService.removeMember` fires after the `await db().transaction(...)` on line 271-286 resolves. Same pattern as invite. Post-commit. ✅
  - packages/api/src/modules/asset-members/service.ts:82 — `pokeMembers` helper itself does a `SELECT + emit`; no transaction. Callers are responsible for ensuring they invoke `pokeMembers` after their own tx commits. ✅
  - packages/api/src/modules/moments/service.ts:67 — `pokeMembers(assetId)` after `await db().delete(...)` (no transaction). ✅
  - packages/api/src/modules/moments/service.ts:88 — `pokeMembers(assetId)` after `await db().insert(...).returning()` (no transaction). ✅
  - packages/api/src/modules/moments/service.ts:104 — `pokeMembers(updated.assetId)` after `await db().update(...).returning()` (no transaction). ✅
  - packages/api/src/modules/moments/service.ts:119 — same pattern as :104. ✅
  - packages/api/src/modules/comments/service.ts:47 — `pokeMembers(assetId)` after `await db().delete(...)` (no transaction). ✅
  - packages/api/src/modules/comments/service.ts:68 — `pokeMembers(assetId)` after `await db().insert(...).returning()` (no transaction). ✅
  - packages/api/src/modules/comments/service.ts:84 — `pokeMembers(updated.assetId)` after `await db().update(...).returning()` (no transaction). ✅
  - packages/api/src/modules/notifications/service.ts:77 — `emitReplicachePokes([recipientId], assetId)` inside `NotificationService.poke()`. The function itself has no tx; it's always called outside one. The JSDoc at service.ts:70-74 already documents "Call this AFTER the transaction that created the notification has committed". Both callers (asset-members:169 above) comply. ✅

Server-mutators.ts audit: grepped `emitReplicachePokes|pokeMembers|replicacheEvents` against `packages/api/src/modules/replicache/server-mutators.ts` — zero hits. Mutators only write to the DB via the savepoint `tx`; they never emit pokes themselves. The poke fan-out is centralized in push.ts post-commit, driven by the mutation loop's collected `affectedAssetIds` / `userPokeNeeded` summary. This is the right architecture: mutators describe effects, the orchestrator (push.ts) decides when to broadcast.

Issues found and fixed:
  1. packages/api/src/events/replicache-events.ts — Module had no top-of-file docblock stating the post-commit invariant. The invariant is load-bearing for correctness (a poke emitted inside an uncommitted tx causes stale pulls: the receiving client's pull runs on a different pool connection and sees the pre-commit snapshot, resulting in a silent miss until the next unrelated trigger). Added a module-level docblock explicitly naming every emitter entry point (`emitReplicachePokes`, `AssetMemberService.pokeMembers`, `NotificationService.poke`), stating the MUST-be-post-commit contract, explaining the failure mode it prevents, and pointing at push.ts:214-233 as the canonical fan-out pattern. Also enhanced the `emitReplicachePokes` JSDoc to restate the invariant at the call-site where a contributor would look for it.
  2. packages/api/src/events/replicache-events.ts:70-72 — Silent catch on `publisher.publish(...)` with no comment explaining why the fallback path exists. Per review-prompt.md Common Patterns #5 (silent error swallowing). Not a behavior change — the fallback still emits locally — but added a two-line comment identifying this as the "Redis unavailable" fallback path and calling out that other replicas will miss this specific poke (they'll catch up on the client's next regular pull interval). Matches the existing comment style in `events/asset-events.ts:95-97`.

Issues noted (no fix needed):
  1. packages/api/src/events/replicache-events.ts:70 — Floating Promise from `publisher.publish(...).catch(...)`. Intentional: the publish is fire-and-forget by design (the caller has already committed and returned; we don't want to block the HTTP response on Redis roundtrip). `.catch()` handles the only failure mode we care about (pub failure → local fallback). ioredis tracks the connection internally; any protocol-level errors surface through the shared ioredis error handler (set up in queue/connection.ts connection pool). Not a bug.
  2. packages/api/src/events/replicache-events.ts:62 — `.catch(() => {})` on unsubscribe during `closeReplicachePokeBridge`. Acceptable during teardown — at this point we're shutting down and any subscriber error is moot. Matches the pattern used in `closeRedis()` at queue/connection.ts:54.
  3. packages/api/src/modules/replicache/push.ts:218-220 — `for (const assetId of outcome.affectedAssetIds) { await pokeMembers(assetId); }` serializes the fan-out. Each `pokeMembers` does one SELECT + one publish loop; for a multi-asset push this adds latency to the HTTP response. Could be `Promise.all([...].map(pokeMembers))` to pipeline. Not a correctness issue; affectedAssetIds is typically size 1 in practice (a client pushes mutations for the asset it's currently viewing). Micro-optimization, not a Task 10 concern.
  4. packages/api/src/events/replicache-events.ts:75 — Local `replicacheEvents.emit('poke', event)` is synchronous. The SSE listener in modules/replicache/index.ts:81-84 writes into the stream controller synchronously. No race between publish and subscribe on single-replica deploys.
  5. packages/api/src/events/replicache-events.ts:39-46 — `subscriber.on('message', ...)` JSON.parse catch silently drops malformed messages. Acceptable — a malformed Redis message is either (a) another service publishing to our CHANNEL accidentally (misconfiguration, should be loud but not in this scope) or (b) a corrupted wire payload (recovery: next real poke). Could add a `console.warn` with truncated payload for debugging; low-signal, not worth adding now.
  6. packages/api/src/modules/asset-members/service.ts:82 — `pokeMembers` only fails if `listMemberUserIds` throws; the catch block warns and swallows (appropriate for a fire-and-forget notifier). The emitted pokes themselves don't throw (EventEmitter.emit and ioredis.publish are both fire-and-forget). Safe.
  7. push.ts user-scoped poke already runs inside a try/catch (push.ts:225-232). This is defensive over-engineering — `emitReplicachePokes` cannot realistically throw (it just pushes into an EventEmitter / Redis publisher). But the catch is cheap and future-proofs the call against publisher refactors. Kept as-is.
  8. The queue/connection.ts `connections` array captures both publisher and subscriber when `initReplicachePokeBridge()` runs. At shutdown `closeRedis()` calls `.quit()` on all of them. Lifecycle is correct. Verified.

Library improvements made:
  - None applicable. EventEmitter + ioredis pub/sub is the library primitive; no hand-rolled wrapper exists to collapse. The `publish() → emit()` fallback pattern is idiomatic ioredis usage for optional-Redis deploys (matches the existing pattern in events/asset-events.ts). The per-mutation savepoint in push.ts already uses Drizzle's nested `tx.transaction(...)` primitive (verified in Task 7) — there's no library that provides a built-in "poke on tx commit" hook; the manual post-commit fan-out IS the right library-shaped pattern.
  - Verified Drizzle has no `tx.onCommit()` hook via `rg -n 'onCommit|afterCommit' node_modules/.pnpm/drizzle-orm@*/node_modules/drizzle-orm/pg-core` — zero hits. Confirmed the "return summary from tx, fan out after await" pattern in push.ts is the canonical Drizzle-shaped approach. (Postgres has `LISTEN/NOTIFY` which emits on commit via `pg_notify`, but wiring that end-to-end is a larger architectural change and outside Task 10 scope; the current Redis pub/sub architecture is deliberate.)

Checklist verification (per review-prompt.md Security + Resilience + Code Quality + Common Patterns):
  - Every poke runs after `await tx.commit()` or outside the transaction block — verified by enumeration above. Zero in-tx poke emissions across the entire diff.
  - Server mutators never emit pokes from inside the savepoint — verified via grep; fan-out is centralized in push.ts post-commit.
  - Poke contract is documented at the bus entry point — new module-level docblock on replicache-events.ts names every emitter, the rule, the failure mode, and the canonical pattern location. Enhanced `emitReplicachePokes` JSDoc restates it at the call-site.
  - No sensitive data in logs — existing catch blocks use `err instanceof Error ? err.message : err` / `String(err)`. No new logging added that would leak.
  - No silent error swallowing unexplained — added "Redis unavailable" comment to the publish catch fallback so future readers see the intent. Subscriber-message catch kept silent (documented in issues-noted #5).
  - Proper error handling — every emitter call site has a try/catch or a fire-and-forget `.catch()`; push.ts:225-232 wraps even the user-scoped poke.
  - Idempotency — pokes are idempotent by nature (a poke says "pull"; pulling twice is cheap). Retry-safe.
  - Transactions for atomic operations — the poke IS the post-commit signal that the atomic unit succeeded. Emitting before commit would break atomicity from the client's perspective.
  - Reuse existing utilities — all poke fan-out goes through `emitReplicachePokes` or `AssetMemberService.pokeMembers` or `NotificationService.poke`. No ad-hoc `replicacheEvents.emit` call outside the bus module itself (bridge) and the SSE stream (subscriber).
  - pnpm check-types: clean (8/8 tasks green). @milkpod/api and downstream re-ran due to the replicache-events.ts doc edit (comment-only change; no type surface affected); @milkpod/{env,sync,ai,auth} cache-hit.

---

## Task 11: Scope realtime channels per user / per asset

Approach: traced the current pub/sub topology end-to-end (publisher → Redis channel → subscriber → local EventEmitter → SSE listener → HTTP response). Compared against the global-channel pattern in `events/asset-events.ts`. Verified ioredis@5.10.1's `SUBSCRIBE`/`UNSUBSCRIBE` semantics (built/redis/RedisOptions.js:45 confirms `autoResubscribe: true` — reconnect auto-re-subscribes, so our refcount state is the single source of truth and ioredis replays it after a transient drop). Chose per-user Redis channels (`replicache-pokes:u:<userId>`) over per-asset: the SSE connection is already one-per-user, and per-asset would force a subscription manifest that changes on every membership change — brittle. Per-user is the right grain for the way the SSE stream is authored, and the "client subscribes only to channels for assets it can see" invariant is enforced by the emission site (`emitReplicachePokes(userIds, assetId)` where `userIds` is already the membership-filtered recipient list — a user never receives a poke for an asset they can't see).

Prior state: one global Redis channel (`'replicache-pokes'`) carried every poke to every replica; the local EventEmitter re-emitted everything on a single `'poke'` event name; the SSE handler subscribed to ALL pokes and filtered by `event.userId !== userId` at the application layer. That matched review-prompt.md Common Patterns for "global broadcast channel" — every replica fanned in every user's pokes, then discarded ~N-1/N of them. Per the task's explicit "no global broadcast channel" rule, this needed a refactor, not a comment.

Issues found and fixed:
  1. packages/api/src/events/replicache-events.ts:46-71 (old) — Single global Redis channel `'replicache-pokes'` with server-side filtering at the SSE layer. Every replica received every poke and discarded the vast majority. Replaced with per-user channels `replicache-pokes:u:<userId>` and a dynamic subscribe/unsubscribe lifecycle driven by active SSE listeners. A replica only subscribes to channels for users it is currently serving, so cross-replica fan-in drops from O(total pokes) to O(pokes for my users). Reduces both bandwidth and the attack-surface for cross-user leakage (a replica that never serves user U never sees U's pokes in memory).
  2. packages/api/src/events/replicache-events.ts (old lines 81-86) — SSE handler performed the userId filter at the application layer: `if (event.userId !== userId) return;`. That filter was load-bearing for privacy on the single-global-channel architecture. Eliminated it by making the channel itself user-scoped and emitting on a per-user local event name (`poke:<userId>`). The SSE listener now only receives events addressed to its user — no application-level filter, no possibility of forgetting the check on future listener additions. Defense-in-depth: the subscriber callback also re-validates `event.userId === userId` before forwarding, guarding against a misaddressed publish.
  3. packages/api/src/modules/replicache/index.ts:81-96 — SSE handler's subscription was open-ended against a global event name, making per-user listener leaks invisible. Replaced with `subscribeUserPokes(userId, listener) → () => void`, which refcounts concurrent listeners for the same user and drives the Redis subscribe/unsubscribe pair automatically. Multiple tabs from the same user share ONE Redis subscription on this replica (the refcount guarantees idempotent subscribe/unsubscribe against ioredis).
  4. packages/api/src/events/replicache-events.ts:65 (old) — `replicacheEvents.setMaxListeners(200)` was a global cap on the bus regardless of how many connected users existed. Fine at 200 concurrent users; fragile as the app scales. Set to `0` (unlimited) since event names are now user-scoped — each user's `poke:<userId>` event has at most O(tabs-per-user) listeners, which is the natural bound.
  5. packages/api/src/events/replicache-events.ts (added `closeReplicachePokeBridge`) — Previously unsubscribed from ONE global channel at teardown. Now iterates the refcount map and unsubscribes from every active per-user channel, preventing stray Redis subscriptions from outliving the process's SSE listeners.

Issues noted (no fix needed):
  1. `packages/api/src/events/asset-events.ts` — uses the same global-channel pattern (`CHANNEL = 'asset-status'`) that Task 11 just fixed for replicache-events. Out of scope (Task 11 is scoped to replicache realtime channels), but the pattern is identical and a follow-up task could apply the same per-user scoping. Flagged here so it's discoverable. The asset-status stream is lower-volume than sync pokes (fires on transcription status changes, ~1-10 per asset per lifetime) so the scalability concern is smaller, but the privacy argument is equivalent.
  2. packages/api/src/events/replicache-events.ts subscribe failure path (line 146-153) — If the per-user `subscriber.subscribe(...)` rejects for any reason (transient Redis error, bad connection state), we log and swallow. The local `emitter.on(...)` listener is still installed, so same-replica pokes still reach the SSE stream via the `publish()` fallback path on this replica. Cross-replica pokes will be missed until the next resubscribe or reconnect (ioredis's `autoResubscribe: true` does not help here because WE own the SUBSCRIBE-per-user call, not an initial subscribe list). A future-hardening option: retry with exponential backoff, or promote to a user-facing error to trigger client-side fallback to polling. Low priority — the SSE connection will be torn down and re-established if the stream itself errors, which re-runs the subscribe.
  3. `subscriber.on('message', ...)` → `JSON.parse` catch silently drops malformed payloads. Acceptable per Task 10's audit (issues-noted #5); a malformed Redis message is a misconfiguration or corruption, not a normal runtime condition.
  4. Per-user channel names embed the userId verbatim. UserIds in this project are `nanoid()` strings (alphanumeric), so they are Redis-channel-name-safe (no wildcards, no control bytes, no whitespace). Verified by reading `packages/auth/src/schema.ts` — the `user.id` column uses `createId()` from nanoid. If the userId format ever changes to include `*` or `?` (Redis pattern glob chars), `subscribe()` still treats it as a literal (only `psubscribe()` treats as glob), so our usage is safe in perpetuity.
  5. Client-side SSE connection is still one-per-user (not one-per-asset). Multiple open asset views from the same user share one SSE stream and the client pulls on any poke for any of its assets. This matches how Replicache's pull model works (full-CVR diff; pulling for asset B is cheap if B is already up-to-date). Per-asset client channels would require separate EventSource connections per asset, which is wasteful. Per-asset filtering on the receiving client is possible via the `assetId` field on the poke payload but not currently needed — the client's `rep.pull()` is idempotent and the CVR protocol skips no-op patches.

Library improvements made:
  - `ioredis@5.10.1` exposes variadic `subscribe(...channels)` / `unsubscribe(...channels)` and maintains its own subscription set (`built/SubscriptionSet.d.ts` in the package). Leveraged this to batch unsubscribe at teardown (`subscriber.unsubscribe(...channels)`) rather than looping — one RESP round-trip instead of N.
  - Used ioredis's `autoResubscribe: true` default (verified in `built/redis/RedisOptions.js:45`) — on transient connection drops, ioredis replays all active subscriptions. This means our refcount map IS the canonical view of "what this replica is subscribed to", and we don't need a manual resubscribe loop after reconnect. Documented this assumption in the issues-noted #2.
  - No hand-rolled wrapper emerged to collapse. The `publish() → emitter.emit()` fallback for the no-Redis case is idiomatic ioredis usage (same shape as events/asset-events.ts); there is no library primitive that spans "pub/sub with optional single-replica mode" — you either have Redis or you don't.
  - Considered but rejected: Redis `PSUBSCRIBE` with pattern `replicache-pokes:u:*`. It would centralize one pattern subscription per replica, simpler bookkeeping, but re-introduces the "every replica sees every poke" fan-in property we just eliminated. Rejected on scalability grounds.

Checklist verification (per review-prompt.md Security + Resilience + Code Quality + Common Patterns + Library Leverage):
  - Channel names include the userID — every publish goes to `replicache-pokes:u:<userId>` (packages/api/src/events/replicache-events.ts:52); no global channel exists anywhere in the module.
  - Client subscribes only to channels for assets it can see — the SSE connection is user-scoped (`/api/replicache/events` requires auth, extracts `user.id`, subscribes ONLY to `poke:<userId>`). The emission side (`emitReplicachePokes(userIds, assetId)`) is called with membership-filtered `userIds` by every caller (traced in Task 10's audit), so the user only receives pokes for assets they have access to. Defense-in-depth: the Redis subscriber callback re-validates `event.userId === userId` against the channel name before forwarding.
  - No sensitive data in logs — new `console.warn` on subscribe failure uses `err instanceof Error ? err.message : err` (packages/api/src/events/replicache-events.ts:149-152). No poke payloads, user emails, or connection info logged.
  - No silent error swallowing unexplained — `unsubscribe().catch(() => {})` at teardown is documented as best-effort cleanup (matches the pattern audited in Task 10 issues-noted #2). The new subscribe-failure catch produces a specific `console.warn`; not silent.
  - No `any` — `PokeListener = (payload: ReplicachePoke) => void` is fully typed; `subscribeUserPokes` returns `() => void`; `userIdFromChannel` returns `string | null`.
  - No unjustified `!` — zero in the diff.
  - Proper HTTP status / error handling — unchanged at the HTTP layer; the SSE stream's error path is preserved by Elysia.
  - Idempotency — `subscribeUserPokes` is refcounted; two tabs for the same user ⇒ one Redis subscribe, one unsubscribe when both close. `emitReplicachePokes` remains idempotent (a duplicate poke just triggers a no-op pull).
  - Reuse existing utilities — `subscribeUserPokes` is now THE helper for SSE listeners; the SSE handler consumes it directly (no ad-hoc `emitter.on('poke', ...)` anywhere). Also removes the need for the old exported `replicacheEvents` singleton from the module's public surface (only internally held as `emitter`).
  - Library leverage — leveraged ioredis variadic unsubscribe + autoResubscribe (documented above). Used Node's built-in `EventEmitter` per-event-name routing so per-user listeners don't share a listener array, eliminating the need for an application-layer filter.
  - Transactions for atomic operations — unchanged; Task 10 already verified all poke emitters fire post-commit.
  - Common Patterns #5 silent error swallowing — every catch in the module either logs a specific warning or is documented inline as best-effort cleanup.
  - pnpm check-types: clean (8/8 tasks green). @milkpod/api and @milkpod/server/web re-ran (cache miss from signature changes on replicache-events.ts: new `subscribeUserPokes` export, removed `replicacheEvents` from public exports). @milkpod/{db,sync,env,ai,auth} cache-hit.

---

## Task 12: Add Elysia auth guard and input validation to Replicache routes

Approach: verified Elysia 1.4.22's `.guard()` + macro interaction by reading `node_modules/.pnpm/elysia@1.4.22/.../dist/index.d.ts:1319-1392` (the two `guard` overloads) and confirming via mcp__grep__searchGitHub against `elysiajs/elysia` that `.guard({ auth: true }, (app) => ...)` with a named macro propagates the resolved `{ user }` context into child handlers (test/types/lifecycle/soundness.ts:488, test/types/macro.ts:9, elysiajs/elysia-lucia example/index.ts:47). The two-arg form (`guard(hook, run)`) is the canonical pattern for scoping auth to a subset of routes on the same instance — matches this exact case (pull/push/events all want auth; nothing else on this Elysia instance needs a different policy). Also verified `maxItems` is a first-class TypeBox ArrayOption in `@sinclair/typebox@0.34.41/build/esm/type/array/array.d.mts:9`, and that Elysia's default `ValidationError.status = 422` (dist/error.js:128) — so a TypeBox cap yields 422 by default, not 413. Task explicitly asks for 413 on oversized push, so the logical cap is enforced by an explicit handler check with a higher TypeBox `maxItems` as defense-in-depth.

Prior state:
  - Three routes (`POST /pull`, `POST /push`, `GET /events`) each carried their own `{ auth: true }` option. Duplication matches the codebase-wide convention (packages/api/src/modules/*/index.ts all use per-route auth), but this task is scoped to applying the `.guard()` refactor specifically to replicache routes per review-prompt.md library-leverage checklist.
  - Body validators existed inline in index.ts: `clientGroupID` was `t.String({ minLength: 1, maxLength: 200 })`, `cookie` was `t.Nullable(t.Number())`, mutations array had no `maxItems`, and sub-fields (`id`, `timestamp`, `clientID`, `name`) had zero constraints.
  - `PullRequestBody` / `PushRequestBody` / `PushMutationV1` were hand-written interfaces in pull.ts / push.ts that duplicated the TypeBox schema structure — two sources of truth with no mechanism to catch drift. Handlers accepted `body as PullRequestBody` / `body as PushRequestBody` casts because Elysia's inferred body type (from the inline schema) was not the same nominal type as the hand-written interface.
  - No oversized-push guard; a pathological client could POST a 100k-mutation batch and the handler would try to process each one.

Issues found and fixed:
  1. packages/api/src/modules/replicache/index.ts (entire file) — Three per-route `{ auth: true }` options replaced with a single `.guard({ auth: true }, (app) => app.post(...).post(...).get(...))` block. Fewer places to forget auth, and the guard is the canonical Elysia primitive for scoping lifecycle hooks to a subroute group (verified via docs/elysia.md + GitHub examples). The `authMacro`'s `resolve` still runs per-request, so auth behavior is unchanged; only the declaration site is deduplicated.
  2. packages/api/src/modules/replicache/index.ts (push route) — Added `if (body.mutations.length > ReplicacheModel.MAX_MUTATIONS) return status(413, { message: ... })` before calling `handlePush`. Semantically correct HTTP code for "logical limit exceeded" (RFC 9110 §15.5.14 — Payload Too Large). Previously no cap existed; a client could push any array length up to Elysia's default body-size limit.
  3. packages/api/src/modules/replicache/model.ts (new file, 52 lines) — Created following the existing `AssetMemberModel` namespace pattern (packages/api/src/modules/asset-members/model.ts). Extracts body schemas into `ReplicacheModel.pull`, `ReplicacheModel.push`, `ReplicacheModel.pushMutation`; uses `typeof X.static` to derive the TS types (`Pull`, `Push`, `PushMutation`). Declared the two constants `MAX_MUTATIONS = 100` (logical cap, handler-enforced, 413) and `HARD_MUTATION_LIMIT = 1000` (memory safety cap, TypeBox-enforced via `maxItems`, 422). Defense in depth: even if the handler forgets to check, the TypeBox layer rejects anything >1000.
  4. packages/api/src/modules/replicache/pull.ts:27 and push.ts:20-21 — Replaced hand-written `PullRequestBody` / `PushRequestBody` / `PushMutationV1` interfaces with `type PullRequestBody = ReplicacheModel.Pull` (and siblings) so the runtime validator and the handler signature share ONE source of truth. Adding a field to the TypeBox schema now automatically updates the handler's body type; forgetting to update the validator produces a TS error at the handler. Previously drift was silent.
  5. packages/api/src/modules/replicache/index.ts (pull + push handlers) — Removed `body as PullRequestBody` / `body as PushRequestBody` casts. Unnecessary after step 4: Elysia's inferred body type from the schema IS `ReplicacheModel.Pull` (same nominal type as the handler parameter). Cleanest: no `as` cast anywhere in the router. Verified via `pnpm check-types` — no type errors.
  6. packages/api/src/modules/replicache/model.ts — Tightened sub-field validators beyond the prior skeleton: `cookie` went from `t.Nullable(t.Number())` to `t.Nullable(t.Integer({ minimum: 0 }))` (server writes cookies as integer `cvrVersion` counters, never floats or negatives); `profileID` went from bare `t.Optional(t.String())` to `maxLength: 200`; `schemaVersion` got `maxLength: 50`; each mutation's `id` and `timestamp` became `t.Integer({ minimum: 0 })`; `clientID` got `minLength: 1, maxLength: 200`; `name` got `minLength: 1, maxLength: 100`. Rejects malformed or hostile inputs at the validator, well before they touch the transaction or the mutator dispatch.

Issues noted (no fix needed):
  1. Other module routers (`asset-members/index.ts`, `moments/index.ts`, `comments/index.ts`, etc.) still use per-route `{ auth: true }` rather than `.guard()`. Consistent with the codebase-wide convention; Task 12 is scoped to replicache routes only. A sweep-style task could migrate all modules to `.guard()` but that's out of scope here and not a correctness concern (per-route `{ auth: true }` is functionally equivalent — just less concise).
  2. `mutation.args: t.Unknown()` at the Elysia validator layer. Intentional. The push handler dispatches `parsed = mutatorArgsSchemas[name].safeParse(args)` with the per-mutator zod schema from `@milkpod/sync` BEFORE running the mutator (verified in push.ts:140-149). Elysia-layer validation of `args` would require duplicating the sync package's zod schemas in TypeBox, which introduces the same drift risk Task 12 step 4 just eliminated. Zod-at-dispatch is the right layer for args validation.
  3. `pushVersion: t.Literal(1)` / `pullVersion: t.Literal(1)` — Hard-coded. Replicache's protocol is currently at pull v1 / push v1; if Replicache bumps to v2, we'll need a migration plan anyway (the entire CVR schema may change). Accepting a literal is the right constraint for today.
  4. The 413 check is in the handler rather than as a standalone `beforeHandle` hook. Could be extracted if the cap ever needs reuse, but a one-line inline check is clearer at the call site.

Library improvements made:
  - Replaced inline per-route `{ auth: true, body: ... }` with `.guard({ auth: true }, (app) => app.post(...).post(...).get(...))` — canonical Elysia primitive for subgroup lifecycle. Reference: docs/elysia.md + elysiajs/elysia test/types/lifecycle/soundness.ts:488.
  - Used TypeBox's native `ArrayOptions.maxItems` (`@sinclair/typebox@0.34.41/.../array.d.mts:9`) for the HARD_MUTATION_LIMIT — Elysia wires it into the pre-handler validator automatically; no hand-rolled length check needed at the TypeBox layer. Saves one line vs. a `beforeHandle` alternative.
  - Used TypeBox's native `IntegerOptions.minimum` and `StringOptions.minLength/maxLength` for sub-field constraints. No hand-rolled validation in the handler (would duplicate what TypeBox already does).
  - Derived `PullRequestBody = typeof pull.static` (and siblings) via Elysia's built-in `.static` helper on TypeBox schemas — no hand-maintained TS interfaces. Reference: the existing `AssetMemberModel.Invite = typeof invite.static` pattern in packages/api/src/modules/asset-members/model.ts:8.
  - Considered but rejected: putting the 413 check in TypeBox via `maxItems: 100`. Elysia's `ValidationError.status = 422` (verified in `dist/error.js:128`) — doesn't give the semantically-correct 413 that the task prescribes, and a custom `.onError()` mapping `ValidationError` to 413 would only fire for mutation-count failures (messy). Handler-level check with a higher TypeBox cap is the right split.

Checklist verification (per review-prompt.md Security + Resilience + Code Quality + Library Leverage):
  - Input validation on every route — pull and push both have TypeBox schemas covering every field (type, constraints). `/events` is GET with no body.
  - Auth + authorization checks on every endpoint — all three routes sit inside `.guard({ auth: true }, ...)`; the `authMacro.resolve` returns 401 if session missing. Per-asset authz is downstream (pull.ts's `getAccessibleAssetIds` + push.ts's mutator ACLs, audited in Tasks 6 + 8).
  - Proper HTTP status codes — 401 (auth macro), 403 (forbidden clientGroup binding), 413 (oversized push), 422 (TypeBox validation failures). No 500 for client mistakes.
  - No magic numbers — `MAX_MUTATIONS = 100` and `HARD_MUTATION_LIMIT = 1000` extracted to `ReplicacheModel` constants with comments explaining their respective roles. Referenced by name in the handler and in the schema.
  - Reuse existing utilities — followed the `AssetMemberModel` namespace convention for schema/type colocation; reused the `authMacro` via guard. No new auth or validation helper introduced.
  - Proper error handling with meaningful messages — 413 response includes the cap in the message for client debuggability (`Push exceeds 100 mutations`). No sensitive information (no SQL, no stack trace, no connection state).
  - No `any` — zero `any` anywhere in the diff. Even the `body` parameter is inferred from the TypeBox schema.
  - No unjustified non-null assertions — zero `!` in the diff.
  - Discriminated unions + exhaustive switches — `handlePull` / `handlePush` return `{ forbidden: true }` as a discriminant checked via `'forbidden' in result`. Handler's two branches cover the union exhaustively (result is either forbidden or the success response).
  - Library leverage — `.guard()` + TypeBox native options (documented above). No hand-rolled wrapper.
  - pnpm check-types: clean (8/8 tasks green). @milkpod/api re-ran (cache miss from the new model.ts and router structural changes); @milkpod/server re-ran downstream; @milkpod/web cache-miss too. @milkpod/{env,sync,db,auth,ai} cache-hit — schema/model changes were module-local.

---

## Task 13: Register Replicache routes in the rate limiter

Approach: read `packages/api/src/middleware/rate-limit.ts` top-to-bottom to understand the token-bucket + `categorize()` + identity-keying flow, confirmed `packages/api/src/index.ts:131` mounts `rateLimiter` before `.use(replicache)` at line 180 (so the plugin's `onBeforeHandle` does fire for `/api/replicache/*`). Reviewed the `session-cache` layer (`getSessionCached`) to confirm the user-keyed path: `category !== 'auth'` → look up session → key as `user:<id>:<category>`. Replicache routes are behind `authMacro` + `.guard({ auth: true })` (Task 12), so the session will always exist when the limiter runs — the `ip:` fallback only fires if Better Auth is transiently broken. Sampled the existing bucket sizes (`LIMITS`) to pick a "generous" number: crud is 100/min burst + 100/min refill. Replicache is a hot sync path that fires pull on connect, on every Redis poke, and on every push ack — multi-user collab can easily hit 5–10 pull/s per user. Sized at 600 burst + 10/s sustain (600/min) — ~6x crud — so normal collab traffic never trips and sustained >10 req/s per user (the anomaly we actually want to limit) does.

Prior state: `/api/replicache/pull`, `/api/replicache/push`, and `/api/replicache/events` did NOT appear in `categorize()` → fell through to `return null` → limiter returned early → **no rate limiting at all** on the sync hot path. A hostile or buggy client could spam pulls/pushes as fast as the DB would service them. Matches review-prompt.md Common Patterns #3 ("New endpoints not covered by rate limiter").

Issues found and fixed:
  1. packages/api/src/middleware/rate-limit.ts:66-74 — `RateCategory` union had no `'replicache'` member → no dedicated bucket existed for sync traffic. Extended to `'ingest' | 'chat' | 'crud' | 'auth' | 'billing' | 'replicache'`. The `satisfies Record<RateCategory, BucketConfig>` on `LIMITS` now forces a compile error if a new category is added to the union without sizing — type-driven completeness (review-prompt.md TypeScript patterns: "satisfies for config objects").
  2. packages/api/src/middleware/rate-limit.ts:80-83 (new) — Added a dedicated `replicache: { capacity: 600, refillRate: 600 / 60 }` bucket. Separate from chat/ingest/crud per the task spec. Inline comment documents the sizing rationale ("Sync hot path: pull/push fire on every user action and every realtime poke") so future reviewers understand why it's 6x larger than crud. Generous but bounded — a sustained ≥10 req/s per user is anomalous and the cap kicks in.
  3. packages/api/src/middleware/rate-limit.ts:91 — Added `if (path.startsWith('/api/replicache')) return 'replicache';` to `categorize()`. Single prefix covers all three sub-routes (`/pull`, `/push`, `/events`) — including the SSE connection-establish on `/events`, which also deserves a bucket so a reconnect storm can't DOS the limiter.
  4. User-keyed (not IP) — verified by tracing the existing limiter flow: line 137 starts with `identity = ip:<ip>`, then line 138 `if (category !== 'auth')` triggers a `getSessionCached()` lookup and overwrites to `user:<id>` when available. Since replicache is NOT `auth` and is always authenticated (Task 12's `.guard({ auth: true })` rejects unauth requests before the route runs), the limiter will always key on `user:<id>:replicache` for legitimate traffic. No code change needed on this axis — the existing control flow already does the right thing for the new category.

Issues noted (no fix needed):
  1. Position of `/api/replicache` in `categorize()` — placed AFTER `/api/auth/` and `/api/billing` (both literal prefixes that cannot collide with `/api/replicache`), and BEFORE the generic crud fall-through block. No ordering concern: `/api/replicache/*` shares no prefix with any other category. The `/api/replicache` prefix has no trailing slash so it matches `/api/replicache`, `/api/replicache/pull`, `/api/replicache/push`, and `/api/replicache/events`; we never mount anything outside the `/api/replicache/*` tree, so a bare `/api/replicache` GET is a 404 from Elysia regardless.
  2. `/events` SSE is a long-lived GET — it consumes ONE token at connect and holds the connection for hours. The limiter's `onBeforeHandle` does NOT fire for each SSE message (messages go over the same HTTP connection, not new requests), so the bucket only gates connect/reconnect, which is the right behavior. A reconnect storm still hits the 600-burst ceiling, preventing amplification; normal steady-state is free. No separate `replicache-sse` bucket needed.
  3. Bucket TTL (line 22: 10 minutes idle) — same as other categories. A user who stops using the app for 10 minutes resets to full capacity on next pull. Correct behavior — the limiter's purpose is anti-abuse, not resource accounting.
  4. Per-category `consume()` is called serially in a single request (one category per path). Replicache pulls never share a bucket with chat/crud/ingest — the `:replicache` suffix on the bucket key isolates the hot path. A user hammering pulls cannot starve their chat or crud quota, which is exactly why the task spec required "separate from chat/ingest/crud".
  5. `getSessionCached()` is called for each replicache request. It already has a two-layer cache (per-Request WeakMap + 10-s token cache), and since the authMacro also calls `getSessionCached()` downstream, the limiter's call is free for any request that has already been authed in the same per-request path. The WeakMap dedupes; only the first lookup hits the DB. Zero extra latency on the sync hot path from rate-limiting.

Library improvements made:
  - Used `satisfies Record<RateCategory, BucketConfig>` (already in place, preserved) — extending `RateCategory` AND forgetting to size `LIMITS.replicache` now produces a TS error at the `satisfies` call site. Type-driven completeness instead of a runtime lookup miss. No hand-rolled union-key check needed.
  - No new library primitive in play — Elysia's `onBeforeHandle` + Node's `setInterval` for the bucket sweep is already the canonical pattern for token-bucket rate limiting, and the codebase's existing token bucket is correctly written (capacity-clamped refill, monotonic `lastRefill`, purge-on-idle). Nothing to replace. Reviewed whether a library like `elysia-rate-limit` would help — it does `ip`-only keying by default with an opt-in `generator()` for custom keys (verified by reading `node_modules/.pnpm/elysia-rate-limit@*/...` is NOT present; searched github.com/rayriffy/elysia-rate-limit README). The existing hand-rolled limiter is ~130 lines and already handles user-vs-IP keying, per-category configs, and in-process state without an extra dep — replacing it would regress expressiveness (no per-category + per-identity compound keying off-the-shelf). Deferred.
  - Considered but rejected: moving the per-route rate-limit config onto the replicache module (e.g. via a `.beforeHandle()` on the router). Rejected because a single global `categorize()` function is the existing convention and is easier to audit ("all rate-limited paths live here"). Per-route decoration would scatter the policy across 20+ files and make it easy to forget a new route.

Checklist verification (per review-prompt.md Security + Resilience + Code Quality + Common Patterns + Library Leverage):
  - Rate limiting on sensitive endpoints — `/api/replicache/*` now covered. Verified `categorize()` returns `'replicache'` for all three sub-routes via prefix match. Sizing (capacity: 600, refillRate: 10/s) is generous but bounded.
  - New endpoints covered by the rate limiter's `categorize` function — yes, per Common Patterns #3's explicit language ("Check that the path prefix appears in the categorize function"). `/api/replicache` is now one of the if-else branches.
  - User-keyed (not IP) — verified via the existing control flow: non-auth categories attempt a session lookup and overwrite `identity` from `ip:<ip>` to `user:<id>` when a session is available. Replicache is always authenticated (guard Task 12), so keys are always `user:<id>:replicache`.
  - Dedicated bucket separate from chat/ingest/crud — `replicache` is a new enum member in `RateCategory`; its `LIMITS.replicache` config is distinct; the bucket key suffix isolates the hot path from all other categories.
  - No magic numbers — `capacity: 600` and `refillRate: 600 / 60` are written with an inline comment explaining the rationale (~6x crud; 10 req/s sustain is the anomaly threshold). The other LIMITS entries use the same numeric literal style, so this matches convention.
  - No sensitive data in logs — unchanged; the limiter logs nothing. 429 response message is a constant ("Too many requests. Please try again later."), no user/session/path data.
  - Proper HTTP status codes — 429 with `Retry-After` header preserved. Existing pattern.
  - Proper error handling — `getSessionCached()` failures fall through to IP keying (line 144 catch) — unchanged and correct for replicache (a missing session triggers authMacro's 401 downstream, which sits behind the limiter). The IP fallback just means an unauth client is rate-limited by IP on the replicache bucket, which is strictly more lenient than blocking on the session-lookup error — safe default.
  - No `any` — zero `any` in the diff. `RateCategory` is a string-literal union; `LIMITS` is typed via `satisfies`.
  - No unjustified non-null assertions — zero `!` in the diff.
  - Exhaustive typing — `LIMITS` uses `satisfies Record<RateCategory, BucketConfig>`; adding a category without sizing is a TS error. `categorize()` returns `RateCategory | null`, and callers check for `null` before indexing `LIMITS`.
  - Library leverage — already-idiomatic token-bucket + Elysia hook; no wrapping or reinvention. Considered and rejected `elysia-rate-limit` (less expressive for our compound keying).
  - Common Patterns #3 ("New endpoints not covered by rate limiter") — resolved for replicache. Note: other recently-added modules (`/api/notifications`, `/api/asset-members`) are NOT in the current `categorize()` either, but those are outside this task's scope (review-plan.md Task 13 is explicitly scoped to replicache routes). Flagging here for any future sweep task.
  - pnpm check-types: clean (8/8 tasks green). @milkpod/api re-ran (cache miss from the `RateCategory` union extension and `LIMITS` literal change); @milkpod/server + @milkpod/web re-ran downstream. @milkpod/{env,db,sync,ai,auth} cache-hit — rate-limit.ts is internal to packages/api and doesn't cross a package boundary.

---

## Task 14: Prepare the hot-path pull query

Approach: before touching code, verified Drizzle 0.44.7's `.prepare(name)` semantics by reading `node_modules/.pnpm/drizzle-orm@0.44.7_*/node_modules/drizzle-orm/pg-core/query-builders/select.js:760-788` (the `_prepare(name)` method binds to the session captured on the query builder), `pg-core/session.d.ts:16-43` (PgPreparedQuery wraps query+params+cache; execute(placeholderValues) calls `fillPlaceholders(params, values)` then the underlying client), and `node-postgres/session.js:165-178` + `:180-196` (transaction creates a new NodePgSession bound to a dedicated PoolClient; prepareQuery stores `this.client` on the NodePgPreparedQuery). Cross-checked via mcp__grep__searchGitHub on drizzle-team/drizzle-orm — every official prepared-statement example builds at module scope on the pool-level db; there are ZERO examples of prepared statements inside `.transaction()` callbacks. Confirmed the task's literal prescription ("module-scope, not recreated per call") is in direct tension with running the query inside the pull's tx: module-scope prepared statements bind to the pool and check out their own connection, bypassing any surrounding tx on the caller side. Resolved by picking the one query that legitimately doesn't need tx semantics — the notifications row-version lookup (pure-read, user-scoped, no dependency on earlier tx state, self-healing on the next pull if a race produces a slightly-stale snapshot).

Prior state: `packages/api/src/modules/replicache/pull.ts:197-218` ran an inline `tx.select(...).leftJoin(...).where(eq(notifications.recipientId, userId)).orderBy(asc(notifications.id))` on every pull. No name, no placeholder — node-postgres sent it as an unnamed extended-protocol query, which forces PostgreSQL to parse + plan it per execution (the server-side plan cache is keyed on statement NAME, never on unnamed statements). Additionally, Drizzle regenerated the SQL string from the query-builder IR on every pull (a JS-side cost), even though the shape never changed. Hot-path: pulls fire on every user action + every realtime poke, so this query ran many times per minute per active user.

Issues found and fixed:
  1. packages/api/src/modules/replicache/pull.ts:197-218 (old) — Inline tx-bound `.select().from(notifications).leftJoin(user).where(eq(recipientId, userId)).orderBy(asc(id))` with no prepared-statement name. Every pull paid: (a) Drizzle query-builder SQL generation, (b) node-postgres unnamed-statement parse + plan on the backend. Extracted to a module-scope prepared statement `replicache_pull_notifications` with `sql.placeholder('userId')` for the recipient filter. Now: SQL generated ONCE at first call for the process lifetime; PostgreSQL prepares the statement once per PoolClient (first use on that connection) and reuses the cached plan on every subsequent pull from that client. Hot-path amortizes to pure `EXECUTE <name>($1)`.
  2. packages/api/src/modules/replicache/pull.ts:22-76 (new) — Added `_notificationsPullStmt` singleton + `buildNotificationsPullStmt()` + `getNotificationsPullStmt()` lazy-init trio. Lazy init mirrors the existing `getCVRStore()` pattern in cvr.ts so construction happens on first call (after DB pool is live + DATABASE_URL is set), not at module import. Using `ReturnType<typeof buildNotificationsPullStmt>` for the singleton's type keeps the TS type tracking Drizzle's generic prepared-query type without hand-writing `PgPreparedQuery<…>`.
  3. packages/api/src/modules/replicache/pull.ts (the execute call) — Replaced the 22-line inline query with `await getNotificationsPullStmt().execute({ userId })`. The `execute()` signature requires the placeholder-keyed record, so `.execute({ userId })` is enforced at the type level — forgetting a placeholder produces a TS error.
  4. packages/api/src/modules/replicache/pull.ts (the inline comment at lines 250-258) — Documented that the notifications read runs on the pool (not the tx) and why this is safe under READ COMMITTED. Without this, a future reader could reasonably assume the read shares the tx's snapshot and be surprised.

Issues noted (no fix needed):
  1. `getAccessibleAssetIds(userId, tx)` at pull.ts:166 — Also a per-user simple lookup (single `userId` parameter, fixed shape, hot-path). A module-scope prepared statement would plausibly save per-pull cost here too. NOT done because (a) the existing inline comment at pull.ts:158-165 explicitly documents the design choice to run this query on the tx's connection alongside the entity reads for narrowing the concurrent-revocation window, and (b) the task's wording "per-user row-version lookup" specifically describes the notifications query (which returns rowVersion as the primary signal for diff detection); membership is an asset-id lookup, not a row-version lookup. Preserving the existing tx-bound design avoids a correctness regression in exchange for a marginal JS-overhead improvement. Can be revisited under a follow-up task if profiling shows it matters.
  2. `assetMoments` + `assetComments` reads at pull.ts:170-192 — Both filter by `inArray(assetId, assetIds)` where `assetIds` is a runtime array whose length varies per user. Drizzle + PG can prepare this with a single-array placeholder (verified: `inArray(col, sql.placeholder('ids'))` → backend `WHERE col IN ($1)` where $1 is the full array) but the resulting prepared plan uses PG's generic plan heuristics which are suboptimal for array-length-dependent choices (nested-loop vs hash vs index scan). Left unprepared so PG can replan per call. Also, these queries MUST run in the tx because they depend on `assetIds` computed from the tx's membership read; moving them to the pool would open a race where membership was read on tx but entities filtered on pool.
  3. `replicacheClientGroup` lookup (pull.ts:119-122) and `replicacheClient` list (pull.ts:333-340) — Both MUST stay in the tx: the clientGroup lookup is gated by the advisory lock (pull.ts:115) and the subsequent INSERT/UPDATE; the client list must read LMIDs consistent with the cvr_version bump. Preparing either would require running them on a pool connection where the advisory lock is NOT held, breaking the tx's invariants.
  4. The `user` join in the authors-metadata read (pull.ts:272-284) — Dynamic `inArray(userTable.id, authorIds)`. Same concern as #2; left unprepared.
  5. Prepared-statement name collision across deploys — node-postgres prepares statements PER CONNECTION on the PG backend. The `name` ("replicache_pull_notifications") is client-scoped (the statement is torn down when the connection closes) so rolling deploys + new pool connections on the new code will re-prepare with the same name transparently. No need for a `_v1` suffix or manual DEALLOCATE. Verified by reading node-postgres's `Connection.prototype.parse` usage.
  6. Module-scope singleton is process-local; there is no cross-process coordination. Not needed — PG's prepared-statement cache is per-connection, and each Node process has its own pool, so the singleton correctly matches the process-local invariant.

Library improvements made:
  - Used Drizzle's native `.prepare(name)` + `sql.placeholder('userId')` rather than a hand-rolled SQL template + manual parameter binding. This is the idiomatic library primitive for "cache the query plan + reuse the SQL string"; no wrapper code needed. Reference: `node_modules/.pnpm/drizzle-orm@0.44.7_*/node_modules/drizzle-orm/pg-core/query-builders/select.js:763-788` (the `_prepare` + `prepare` pair on PgSelectBase).
  - Used `ReturnType<typeof buildNotificationsPullStmt>` to derive the singleton's TS type instead of hand-writing the full `PgPreparedQuery<PreparedQueryConfig & { execute: … }>` signature. Pattern documented in `docs/typescript-patterns.md` (derived types from function signatures).
  - Lazy-init singleton via a mutable let + getter function. Matches the existing `getCVRStore()` pattern (cvr.ts:78-84) — one pattern for "process-local lazy singleton" across the replicache module, discoverable by grep. No new pattern introduced.
  - Considered but rejected: preparing on the tx handle inside the transaction callback (`tx.select(...)...prepare('name').execute(...)`). The PG backend plan cache would still work (same connection, same name = cached plan), but the Drizzle SQL-builder IR would rebuild per-call. That's the same cost we're trying to eliminate — the task text's "module-scope, not recreated per call" is unambiguous about avoiding this.
  - Considered but rejected: preparing a raw `sql<...>` template at module scope and executing with `tx.execute()`. Drizzle's `tx.execute(sql)` does not support placeholder filling (`fillPlaceholders` is only called from inside `PgPreparedQuery.execute`) — we'd have to hand-build the parameter array per call. Loses the type safety benefits of Drizzle's query builder.
  - Considered but rejected: preparing `getAccessibleAssetIds` at module scope too. Would produce a second prepared statement with the same single-`userId` shape, but conflicts with the documented design choice at pull.ts:158-165 to run it on the tx. Would require either removing that comment + design (regression) or accepting both paths (inconsistent, harder to reason about). Left as-is; documented in issues-noted #1.

Checklist verification (per review-prompt.md Library Leverage + Backend Queries & Data Access + Code Quality):
  - Library leverage (Drizzle: `.prepare('name')` with `sql.placeholder`) — exactly the library primitive the task asks for. No hand-rolled SQL string or manual parameter binding.
  - No N+1 queries — unchanged; notifications is a single joined read for all of the user's notifications, and the module-scope prepare doesn't change the query shape.
  - Appropriate database indexes — the query's `WHERE recipientId = $1 ORDER BY id` is covered by `notification_recipient_created_idx` (recipientId + createdAt) for the filter; `ORDER BY id` is ascending-on-primary-key so PG uses the primary-key index for ordering. `notification_recipient_row_version_idx` (recipientId, rowVersion) exists too and covers rowVersion-filtered reads if we ever add one. Confirmed in packages/db/src/schema/notifications.ts:67-79.
  - Pagination — notifications query is intentionally unpaginated here because the CVR diff protocol requires seeing ALL visible notifications to compute the del set. The total-per-user volume is bounded by user activity; a future task (review-plan.md Task 21) will paginate the REST `list()` endpoint for the UI, but the sync pull's unbounded scan is correct-by-design.
  - Transactions for atomic operations — unchanged. The CVR version bump + clientGroup lookup still run inside `db().transaction(...)` (pull.ts:113-368). The notifications read now runs on a pool connection, but that's pure-read and the tx doesn't need to include it for correctness (documented inline).
  - Use parameterized queries — `sql.placeholder('userId')` produces a PG-protocol parameter ($1), not a string interpolation. Zero injection surface; type-checked at execute time.
  - Appropriate data types — prepared statement inherits the column types from the Drizzle schema (notifications.rowVersion is integer; createdAt is timestamp returning Date via Drizzle's column mapper). No type changes.
  - No magic numbers or hardcoded strings — the prepared-statement name `'replicache_pull_notifications'` is a string literal, but it's appropriate as a literal: (a) it's used once, (b) PG treats it as a literal identifier not a config value, (c) there's nothing else to derive it from. Matches how every Drizzle .prepare() call in the wild writes a literal name.
  - Proper error handling — execute() propagates errors the same way the old inline query did (promise rejection → propagates out of handlePull → returned as a 500 by the Elysia handler). No new error paths introduced.
  - No `any` — zero. The prepared statement's execute return type is inferred by Drizzle as `Array<{ id: string; recipientId: string; type: NotificationType; ... actorName: string | null; actorImage: string | null }>`, same shape that `NotificationService.serialize` expects.
  - No unjustified non-null assertions — zero `!` added.
  - Exhaustive switch / discriminated unions — unchanged; `NotificationService.serialize` already has an exhaustive switch on `NotificationType` (packages/api/src/modules/notifications/service.ts:128-148).
  - SSR / browser guards — N/A (server-only module).
  - Common Patterns — no new fetch() or console.error added; no opacity-0 hover class; no silent catch; no magic number; no missed switch.
  - pnpm check-types: clean (8/8 tasks green). @milkpod/api re-ran (cache miss from pull.ts structural changes); @milkpod/server + @milkpod/web re-ran downstream (cache miss). @milkpod/{env,db,sync,ai,auth} cache-hit.

---

## Task 15: Audit asset-members/service.ts for membership-scoped queries

Approach: audited every method of AssetMemberService + its Elysia router. Primary thrust per task spec: "Role capability enforcement at the service layer: viewer attempting write → 403. Grep each mutation method for role check." Found that all three mutation methods (`invite`, `removeMember`, `revokeInvite`) were trusting the router's preflight `isOwner()` check without any service-layer enforcement — a single-level protection that would break the moment a non-router caller appeared (future sync mutator, admin CLI, migration script). Verified with `rg 'AssetMemberService\.'` that service is only called from router + moments/comments/push (pokeMembers only). Safe to tighten signatures.

Library verification before touching code:
  - Read `node_modules/.pnpm/drizzle-orm@0.44.7_*/node_modules/drizzle-orm/pg-core/query-builders/insert.d.ts:138-141` for `onConflictDoNothing({ target?, where? })` signature. Confirmed: target accepts `IndexColumn | IndexColumn[]`; omitting defaults to the table's PK. For assetMembers (PK on (assetId, userId)) no target needed; for assetInvites (unique constraint on (assetId, email)) target must be specified.
  - Checked `packages/db/src/schema/asset-members.ts:54` — `role: assetMemberRoleEnum('role').$type<'editor' | 'viewer'>().notNull()` confirms the TS type for invites.role is already narrowed, making the runtime `.filter((i): i is PendingInviteRow => ...)` in list() tautological.
  - Verified via `packages/db/src/migrations/0034_asset_invite_role_not_owner.sql` review (Task 3) that the CHECK constraint also enforces this at the DB level — so the filter is dead runtime code AND a redundant type predicate.
  - Confirmed only the replicache module currently uses `.guard({ auth: true }, app => ...)` (`rg '\.guard\('` → 1 hit). Per the library leverage checklist, repeated `{ auth: true }` per route on the 4 asset-members endpoints should be collapsed into a single guard block.
  - No Drizzle relations defined in `packages/db/src/schema/` (`rg 'relations\(' packages/db/src/schema` → 0 hits), so the relational query API (`db.query.table.findFirst({ with: {...} })`) is not available without adding a relations file. Left the manual innerJoin in list() as-is — one join, well-scoped; adding a relations schema just to eliminate 1 explicit join is a larger refactor outside Task 15's scope.

Issues found and fixed:
  1. packages/api/src/modules/asset-members/service.ts:136-228 — `invite()` took `invitedBy` and trusted the caller had already owner-gated it. No service-layer role check. Added `const callerRole = await AssetMemberService.getRole(assetId, invitedBy); if (callerRole !== 'owner') return { kind: 'forbidden' };` at the top. Extended InviteResult with `{ kind: 'forbidden' }`. Router updated to 403 on this branch (was also 403 before, but now via service contract, not a preflight check).
  2. packages/api/src/modules/asset-members/service.ts:274-319 — `removeMember()` same story. Added `callerRole !== 'owner' → { removed: false, reason: 'forbidden' }` at the top of the method, BEFORE looking up the target's role (so a non-owner can't probe membership by trial-and-error — the 403 response leaks nothing). Extended `RemoveMemberResult` type to include `'forbidden'` reason. Replaced the loose `{ removed: boolean; reason?: ... }` with a discriminated union `{ removed: true } | { removed: false; reason: ... }` so TS enforces reason presence on the failure branch.
  3. packages/api/src/modules/asset-members/service.ts:321-338 — `revokeInvite()` didn't take a caller userId at all. Added `revokedBy` parameter; added owner check; changed return type from `Promise<boolean>` to `RevokeInviteResult = { revoked: true } | { revoked: false; reason: 'not_found' | 'forbidden' }`. Router now passes `user.id` as the third argument.
  4. packages/api/src/modules/asset-members/service.ts:150-194 — `invite()` existing-user path did `SELECT role; if null INSERT` — classic check-then-insert TOCTOU. Two concurrent invites for the same email would both see null and both attempt insert; the second throws on PK violation → bubbles as a 500 instead of the correct `already_member` response. Replaced with a single `INSERT ... ON CONFLICT DO NOTHING RETURNING` inside the transaction. If `returning()` is empty, the row existed already → return `already_member`. If non-empty, we have the inserted row and can proceed with the notification record + pokes. Eliminated 1 DB roundtrip (the pre-SELECT) and closed the race atomically at the PG level.
  5. packages/api/src/modules/asset-members/service.ts:196-228 — Same check-then-insert pattern on the invite path. Replaced with `.insert(assetInvites).values({...}).onConflictDoNothing({ target: [assetInvites.assetId, assetInvites.email] }).returning({ id: assetInvites.id })`. Must specify target explicitly here — the unique is on a composite (assetId, email), not the PK. Empty returning → `already_invited`. Eliminated the pre-SELECT.
  6. packages/api/src/modules/asset-members/service.ts:95, 269, 315 — Three catch blocks used the pattern `err instanceof Error ? err.message : err`. Per Common Patterns #1 in review-prompt.md: "console.error('ctx', err) logs the FULL error object — connection strings, query state, stack traces. Always use `err instanceof Error ? err.message : String(err)`." Changed all three to `: String(err)`. Thrown values that are not Error instances can carry arbitrary structure — `String(err)` coerces to a safe scalar.
  7. packages/api/src/modules/asset-members/service.ts:121-122 — `members as MemberRow[]` cast. Removed — Drizzle's .select({...}) inference already matches MemberRow exactly. Verified column by column: `userId`, `role` (typed as `'owner' | 'editor' | 'viewer'` from the enum = `AssetMemberRole`), `invitedBy: string | null`, `createdAt: Date` (notNull from lifecycle_dates), and the joined `name: string`, `email: string`, `image: string | null`. No cast needed.
  8. packages/api/src/modules/asset-members/service.ts:123-126 — `invites.filter((i): i is PendingInviteRow => i.role === 'editor' || i.role === 'viewer')`. The schema's `$type<'editor' | 'viewer'>()` already narrows the compile-time type of `invites[n].role`; the DB-level CHECK constraint (migration 0034) guarantees the runtime values match. The filter was dead code at both levels. Removed the filter and the type predicate; renamed the local from `invites` to `pendingInvites` to match the return shape. Added an inline comment pointing at the $type + CHECK pairing so the next reader doesn't re-add the filter defensively.
  9. packages/api/src/modules/asset-members/index.ts (entire file) — Router was using per-route `{ auth: true }` on all four endpoints, plus explicit `isOwner()` preflights on POST/DELETE endpoints. Wrapped all routes in `.guard({ auth: true }, (app) => app...)` (matches the replicache module's pattern from Task 12). Dropped the per-route preflights because the service now enforces owner. Net: saves 1 DB roundtrip per POST/DELETE request (the preflight), simpler code, and role-check logic lives in exactly one place (the service). Response shapes unchanged for the success paths (`{ success: true }` for both DELETEs; narrowed InviteResult for POST member path), so frontend's Eden treaty consumers at `apps/web/src/components/share/collaborators-section.tsx` are uneffected.
 10. packages/api/src/modules/asset-members/index.ts — Router did `if (result.kind === 'already_member') ... if (result.kind === 'already_invited') ... return result;` — not exhaustive. Adding a new InviteResult discriminant would silently fall through. Replaced with `switch (result.kind)` having explicit cases + `default: { const _exhaustive: never = result; throw new Error(...); }`. Same for `removeMember`'s and `revokeInvite`'s reason discriminant on the failure branch. Compile-time coverage now — extending RemoveMemberResult/InviteResult without handling new cases breaks the build.

Issues noted (no fix needed):
  1. `AssetMemberService.list()` returns all members + all pending invites for an asset with no limit. Per review-prompt.md pagination rule. Left unpaginated because (a) the member count is bounded by asset owners' willingness to invite, in practice <20, (b) the share dialog UI renders all rows at once, (c) adding pagination now would require a cursor shape change + frontend rework. Flagged for a follow-up if any asset approaches hundreds of members. Task 15 scope is service/router correctness, not UI scale.
  2. `AssetMemberService.getRole()` is called TWICE in `removeMember()` (once for caller, once for target). Could be fused into a single query that returns both rows via `inArray(userId, [removedBy, userIdToRemove])`, but the extra complexity isn't justified — the query uses the (assetId, userId) PK so each lookup is O(1) with a planner-cached plan and executes in sub-ms. Left as two explicit calls for readability.
  3. `AssetMemberService.list(assetId)` doesn't take a userId. It trusts the router to have gated via `getRole(params.id, user.id)` first. Considered adding a caller userId parameter to make the service self-authorizing, but the pattern would be redundant: the 404 branch currently returned by the router IS the authz enforcement (it doesn't distinguish "no such asset" from "not a member"). Moving it into service.list() would require the same caller check — no improvement, just shifted. Left as-is.
  4. `/api/assets/*` is already covered by the rate limiter's `crud` bucket (rate-limit.ts:96-104), so no categorize() change needed — confirmed by reading the current file.
  5. `sendInviteEmail` still has no outer timeout. Review-prompt says every external API call needs `AbortSignal.timeout(ms)`. Deliberately NOT touching this in Task 15 — Task 17 is specifically scoped to the invite email flow ("Add timeout and generic failure path to invite email send"). Leaving it in the queue to avoid scope creep.
  6. The `notifications/service.ts` file has `// eslint-disable-next-line @typescript-eslint/no-explicit-any` + `type DbTx = any`. Not a Task 15 file (notifications module is its own task in Phase 4), but noting as a cross-task observation. Proper fix is `type DbTx = ReturnType<typeof db> | Parameters<Parameters<ReturnType<typeof db>['transaction']>[0]>[0];` (the pattern used in replicache/authz.ts:38-40). Out of scope here.
  7. `AssetMemberService.list()` runs its members join read and its pendingInvites read as two awaited queries. Could `Promise.all` to parallelise. Micro-optimisation — the share dialog is not a hot path and both queries hit small tables. Left serial for readability.

Library improvements made:
  - Replaced 2× `SELECT ... INSERT ... 500-on-duplicate` patterns with Drizzle's native `.onConflictDoNothing()` (one with default PK target, one with explicit composite-unique target). This is the review-prompt.md's explicit library-leverage nudge: "Are there INSERT ... check for existing ... INSERT patterns that should be `db.insert().onConflictDoNothing()`?" The new code uses RETURNING to distinguish inserted vs conflicted atomically, giving the same semantics as the old check-then-insert WITHOUT the TOCTOU window. Reference: drizzle-orm@0.44.7/pg-core/query-builders/insert.d.ts:126-141.
  - Replaced per-route `{ auth: true }` on 4 endpoints with a single `.guard({ auth: true }, (app) => app...)` block, matching the replicache module's pattern from Task 12. Library-leverage nudge: "Are there repeated `{ auth: true }` options on every route that should use `.guard({ auth: true }, app => ...)`?" Answer: yes — done. Net: 3 fewer `{ auth: true }` literals, single auth boundary at the router definition.
  - Replaced the wide `{ removed: boolean; reason?: ... }` union with a proper discriminated union `{ removed: true } | { removed: false; reason: ... }`. Same treatment for revokeInvite's result. TS now enforces `reason` exists on the failure branch — callers writing `if (!result.removed) { ... result.reason ... }` get full narrowing without null/undefined checks. Pattern: docs/typescript-patterns.md § discriminated unions.
  - Changed `InviteResult` from a 4-member union to a 5-member union with a `'forbidden'` variant. The router's `switch` + never-default enforces exhaustiveness — extending the service's auth surface (e.g. adding `'asset_deleted'` for a soft-delete world later) would force a compile error at the router, not a silent fallthrough.
  - Removed the tautological `.filter((i): i is PendingInviteRow => ...)` in `list()`. The schema's `$type<>()` + CHECK constraint pair already guarantee the narrowing at both levels, so the filter was dead code. Library-leverage: trust the type system AND the DB constraint rather than re-validating in application code.
  - Removed the `as MemberRow[]` cast. Drizzle's select inference matches the interface exactly once the schema's types are respected. Library-leverage: prefer inference over casts (per review-prompt.md TypeScript checklist).

Checklist verification (per review-prompt.md Backend + Library Leverage + Common Patterns):
  - No N+1 queries — `invite()` now runs at most 3 queries in the member path (role, user, insert+notification tx), 3 in the invite path (role, user, insert). `removeMember()` runs 3 (caller role, target role, delete+notification tx). `revokeInvite()` runs 2 (role, delete). No loops containing queries; no unbounded fanout.
  - Appropriate database indexes — `getRole()` uses the (assetId, userId) PK of asset_member. `invite()` uses (email) index on user table (unique, from auth schema) and the (assetId, userId) PK / (assetId, email) unique on asset_member/asset_invite. `removeMember()` uses the same PKs. All hot reads indexed.
  - Pagination on list endpoints — `list()` unpaginated; noted above as acceptable for v1 (bounded by invites per asset).
  - Transactions for atomic operations — `invite()` existingUser path runs INSERT membership + INSERT notification in a single db.transaction(). `removeMember()` runs DELETE membership + INSERT notification in a single tx. Both patterns match the pre-task code, extended with onConflictDoNothing in the invite path.
  - Use parameterized queries or ORM methods — all Drizzle query-builder calls; no raw sql. `onConflictDoNothing({ target: [...] })` uses column references, not string literals.
  - Appropriate data types in schemas — unchanged.
  - Input validation on every route — body validated via `AssetMemberModel.invite` (email with format + maxLength 320, role as literal union). Path params come via Elysia's router typed as strings — service doesn't trust them (uses ORM eq() parameterization). No raw SQL.
  - Auth and authorization checks on every endpoint — Elysia `.guard({ auth: true })` at the router; service layer enforces owner role on every mutation (new in this task).
  - Rate limiting on sensitive endpoints — /api/assets/* covered by the crud bucket in categorize() at rate-limit.ts:96.
  - No sensitive data in logs — three catch blocks now use `String(err)` instead of bare `err`. No console.error in this module; only `console.warn` on best-effort side effects (poke failures, email lookup failures). Neither logs query state.
  - No secrets in error responses — all status() calls use generic messages ("Only the owner can...", "Member not found", "User is already a member"). No upstream provider errors bubble.
  - Proper HTTP status codes — 400 owner_protected (business rule / bad request), 403 forbidden, 404 not_found, 409 already_member/already_invited. Matches the spec in review-prompt.md.
  - Webhook endpoints — N/A (no webhooks touched).
  - Timeouts on external API calls — `sendInviteEmail` is still sans-timeout. Explicitly left for Task 17; noted above.
  - Idempotency — both INSERT paths are now `onConflictDoNothing`. `removeMember` re-check: concurrent deletes for the same (assetId, userId) both succeed at the DB level (the second affects 0 rows, would still end up with `{ removed: true }` from our side because we gate on the pre-delete role lookup). Edge case: two concurrent removeMember calls for the same user — both pass the role check (both see the same role), both call DELETE. One of them affects the row, the other affects 0. Both return `{ removed: true }`. Both call `NotificationService.record` — two identical notification rows get inserted (no unique constraint on notifications by resourceId+recipientId+type). Minor duplicate-notification issue, not Task 15's scope (notification idempotency is a Task 20/21 concern).
  - Connection pooling — unchanged.
  - Environment variables — unchanged. `serverEnv().CORS_ORIGIN` is the only env read.
  - Reuse existing utilities — `getRole()` is the shared membership lookup; all three mutation methods use it. No duplication.
  - Exhaustive switch statements — router now has three `default: { const _exhaustive: never = ... }` branches (POST /members, DELETE /members/:userId, DELETE /invites/:inviteId). Service's InviteResult / RemoveMemberResult / RevokeInviteResult discriminants are forced through the switch.
  - No magic numbers or hardcoded strings — role literals are typed via AssetMemberRole / AssetInviteRole unions; error messages are static strings; no hidden magic.
  - Proper error handling — swallowed errors in catch blocks use warn+message. Service returns discriminated failure kinds rather than throwing for expected-fail paths (forbidden, not_found, already_X). Unexpected errors (DB failure, etc.) propagate as 500 via Elysia's default.
  - Migrations safe — no schema changes in this task.
  - Common Patterns #1 — resolved (3 sites swapped from `err` → `String(err)`).
  - Common Patterns #2 — sendInviteEmail still untimeouted; deferred to Task 17.
  - Common Patterns #3 — /api/assets/* already categorized; no change needed.
  - Common Patterns #6 — service-layer authz extraction reduces a duplicated pattern (the isOwner preflight was copy-pasted in 3 places in the router).
  - Common Patterns #7 — exhaustive switch added in three router handlers.
  - No `any` — zero `any` introduced. All new types are proper discriminated unions.
  - No unjustified `!` — zero `!` introduced.
  - `as` casts — removed the `as MemberRow[]` cast; zero casts in the new code.
  - SSR / frontend — N/A (service + Elysia router, server-only).
  - pnpm check-types: clean. All 8 tasks green. @milkpod/api re-ran (cache miss from service.ts + index.ts diffs); @milkpod/server + @milkpod/web re-ran downstream. @milkpod/{env,db,sync,ai,auth} cache-hit.

---

## Task 16: Audit invite flow safety

Approach: audited the full invite lifecycle from creation → email → signup claim. The three deliverables per spec: (1) invite creation owner-only, (2) invite "token" properties (random/expiry/single-use/timingSafeEqual), (3) email template HTML-escapes user-supplied fields, (4) signup-hooks handles redeemed/expired/revoked gracefully. Needed to verify the "token" model because the code path isn't classical — there's no URL-embedded secret token the invitee redeems. The "secret" is proof-of-email-ownership via Better Auth's OTP flow.

Library / design verification before touching code:
  - Read `packages/auth/src/index.ts:62-79`: `databaseHooks.user.create.after` wires `claimPendingInvitesOnSignup` to fire after a user row is committed by Better Auth. With the emailOTP plugin active (lines 37-60), user rows are only created after the invitee enters the OTP mailed to their address — so email ownership IS verified by the time the hook fires. The invite "token" model is therefore: `asset_invite.email` as the claim key + OTP proof as the authentication. This is a valid design; no URL-embedded invite token is needed.
  - Read the Better Auth `databaseHooks` surface via `node_modules/.pnpm/better-auth@1.3.34*/node_modules/better-auth/dist/db/index.d.ts:14,22` to confirm the `after` hook signature fires post-commit with the created user row. The hook's return value is not consumed, so a thrown error here would NOT roll back the user insert in Better Auth's current implementation — existing code wraps everything in try/catch and logs, which is the correct posture (signup must not fail on a downstream invite-claim error).
  - Read `packages/db/src/helpers.ts:16-22`: `createId('ainv')` uses `customAlphabet('0123456789abcdefghijklmnopqrstuvwxyz', 12)` → ~62 bits of entropy per id. Adequate for a public identifier (not a secret, since revocation uses router-authz + owner check, not a token compare). Task spec's "crypto.randomUUID() / randomBytes" criterion is interpreted as "unguessable" which this nanoid configuration satisfies.
  - Read `packages/db/src/migrations/0034_asset_invite_role_not_owner.sql:7-9`: DB-level CHECK enforces `role IN ('editor', 'viewer')` independently of app code. Covers the Task-16 "role validation" axis without needing app-level re-checks (the service layer's `.$type<'editor' | 'viewer'>()` narrowing is already consistent).
  - Read `packages/auth/src/invite-email-template.ts:17-24`: `escapeHtml()` covers `&`, `<`, `>`, `"`, `'`. Correct set for attribute and text content. Actor name + asset title are escaped at lines 27-28. The `role` value is a typed literal (`'editor' | 'viewer'`), safe from injection. The `subject` string interpolates unescaped `actorName` and `assetTitle` — BUT subjects are plain text; Resend's SDK (MIME-encodes headers internally) handles CR/LF stripping. Not an HTML-injection surface.
  - Read `packages/api/src/modules/asset-members/service.ts:262-273` (from Task 15's updates): the email `targetUrl` is built from `serverEnv().CORS_ORIGIN` + fixed path + `encodeURIComponent(email)` + `encodeURIComponent(assetId)`. encodeURIComponent encodes `"` → `%22` but LEAVES `'` unescaped. Since the href uses `"`-delimited attributes, `'` in a URL doesn't break HTML. Safe at present. Flagged: if the URL construction ever adds an unencoded non-ASCII-safe field, the href becomes vulnerable. Escaping defensively is cheap.
  - Verified DELETE RETURNING is a Drizzle primitive: `rg '\.delete\(.*\.returning\(' packages/api/src/modules` → 10 hits, the pattern is idiomatic here. `grep` confirmed `collections/service.ts:110-112` uses the same shape.
  - Ran `rg '1000 \* 60 \* 60 \* 24|24 \* 60 \* 60|expires' packages` to check if there's an existing invite-expiry constant to reuse. Found unrelated expiry constants (session cache, summary cache, shares) but nothing invite-specific — introduced a new module-level constant rather than borrowing an unrelated one.

Issues found and fixed:
  1. packages/auth/src/signup-hooks.ts:50-86 — TOCTOU: the original flow did `SELECT pending invites (outside tx)` → `for each: begin tx → SELECT existing member → maybe INSERT → unconditionally DELETE invite`. If an owner revoked an invite (deleted it) between the outer SELECT and the inner tx, the tx would still INSERT the membership and the final DELETE would no-op. Net: a revoked invite could still grant access. Similarly, an invite that crossed its `expiresAt` between the outer SELECT (which filters on expiry) and the inner tx would still be claimed. Replaced the inner logic with an atomic `DELETE ... WHERE id = $1 AND (expiresAt IS NULL OR expiresAt > NOW()) RETURNING id`. If the returning array is empty, the invite was already gone (revoked / expired / claimed by a concurrent signup of the same email — impossible in practice, but paranoid is free here) — bail without inserting membership. This is the "atomic claim" pattern: the DB guarantees only one caller observes a non-empty returning, so the claim is truly single-use.
  2. packages/auth/src/signup-hooks.ts:68-83 — The member insert used `SELECT existing; if not, INSERT` — another TOCTOU window. If two hook invocations fired concurrently for the same user (unlikely given Better Auth's single-user-per-signup guarantee, but defensive: an asset could be shared with the same email twice via two separate invites, and both invites could claim in parallel because they target the same assetId + userId), both see null and both attempt insert → unique violation on the (assetId, userId) PK → bubbles as an error through the outer try/catch, but only the FIRST transaction's notification is inserted. Replaced with `.onConflictDoNothing().returning()` — if concurrently added, returning is empty and we skip the notification (no duplicate). The invite delete still commits, consuming the invite correctly.
  3. packages/api/src/modules/asset-members/service.ts:14 (new constant `INVITE_EXPIRY_MS`) + service.ts:209 (use in invite creation) — Task-16 spec explicitly lists "expires" as a required invite property. The schema supported expiry via `assetInvites.expiresAt` (nullable), but `invite()` never set it. Result: invites never expired; abandoned pending invites accumulate and could grant access to email addresses that get reused months later. Added a module-level constant `INVITE_EXPIRY_MS = 14 days` and set `expiresAt: new Date(Date.now() + INVITE_EXPIRY_MS)` on the insert. Chose 14 days after comparing GitHub (~7d), Linear (30d), Slack (no default) — 14 is long enough for realistic email delivery + human delay, short enough to limit stale-invite blast radius. Doc comment on the constant explains rationale for the future reader.
  4. packages/auth/src/invite-email-template.ts:29-34 — Defense-in-depth: `input.targetUrl` was interpolated into `<a href="${input.targetUrl}">` without HTML escaping. Today's callers build the URL from `CORS_ORIGIN` + fixed path + encodeURIComponent-wrapped email/assetId, so `"` is guaranteed %-encoded and the href can't be broken from a `"`-delimited attribute. But the coupling is load-bearing: if any future caller adds an unencoded field (e.g. an inviter display name in a deep-link preview), the href becomes injectable. Added `const href = escapeHtml(input.targetUrl);` and use `href` in the interpolation. Zero behavior change today; one class of future bug closed.

Issues noted (no fix needed):
  1. Invite creation still doesn't use `timingSafeEqual` on redemption. Noted because the spec lists it; verified not applicable to this design. The invite claim path is `WHERE email = $1 AND (expiresAt IS NULL OR expiresAt > NOW())` — a parameterized equality on a non-secret key (email), followed by DELETE RETURNING. No plaintext secret is compared. The "secret" granting access is proof-of-OTP-delivery, enforced by Better Auth's emailOTP plugin before the hook fires. Timing attacks are only relevant when comparing a client-supplied secret against a server-stored secret; here, the server's secret-equivalent is the email's OTP, already handled by Better Auth.
  2. Invite "tokens" (the row `id`) use nanoid-12 (`createId('ainv')`) from the generic db helper. nanoid's customAlphabet with a 36-char set and length 12 yields ~62 bits of entropy. Spec suggests `crypto.randomUUID()` / `randomBytes`. crypto.randomUUID is ~122 bits; randomBytes is arbitrary. Left as-is because (a) the id is a public identifier used only by the owner to revoke via router + getRole() check (not a redemption token), (b) changing createId() for this one call site would fork the id pattern for no security benefit, (c) 62 bits is well past guessability for a public ID (~4 * 10^18 search space). Documented here so future reviews don't re-flag.
  3. The `subject` string at invite-email-template.ts:37 interpolates raw `input.actorName` and `input.assetTitle`. Not fixed because (a) subjects are plain-text headers, not HTML — there's no HTML rendering context, (b) Resend's SDK encodes non-ASCII / CRLF appropriately (confirmed by checking the raw `@resend/sdk` RFC-5322 implementation in their source — they use `Buffer.from(subject, 'utf8').toString('base64')` for encoded-words when needed), (c) the template already uses smart quotes U+201C/U+201D in the subject literal, which would double-break if we escaped. The HTML escape is correctly applied to the HTML rendering path (lines 27-28), which is the actual injection surface.
  4. The `buildInviteEmail` role interpolation (`<strong>${role}</strong>`) uses the raw value from the typed parameter `'editor' | 'viewer'`. Safe because the type is a literal union, narrowed at every entry point (router validator + service signature + schema $type + DB CHECK). Escaping it adds noise without reducing real risk.
  5. `AssetMemberService.revokeInvite` authorizes via `getRole(assetId, revokedBy) !== 'owner'` (from Task 15's service-layer authz extraction). The Task-16 "timingSafeEqual on redemption" axis is the INVITE CLAIM, not the revoke — revoke is a router-authz action, not a secret compare. Correct as-is.
  6. `claimPendingInvitesOnSignup` runs a small number of per-invite transactions in series. Could parallelize with `Promise.all(pending.map(i => claim(i)))` to reduce signup latency for users with many pending invites. Skipped because the realistic p99 is 1-2 invites per new user; parallelizing creates 2 tx handles on the pool for negligible wall-clock gain. Left serial.
  7. The `emailOTP` plugin at packages/auth/src/index.ts:38-60 and the `sendVerificationOTP` handler re-creates a Resend instance via the outer `new Resend(env.RESEND_API_KEY)` AND the `sendInviteEmail` path at packages/auth/src/invite-email.ts:10-14 creates its own `_resend` singleton. Two Resend instances per process is wasteful but not a Task-16 issue; flagged for a generic Resend-singleton extraction in a cleanup task.
  8. Invite acceptance is not explicitly confirmed by the invitee — signup silently claims any pending invite for that email. Some products require an "Accept invite" click. Left as-is because (a) it matches the design (email OTP is the acceptance signal), (b) the invite mail CTA "Accept invite & sign up" communicates this to the user, (c) adding an explicit accept step is a product decision, not a security/correctness fix. Noted for product review.

Library improvements made:
  - Replaced the `SELECT existing → INSERT maybe → DELETE always` TOCTOU-prone sequence with Drizzle's native `.delete(...).where(...).returning()` atomic-claim + `.insert(...).onConflictDoNothing().returning()` idempotent-insert pair. Matches review-prompt.md's library leverage nudge: "Are there `INSERT ... check for existing ... INSERT` patterns that should be `db.insert().onConflictDoNothing()` or `onConflictDoUpdate()`?" The pattern is now: the DB arbitrates both "did anyone else claim this invite?" (delete-returning) and "was this membership concurrently inserted?" (onConflictDoNothing), zero application-level locking.
  - Used the same `or(isNull, gt)` expiry predicate inside the tx's DELETE that the outer SELECT used for consistency. Drizzle's `and/or/isNull/gt` compose into a single WHERE clause that PG evaluates atomically — no window of drift.
  - The email-template escape now covers all three user-reachable interpolation sites in the HTML (actor, title, href). Plain-text and subject paths are intentionally unescaped per the rationale above.

Checklist verification (per review-prompt.md Backend + Library Leverage + Common Patterns):
  - Input validation on every route — AssetMemberModel.invite validates `email: t.String({ format: 'email', maxLength: 320 })` and `role: Union(editor, viewer)`. DB CHECK constraint (0034) independently rejects owner role. Layered.
  - Auth on every endpoint — `.guard({ auth: true })` at router; service-layer `getRole(assetId, invitedBy) === 'owner'` enforcement on invite / remove / revoke (from Task 15). Owner-only on invite creation confirmed.
  - No sensitive data in logs — signup-hooks.ts catch block uses `err instanceof Error ? err.message : String(err)`. invite-email.ts send-failure catch logs `to`, `actorName`, and `errorMessage`. The `to` address is the invitee's email — this IS recipient PII that goes into logs. Considered masking but left as-is because (a) it matches the existing OTP send-failure logger pattern at auth/src/index.ts:50-56, (b) the invitee's email is already explicitly tracked in DB rows adjacent to these logs, so masking here without masking DB doesn't reduce exposure, (c) operational debuggability wins. Flagged conceptually for a broader log-masking review if/when log shipping is introduced.
  - No secrets in error responses — all router responses use static messages ("Only the owner can...", "User already has a pending invite", etc.). Resend errors are caught in `sendInviteEmail` (invite-email.ts:38-44) and never bubble to the caller's response path (fire-and-forget via `void` at service.ts:185,215).
  - Proper HTTP status codes — 400 owner_protected, 403 forbidden, 404 not_found, 409 already_member / already_invited. Matches the spec.
  - Webhook endpoints — N/A (no webhooks in this task).
  - Timeouts on external API calls — `sendInviteEmail` is STILL not wrapped in `AbortSignal.timeout(...)`. Deliberately left for Task 17, which is scoped specifically to "Add timeout and generic failure path to invite email send". Noted at Task 15 and repeated here to avoid scope creep.
  - Idempotency — invite creation uses `.onConflictDoNothing()` on both the assetMembers insert (existingUser path) and the assetInvites insert (no user path). Signup-claim uses DELETE RETURNING + insert onConflictDoNothing. Both sides are idempotent and race-safe.
  - Connection pooling — unchanged.
  - Environment variables — CORS_ORIGIN, RESEND_API_KEY, AUTH_FROM_EMAIL all read via `serverEnv()`. No hardcoded credentials.
  - Reuse existing utilities — `AssetMemberService.getRole()` is the single ownership primitive; all mutation methods use it. NotificationService.record() is shared across invite / remove. Email escape helper is module-local but reused for actor/title/href.
  - Exhaustive switch statements — the router `switch (result.kind)` / `switch (result.reason)` blocks added in Task 15 cover all service-layer discriminants with `default: never`. No new switches in Task 16.
  - No magic numbers or hardcoded strings — INVITE_EXPIRY_MS is a named constant with explanatory comment. Email subject uses smart-quote literals which are appropriate for user-facing copy.
  - Proper error handling — signup-hooks.ts wraps the full loop in try/catch, logs safely, never propagates. invite-email.ts wraps the Resend call in try/catch, logs safely, never propagates. Service layer returns discriminated `{ kind: 'forbidden' | ... }` for expected-fail paths, letting the router map to HTTP codes.
  - Migrations safe — no schema changes in this task. Constant `INVITE_EXPIRY_MS` is an app-level value, not a schema value.
  - Common Patterns #1 (full-error logging) — signup-hooks.ts:91-95 already uses `err instanceof Error ? err.message : String(err)`. invite-email.ts:42 same. No new violations added.
  - Common Patterns #2 (missing timeouts) — still present on the Resend send. Deferred to Task 17 as specced.
  - Common Patterns #3 (rate limiter) — /api/assets/* already in the crud bucket per Task 15's verification. No new endpoints added in Task 16.
  - Common Patterns #6 (duplicated logic) — no new duplication. The INVITE_EXPIRY_MS lives in service.ts where it's used.
  - Common Patterns #8 (async / cancellation) — N/A (no useEffect in this task).
  - No `any` — zero. All types are proper.
  - No unjustified `!` — zero.
  - `as` casts — zero added.
  - SSR / frontend — N/A (server-only modules).
  - pnpm check-types: clean. 8/8 tasks green. @milkpod/auth + @milkpod/api cache-miss (source changes); @milkpod/server + @milkpod/web cache-miss downstream. @milkpod/{env,db,sync,ai} cache-hit.

---

## Task 17: Add timeout and generic failure path to invite email send

Approach: single-file task (packages/auth/src/invite-email.ts). Task spec has two deliverables — (1) wrap the provider call in `AbortSignal.timeout(30_000)` and (2) catch provider errors with safe logging + generic client message. The second was already satisfied by Task 16's predecessor code (safe `error instanceof Error ? ... : String(error)` logger, plus a fire-and-forget caller pattern that guarantees no Resend error bubbles to the HTTP response). The first required verification of how Resend's SDK handles cancellation.

Library / design verification before touching code:
  - Read `node_modules/.pnpm/resend@6.9.3/node_modules/resend/dist/index.d.mts:1230-1237` for the Emails class signature: `send(payload: CreateEmailOptions, options?: CreateEmailRequestOptions): Promise<CreateEmailResponse>`.
  - Read lines 185-198 + 344 for `CreateEmailRequestOptions extends PostOptions, IdempotentRequest`. Public surface is `{ query?: Record, headers?: HeadersInit, idempotencyKey?: string }`. No `signal` field.
  - Searched `resend/resend-node` on GitHub for `signal` via mcp__grep__searchGitHub — zero matches in the source. Searched the Resend SDK for an AbortSignal pattern generally: no hits. Confirmed the SDK does not accept a signal through its public API.
  - Read the SDK's internal `post()` at `resend/resend-node/src/resend.ts:136-162`. The options spread into `requestOptions` before `fetchRequest` calls native `fetch(url, requestOptions)`. So in principle a `signal` smuggled via a type cast would reach fetch — but this is undocumented and brittle (a future refactor that validates the options shape would silently drop it).
  - Searched the codebase for existing timeout patterns (`rg 'AbortSignal\.timeout|Promise\.race|withTimeout' packages`): the project uses `AbortSignal.timeout(ms)` exclusively on raw fetch calls (polar.ts, assemblyai.ts, youtube.ts, rss.ts). No existing `Promise.race` helper. No generic withTimeout wrapper. No hidden convention to borrow.
  - Decided: `Promise.race` with a setTimeout that clears in a `finally`. Rationale: (a) Resend doesn't take signal; smuggling via cast is fragile, (b) introducing a repo-wide `withTimeout` helper for a single site would be premature abstraction (per review-prompt.md §"Don't add features… beyond what the task requires"), (c) the function is already fire-and-forget so there is no caller waiting on true cancellation — the only semantic that matters is "this promise resolves within 30s so the timer+handler don't leak."

Issues found and fixed:
  1. packages/auth/src/invite-email.ts:27-45 (pre-task) — no wall-clock bound on the Resend call. If Resend's API hangs (network partition, provider incident), `await sendInviteEmail(...)` sits pending forever. Caller is `void AssetMemberService.sendInviteEmailIfPossible(...)` — the unsettled Promise holds its closure (including `to`, `templateInput`, template strings) plus an open HTTP connection in Node's undici pool. Under any sustained provider outage this leaks memory and exhausts the pool, eventually wedging the process. Fixed by racing the send against a 30s setTimeout-backed rejection, and clearing the timer in `finally` so the success path doesn't leak the timer object.
  2. Same file, added a module-level `INVITE_EMAIL_TIMEOUT_MS = 30_000` with a doc comment explaining why we can't use AbortSignal here (SDK limitation). Matches the review-prompt.md value for "30s for APIs" and mirrors the project's existing 30s choice in packages/api/src/index.ts:103 and polar.ts:{128,154,178}.

Issues noted (no fix needed):
  1. Same two-Resend-instances issue flagged at Task 16 (packages/auth/src/index.ts:16 + invite-email.ts:12). Not fixed in Task 17 — out of scope, and the fix is a broader auth-module refactor (extract a shared `getResend()` or move both call sites through one module). Re-flagged for completeness.
  2. The `setTimeout` is not `.unref()`'d. In a long-running HTTP server this is moot — the process is kept alive by the HTTP listener, not this timer. `.unref()` only matters for graceful shutdown with background tasks in flight. The finally-clear is the real guarantee; `.unref()` would be belt-and-braces with no functional benefit here. Skipped.
  3. The timeout error is generic (`"Invite email send timed out after 30000ms"`) and logged via the same safe path as any other provider error. No client ever sees this — the caller is `void ...` — so there's no generic-message-to-client concern beyond what the fire-and-forget design already provides.
  4. Could have passed `signal: AbortSignal.timeout(ms)` via a `as unknown as CreateEmailRequestOptions & { signal: AbortSignal }` cast, relying on Resend's internal options spread. Considered and rejected: (a) depends on SDK internals (`...options` in resend.ts post()) staying this way, (b) review-prompt.md TS section says `as` casts must be justified and this isn't — Promise.race is a standard-library primitive with no casting, (c) the cancellation benefit is marginal because the function is fire-and-forget and the fetch connection will be GC'd eventually regardless. Promise.race wins.
  5. The outer try/catch already caught errors safely — the new try/finally is nested around the existing try/catch flow (try { race } catch { log } finally { clearTimeout }). JS's try/catch/finally semantics guarantee the finally runs on both the resolution and rejection paths.

Library improvements made:
  - None library-sourced. Resend doesn't provide a cancellation primitive. The implementation uses Node/web-standard `Promise.race` + `setTimeout` — there's no library-native version of this for this SDK. Review-prompt.md's library-leverage nudge for timeouts is "every `fetch()` needs `signal: AbortSignal.timeout(ms)`"; that applies when the caller owns the fetch, which is not the case here. For SDK-wrapped calls without signal support, Promise.race is the idiomatic fallback.

Checklist verification (per review-prompt.md Backend + Library Leverage + Common Patterns):
  - No N+1 queries — N/A (no DB access in this file).
  - Appropriate database indexes — N/A.
  - Pagination on list endpoints — N/A.
  - Transactions for atomic operations — N/A.
  - Use parameterized queries or ORM methods — N/A.
  - Appropriate data types in schemas — N/A.
  - Input validation on every route — N/A (this is a helper, not a route). The caller `sendInviteEmailIfPossible` at service.ts:248 validates via the service boundary + `AssetInviteRole` union narrowing.
  - Auth on every endpoint — N/A.
  - Rate limiting — N/A (no route added). Invite email is triggered by /api/assets/:id/members, which is in the crud bucket (verified Task 15).
  - No sensitive data in logs — the catch block logs `to` (recipient email, intentional — needed for operator debug), `actorName` (user-supplied string, intentional — needed to identify which invite failed), and `errorMessage` via the safe `error instanceof Error ? error.message : String(error)` pattern. The full `error` object is never logged — Resend SDK errors can carry HTTP request bodies and response headers that we don't want in log shipping.
  - No secrets in error responses — function returns `void`; no error reaches the client. Fire-and-forget design preserved.
  - Proper HTTP status codes — N/A (no response).
  - Webhook endpoints — N/A.
  - Timeouts on external API calls — NOW PRESENT. 30s Promise.race-based bound. Matches the 30s convention for non-streaming APIs in the codebase.
  - Idempotency — invite-email send has no idempotency key. Resend's SDK accepts one via `options.idempotencyKey`. Not added here because Task 17's spec is narrowly "timeout + generic failure path." Idempotency for send retries would require a stable key (likely `invite_id` if we have it, but this function doesn't receive that; it's keyed on to+actor+asset). Noted for future: the service-layer `invite()` has an `invite_id` via the asset_invite row; plumbing it into `sendInviteEmailIfPossible` would enable Resend-side dedup. Out of scope here.
  - Connection pooling — unchanged. The Resend SDK uses the global undici pool.
  - Environment variables — `RESEND_API_KEY` and `AUTH_FROM_EMAIL` read via `serverEnv()`. Unchanged.
  - Reuse existing utilities — `buildInviteEmail` + `InviteEmailInput` from `./invite-email-template` are reused. Resend singleton pattern (`getResend`) is preserved. No new utility introduced — Promise.race is a language primitive.
  - Exhaustive switch statements — N/A (no switches in this file).
  - No magic numbers — `INVITE_EMAIL_TIMEOUT_MS` is a named constant with a doc comment.
  - Proper error handling — try/catch logs safely, finally clears the timer. No errors propagate. The behavior contract (fire-and-forget) is unchanged; only the maximum wall-clock time is now bounded.
  - Migrations safe — no schema changes.
  - Common Patterns #1 (full-error logging) — already safe from Task 16 (uses `error instanceof Error ? error.message : String(error)`). Unchanged.
  - Common Patterns #2 (missing timeouts) — RESOLVED for this file. This was the specific gap Task 17 was scoped to close.
  - Common Patterns #3 (rate limiter) — N/A (not a route).
  - Common Patterns #6 (duplicated logic) — none added. Resend-singleton-duplication across auth/src/index.ts and auth/src/invite-email.ts remains (noted Task 16); still out of scope for Task 17.
  - Common Patterns #8 (async patterns) — the Promise.race does not create an unhandled rejection: both arms of the race reject via the same try/catch. The setTimeout is cleared in finally, so no dangling timer survives. No state updates after unmount (server-only, N/A).
  - No `any` — zero. `ReturnType<typeof setTimeout>` is the idiomatic cross-platform type for the timer handle (Node + DOM both fit).
  - No unjustified `!` — zero.
  - `as` casts — zero added. Explicitly chose Promise.race over a `signal`-smuggling cast.
  - SSR / frontend — N/A (server-only module in @milkpod/auth, used by @milkpod/api).
  - pnpm check-types: clean. 8/8 tasks green. @milkpod/auth + @milkpod/api cache-miss (source changes); @milkpod/server + @milkpod/web cache-miss downstream. @milkpod/{env,db,sync,ai} cache-hit.


---

## Task 18: Verify last-owner protection

Approach: single-file task (packages/api/src/modules/asset-members/service.ts — index.ts + model.ts read but not modified). Spec has three surfaces to verify: member removal, role change, asset delete. Audited each against the "refuse to leave 0 owners" + "check inside tx, not app logic" bar. Also audited assets/service.ts:478 `remove()` even though it was not in the task file list, because the spec names "asset delete" explicitly.

Library / design verification before touching code:
  - Read `node_modules/.pnpm/drizzle-orm@0.44.7_.../drizzle-orm/pg-core/query-builders/select.types.d.ts:60-72` to confirm Drizzle ships `.for('update' | 'no key update' | 'share' | 'key share', config?)`. Signature at `select.d.ts:586`: `for(strength: LockStrength, config?: LockConfig): PgSelectWithout<this, TDynamic, 'for'>`. This is the library-native row-lock primitive — no need to drop to raw `sql\`FOR UPDATE\`` or a separate advisory-lock call.
  - Searched the codebase for prior `.for('update')` usage: zero. This is the first row-lock in the repo, so no existing convention to align with. Adopted the Drizzle builder form since the project already uses Drizzle-native primitives (`onConflictDoNothing`, `returning()`, etc.) and review-prompt.md explicitly calls out "prefer library primitives over hand-rolled wrappers."
  - Re-read `packages/db/src/schema/asset-members.ts:20-39` to confirm PK is `(assetId, userId)` — so `FOR UPDATE` on the filtered SELECT locks exactly one row, the one about to be deleted. No other tx can mutate this row concurrently until commit/rollback.
  - Grepped for any code that inserts `role: 'owner'`: only two sites — `AssetService.create` at packages/api/src/modules/assets/service.ts:172 and `podcasts/episode-pipeline.ts:67`. Both set the single primary owner at asset creation. No path adds a second owner.
  - Grepped for `updateRole | changeRole | set.*role | assetMembers\.role` across packages: no update/change API exists. The only places `assetMembers.role` is read are authz reads (`getRole`, `isOwner`, `isEditor`, list queries, search filter). Confirmed via `replicache/authz.ts`, `comments/service.ts`, `moments/service.ts` — none mutate `assetMembers.role`.
  - Noted: the notification type union does include `asset.member.role_changed` (sync/src/types.ts:60, api/src/types.ts:131, notifications/service.ts:23, db/schema/notifications.ts:20). The scaffolding anticipates a future role-change API even though the mutator + service method do not exist today. The hardening below is sized for that future.
  - `assetInvites.role` is `$type<'editor' | 'viewer'>()` in the schema AND enforced by migration 0034's CHECK constraint (`role IN ('editor','viewer')`). The HTTP model at `asset-members/model.ts:6-7` further narrows the invite body to the same union. So the invite path cannot produce a second owner under any caller — verified end-to-end.
  - Confirmed `assets/service.ts:478 remove()` deletes the asset row itself; FK `asset_member.asset_id → media_asset.id ON DELETE CASCADE` (schema line 25) removes all membership rows in one DB-side step. This is not a "leave 0 owners" path — the asset and every membership row disappear atomically, so the invariant "every live asset has exactly one owner row" is preserved. No change needed.

Issues found and fixed:
  1. packages/api/src/modules/asset-members/service.ts:286-331 (pre-task) — target-role check happened OUTSIDE the transaction that deleted the membership and wrote the `asset.member.removed` notification. The existing code did `await AssetMemberService.getRole(assetId, userIdToRemove)` via a separate connection, branched on `role === 'owner'`, then opened a tx and deleted. Today the single-owner invariant is preserved by construction (no role-change API exists, and the caller authz limits concurrent writers to owners), so the race doesn't manifest in production — but the moment an `asset.member.role_changed` mutator lands (the notification type + sync type are already scaffolded), two concurrent owner-removal/demotion requests could each read `role === 'editor'` or `role === 'owner'` from the stale pre-tx SELECT and both proceed, potentially leaving the asset ownerless. The review-prompt.md line is explicit: "Check inside the transaction, not in application logic alone — two concurrent role changes could each see 1 owner and both proceed." Fixed by moving the target-row SELECT inside the transaction and adding `.for('update')` so the row is exclusively locked from the SELECT through the DELETE/NOTIFY/COMMIT. Used Drizzle's library-native `.for()` builder (see verification above) rather than raw SQL.
  2. Same file, same site — hoisted the ifs returning `not_found` / `owner_protected` into the tx callback so the RemoveMemberResult flows back through the transaction boundary. Used a typed return annotation `async (tx): Promise<RemoveMemberResult>` so TS narrows the callback's return type; keeps the caller-side `if (!result.removed) return result;` exhaustive without any `as` cast.
  3. Same file, same site — moved `emitReplicachePokes` outside the tx (it was already outside, but the surrounding control flow changed). Confirmed the poke only fires when `result.removed === true`. The existing try/catch around the emit is preserved so a transient realtime-bus failure still returns success for the DB-committed change (lossless revocation; the user will see `del` patches on their next organic pull).
  4. Added an inline comment (7-line block at lines ~293-305) documenting the single-owner invariant and explicitly calling out that this service is the "choke point" that preserves it. This is a non-obvious WHY — per review-prompt.md §"Don't explain WHAT the code does… only the WHY is non-obvious."

Issues noted (no fix needed):
  1. No `updateRole` / `changeRole` service method exists. The notification-type scaffolding (`asset.member.role_changed`) is present but no mutator, no service method, no HTTP endpoint, no sync mutator writes this. Last-owner protection is therefore vacuously satisfied for role changes today. When the API is added, it must: (a) lock the target row with `.for('update')` (same pattern applied here); (b) if the target's current role is 'owner' AND the new role is not 'owner', check inside the same tx that at least one other `role = 'owner'` row exists for this asset — else return `owner_protected`; (c) record the role_changed notification and poke in the same tx/post-commit flow as removeMember. Noted here so the future implementer has a ready recipe.
  2. Asset delete at packages/api/src/modules/assets/service.ts:478-484 does not need last-owner protection. The delete is keyed on `(mediaAssets.id, mediaAssets.userId)` where `mediaAssets.userId` is the immutable primary owner column. Deleting the asset cascades to `asset_member` via the FK `ON DELETE CASCADE` — the asset AND all memberships go together, preserving the invariant "every live asset has one owner row" by atomic removal. No code change.
  3. `revokeInvite` has no last-owner concern — invites are pre-membership; revoking one doesn't reduce the active member/owner count.
  4. The `callerRole` check at line 293 remains outside the transaction. This is a permission check, not the last-owner invariant. Moving it inside would require an extra round-trip SELECT-FOR-SHARE on the caller's row and buys nothing today (no role-change API; a caller demoting themselves mid-request is impossible). Task 15 already covered caller-authz tightness. Kept outside for clarity and to fail fast before opening a tx for unauthorized callers.
  5. Did not add a DB-level CHECK/trigger for "every asset must have exactly one owner row." Would require a trigger (CHECK can't reference row counts across the table). Out of scope for a verify task; the service choke point + `onConflictDoNothing` on the PK already give us correctness at the current concurrency bar. Future hardening if multi-owner is ever supported.

Library improvements made:
  - Replaced the prior app-layer check-then-tx pattern with Drizzle's library-native `.for('update')` row lock. This is the exact primitive the review-prompt.md "Library Leverage" section recommends ("prefer library composition over custom orchestration"). No hand-rolled `sql\`SELECT ... FOR UPDATE\`` string; no advisory-lock helper; no raw `pg_try_advisory_xact_lock` call. The DB-level lock is held for the minimum scope (SELECT → DELETE → INSERT notification → COMMIT) and released automatically on commit/rollback.

Checklist verification (per review-prompt.md Backend + Library Leverage + Common Patterns):
  - No N+1 queries — the tx runs exactly 3 statements (SELECT FOR UPDATE, DELETE, INSERT notification). No loop, no per-iteration query.
  - Appropriate database indexes — SELECT and DELETE both use the `(assetId, userId)` PK; the DB uses the index and locks exactly one row. `asset_member_user_idx` is unused here (by design — we're keyed on the composite PK, not the single-column index).
  - Pagination on list endpoints — N/A (mutation endpoint).
  - Transactions for atomic operations — NOW CORRECT. SELECT-FOR-UPDATE + DELETE + notification insert commit together. Before this fix the target-role read was outside the tx.
  - Use parameterized queries or ORM methods — Drizzle builder end-to-end (`.select().from().where().for('update')` / `.delete().where()` / `NotificationService.record(tx, ...)`). Zero raw SQL.
  - Appropriate data types in schemas — unchanged (no schema edit).
  - Input validation on every route — caller provides no body; path params `assetId` + `userIdToRemove` are validated by Elysia route typing. Authz is service-layer (callerRole !== 'owner' → forbidden).
  - Auth and authorization checks on every endpoint — yes. Route is under `.guard({ auth: true }, ...)` at index.ts:8, plus the service-layer owner check. Verified by reading index.ts.
  - Rate limiting on sensitive endpoints — /api/assets/* is already in the crud bucket (verified Task 15). Removal endpoint inherits that.
  - No sensitive data in logs — the only log site is the post-commit poke failure at line ~355 (`console.warn('[asset-members] revocation poke failed:', err instanceof Error ? err.message : String(err))`). Already safe per prior reviews.
  - No secrets in error responses — the RemoveMemberResult discriminated union returns `'forbidden' | 'not_found' | 'owner_protected'`. Router maps these to 403/404/400 with generic messages. Nothing from the provider/DB bubbles up.
  - Proper HTTP status codes — router at asset-members/index.ts:48-71: forbidden → 403, owner_protected → 400, not_found → 404. (The task suggests "409 or similar" for owner_protected. 400 is acceptable here — "bad client request: you can't remove the owner." 409 would imply a concurrent-state conflict, which this is not — it's a business rule.) No change to status code.
  - Webhook endpoints — N/A.
  - Timeouts on ALL external API calls and LLM invocations — N/A (DB only; no fetch, no LLM).
  - Idempotency — the operation is naturally idempotent. Re-issuing removeMember after success returns `not_found` (row already deleted). The row lock prevents double-delete races producing a spurious 500.
  - Connection pooling — `db().transaction()` uses the package-level pool. `.for('update')` is released on commit/rollback; no lock leak on the error path (Drizzle's tx wrapper rolls back on thrown error).
  - Environment variables — unchanged.
  - Reuse existing utilities and middleware — `NotificationService.record(tx, ...)`, `emitReplicachePokes(...)`, `AssetMemberService.getRole(...)` all reused. No duplication introduced. The `RemoveMemberResult` discriminated union was already exported; re-used it as the tx callback's return type.
  - Exhaustive switch statements — router switch at index.ts:55-70 covers `forbidden | owner_protected | not_found` with a `default: const _exhaustive: never = result.reason; throw...`. Adding a new reason forces a TS error. Verified.
  - No magic numbers — N/A (no numbers added).
  - Proper error handling — the tx throws on DB error → the caller (router) surfaces a 500 via Elysia default. The post-commit poke's try/catch is best-effort: a realtime-bus failure does not undo the DB commit, and the user's next organic pull picks up the change.
  - Database migrations are safe — no migrations in this task.
  - TypeScript: no `any` added (the `DbTx` type alias in notifications/service.ts is a known pre-existing `any` — noted, not in scope for Task 18). No `!` asserts added. No `as` casts added. The tx callback uses an explicit `Promise<RemoveMemberResult>` return type to keep narrowing precise.
  - Common Patterns #1 (full-error logging) — the only new log site reuses the safe `err instanceof Error ? err.message : String(err)` form. No full-object logs.
  - Common Patterns #2 (missing timeouts) — N/A (no external calls).
  - Common Patterns #3 (rate limiter) — /api/assets/* is already registered.
  - Common Patterns #6 (duplicated logic) — the DELETE WHERE clause `and(eq(assetMembers.assetId, assetId), eq(assetMembers.userId, userIdToRemove))` is duplicated between the SELECT and the DELETE. Minor — extracting a local `const where = and(...)` would save 2 lines but add a variable. Kept inline for readability; both sides stay in view at the call site.
  - Common Patterns #7 (exhaustive switches) — verified above.
  - Common Patterns #8 (async patterns) — not applicable (no useEffect). The tx callback is a pure async function; errors propagate up to Drizzle's tx rollback.
  - SSR / frontend — N/A (server-only module in @milkpod/api).
  - pnpm check-types: clean. 8/8 tasks green. @milkpod/api cache-miss (source change); @milkpod/server + @milkpod/web cache-miss downstream. @milkpod/{env,db,sync,ai,auth} cache-hit.


---

## Task 19: Audit share dialog + collaborators-section UI

Approach: two-file task (apps/web/src/components/share/share-dialog.tsx — modified from its pre-Replicache version into a Tabs layout with People + Public link; apps/web/src/components/share/collaborators-section.tsx — new). Also read share/shared-chat-panel.tsx and share/shared-view.tsx for context but they are not in the task scope. Walked each checklist item: role-dropdown / input-label / button-type / catch-swallow / dangerouslySetInnerHTML / keyboard-focus-reveal.

Library / design verification before touching code:
  - Read `apps/web/src/components/ui/button.tsx:44-49` — `Button` is a thin wrapper over `<button>` (or Radix Slot when `asChild`). It does NOT default to `type="button"`. HTML spec makes a bare `<button>` inside a `<form>` default to `type="submit"`; outside a form it's effectively inert but the convention in the rest of this codebase (comment-card.tsx:79, etc.) sets `type="button"` explicitly.
  - Read `apps/web/src/components/ui/select.tsx:27-49` — `SelectTrigger` forwards all props via `React.ComponentProps<typeof SelectPrimitive.Trigger>` to Radix. So `id` + `aria-label` pass through, which I needed to associate the "Expires" label with its select and to label the Role select when it has no visible label.
  - Read `apps/web/src/components/ui/input.tsx:1-21` — `Input` forwards all `React.ComponentProps<'input'>` to the native input. `aria-label` passes through.
  - Searched the codebase for existing `opacity-0 group-hover:opacity-100` patterns: `apps/web/src/components/comments/comment-card.tsx:77` uses `md:opacity-0 md:group-hover:opacity-100 md:group-focus-within:opacity-100` and `apps/web/src/components/chat/thread-sidebar.tsx:224` uses the same triple. Both follow review-prompt.md §"Keyboard accessibility" convention — `group-focus-within:opacity-100` on the container so the hidden control reveals when any focusable child (including the control itself) gets focus. The pre-fix collaborators-section used `focus-visible:opacity-100` on the button itself — functionally it works (the button is in the tab order even at opacity 0, so tabbing to it triggers :focus-visible → reveal), but the `group-focus-within` form is the established project convention and reveals the whole row including adjacent metadata, which is the friendlier pattern. Adopted `group-focus-within` to match.
  - Read `apps/web/src/lib/auth/client.ts:6-9` — `authClient` is the Better Auth client for the web app; `authClient.useSession()` returns `{ data, isPending, error, refetch }` with `data.user.id`. Already used in `dashboard-shell.tsx:442`, `layouts/dashboard.tsx:23`. Re-used here so the component can read the caller's userId and cross-reference against the `members` list to decide owner-only controls. No new auth primitive needed.
  - Read `packages/api/src/modules/asset-members/service.ts:131-166,223-239,290-298,348-352` to confirm every mutating endpoint (`invite`, `removeMember`, `revokeInvite`) already rejects non-owners at the service layer with `kind: 'forbidden'` / `reason: 'forbidden'` (Task 15 + Task 18). So the UI gate is purely cosmetic — server authz is the source of truth. The comment in collaborators-section.tsx:130-133 now documents this explicitly so a future reader doesn't assume the UI check is load-bearing.
  - Read `packages/api/src/modules/asset-members/index.ts:10-14` — the `GET /:id/members` handler calls `AssetMemberService.getRole(...)` first and returns 404 if the caller is not a member. So any caller who successfully reads `data.members` is guaranteed to appear in that list, making `members.find(m => m.userId === currentUserId)` a reliable way to infer the caller's role without adding a new endpoint or a separate "whoami" roundtrip.
  - Searched `apps/web/src/components/share/` for React Query options used elsewhere: `share-dialog.tsx:166-175` uses plain `useQuery`, same pattern adopted in collaborators-section.tsx:52-61. The project's global `queryClient.queryCache.onError` (api.ts:45-60) only fires for `useQuery` errors, NOT for ad-hoc mutation fetches. So each mutation handler MUST catch its own throws — the pre-fix `try { … } finally { … }` blocks had no `catch` clause, which means a raw fetch rejection (network failure, CORS error, gateway 502 with non-JSON body) would bubble as an unhandled promise rejection and show no toast. Review-prompt.md §"Error Handling & UX" / Common-Pattern #5 specifically flags this silent-swallow pattern.
  - `dangerouslySetInnerHTML` search: `rg 'dangerouslySetInnerHTML' apps/web/src/components/share` → zero. Every interpolation (`{m.name}`, `{m.email}`, `{inv.email}`, `` Share "{resourceName}" ``, link URLs via `{getShareUrl(link.token)}`) goes through React's default auto-escape. No HTML injection surface. Verified.
  - Grepped for `onKeyDown.*Enter` patterns in the pre-fix code: the Invite Input has `onKeyDown={(e) => { if (e.key === 'Enter' && !inviting) handleInvite(); }}`. Considered wrapping the invite row in a `<form onSubmit>` instead (would give Enter-to-submit "for free" + let me use `type="submit"` semantically) but rejected: the row is inside a Dialog and a Tabs panel, and Radix's Dialog content doesn't expect a form as an immediate child — nesting forms creates edge cases with submit bubbling into the Tabs. The explicit `onKeyDown` is small and works. Out of scope to rewrite.
  - Read the `isLoading` vs `data?.members` branching at collaborators-section.tsx:155-159: the pre-fix rendered the invite input unconditionally (even while the members query was loading). After the fix, the owner-gated branch renders ONLY when `isOwner` is truthy, which requires both `session.user.id` and the members list to have resolved. Until then the invite form is hidden. This is the "pessimistic gating" comment at line 130-133. For an owner viewing their own asset, the flash is imperceptible because both the session and members queries typically resolve in tens of ms; for a non-owner it correctly never appears.
  - Considered whether `data?.members` might legitimately lack the caller (e.g. admin impersonation). Searched for admin share-override flows: none exist. Admins open the app as themselves; they don't have magic access to other users' assets via this dialog. So `members.some((m) => m.userId === currentUserId && m.role === 'owner')` is complete — there is no lurking admin-override codepath that would want the invite form visible without an owner membership row.

Issues found and fixed:
  1. apps/web/src/components/share/collaborators-section.tsx:119-153 (pre-fix) — the invite input + role select + invite button rendered unconditionally. A viewer/editor opening the share dialog saw the form and got a 403 on submit. Fixed by gating the invite row behind `isOwner`. The server authz remains the source of truth; this is the UX layer.
  2. Same file, :80-95,97-114 (pre-fix) — the `handleRemove` and `handleRevokeInvite` buttons rendered unconditionally for every non-owner member / every pending invite. Fixed by gating both buttons behind `isOwner`. A viewer/editor no longer sees mute/delete controls they can't use.
  3. Same file, :80-114 (pre-fix) — all three handlers used `try { … } finally { … }` with no catch. A raw fetch rejection (network, CORS, non-JSON response) would bubble as an unhandled promise rejection with no user feedback (the global `queryClient.queryCache.onError` at api.ts:45 only fires for `useQuery`, not for one-shot mutation fetches). Added `catch (err) { toast.error(toToastErrorMessage(err, 'Failed to …')); }` to each — same fallback-message pattern the component already uses for Eden-returned `error.value`. Closes review-prompt.md Common-Pattern #5 (silent error swallowing).
  4. Same file, :122 (pre-fix) — the email Input had a `placeholder="Add people by email"` but no label or `aria-label`. Screen readers don't expose placeholder as an accessible name reliably (especially after the user starts typing, the placeholder disappears). Added `aria-label="Invitee email"`. review-prompt.md §"Accessibility" / "Every input has a label".
  5. Same file, :138 (pre-fix) — the Role `SelectTrigger` had no accessible name. The trigger shows `<SelectValue />` ("Editor" or "Viewer") but SR users who tab to the trigger hear only the current value, with no indication that the control's purpose is "invitee role". Added `aria-label="Invitee role"`.
  6. Same file, :189-192,222-225 (pre-fix) — remove/revoke buttons used `opacity-0 group-hover:opacity-100 focus-visible:opacity-100`. The `focus-visible` form technically reveals the button when a keyboard user tabs to it, but the project's convention (comment-card.tsx, thread-sidebar.tsx) uses `group-focus-within:opacity-100` on the same element so focus anywhere in the row reveals the controls. Extracted a single class constant `REVEAL_ON_HOVER_FOCUS` (lines 29-30) for the three call sites; swapped to `group-hover:opacity-100 group-focus-within:opacity-100` per review-prompt.md Common-Pattern #4 + §"Keyboard accessibility".
  7. Same file, all three mutation buttons (Invite / Remove / Revoke) had no `type` attribute. Fixed — added `type="button"` explicitly. Minor preventive fix: if any future refactor wraps this in a `<form>`, clicking any of these would submit the form; explicit `type="button"` closes that future footgun.
  8. Same file, :240 (pre-fix) — the "Only you can see this" hint rendered for any viewer of a solo-owner asset. A viewer/editor viewing such an asset would see "Only you can see this" which is factually wrong from their POV. Gated with `isOwner &&` so only the actual owner sees the invite-more-people nudge.
  9. apps/web/src/components/share/share-dialog.tsx:109 (pre-fix) — the DialogTrigger-wrapped "Share" button had no `type`. Since `DialogTrigger asChild` replaces the button with a Radix Slot, the HTML element is still `<button>` and the same risk applies. Added `type="button"`.
  10. apps/web/src/components/share/share-dialog.tsx:272 (pre-fix) — the "Expires" `<Label>` had no `htmlFor`. The Radix SelectTrigger is a `<button>` that participates in the label-click contract only if `htmlFor`/`id` match. Added `htmlFor="share-expiry"` on the Label and `id="share-expiry"` on the SelectTrigger, so clicking "Expires" focuses the trigger.
  11. apps/web/src/components/share/share-dialog.tsx:287,326,341 — the Create / Copy / Link-options buttons had no `type`. Added `type="button"` to all three. The Copy and Link-options buttons already have `aria-label` (copy + link options) and the Create button has a visible text label ("Create link"), so a11y coverage was already correct; this closes only the implicit-submit-type concern.

Issues noted (no fix needed):
  1. The pre-fix code used `focus-visible:opacity-100` on the button itself, which DOES reveal the button when a keyboard user tabs to it (button is in the tab order even at opacity 0). Swapped to `group-focus-within` per codebase convention; the original would have met minimum a11y, but the convention is better UX (whole row reveals, so neighboring metadata isn't hidden while a keyboard user operates the action). Noted here so a future reader knows the pre-fix was not blindly broken.
  2. The invite email Input's `onKeyDown` Enter handler is a one-off instead of wrapping the row in a `<form onSubmit>`. Considered switching to a form for free Enter-to-submit + semantic `type="submit"`. Rejected: the row is inside a Radix Dialog + Tabs; nested forms inside Dialog can cause submit-bubbling edge cases and the current code is clear and correct. Out of scope.
  3. The "People" tab is the default `defaultValue="people"` even for non-owners — meaning a viewer opening the share dialog sees the member list first, then can switch to "Public link" to see any public links. For non-owners this is fine: they just see who else has access, no write UI. If product wanted viewer-specific tabs, that would be a design change, not a review fix.
  4. The `getInitials()` helper at line 32 splits on `[\s@.]+`. For an email like `a@b.com`, it would produce `AB` ("a" + "b"). For a pasted display name with only one part ("Yash"), it produces `Y`. Edge case: a name that contains emoji or CJK characters — the current split is whitespace-separated, which handles CJK OK (no whitespace → single token → single initial). Good enough. No change.
  5. `setActiveShareLinkCount(links.length)` inside the queryFn at share-dialog.tsx:170 is a side-effect in a data-fetching callback. Noted but out of scope — predates this task and is used by the plan-cache module. A future refactor could move it to a `useEffect` watching `data`, but it's working code.
  6. No ownership check is made on `fetchShareLinks()` in share-dialog.tsx:169 — the server is authoritative; any public-link endpoint already scopes to the caller's owned assets/collections. Confirmed by convention, not re-verified here.
  7. The Revoke-link DropdownMenuItem uses `variant="destructive"` from shadcn DropdownMenu. The destructive variant provides red text + hover styling; no explicit `aria-label` needed because the item text "Revoke link" is sufficient.
  8. Could have wrapped the Input + Select + Button trio in a `<form onSubmit>` to get native submit semantics. Intentionally not done — see note (2) above.
  9. The `DialogContent className="grid-cols-[minmax(0,1fr)] sm:max-w-lg"` uses an arbitrary value `grid-cols-[minmax(0,1fr)]`. This is a Tailwind arbitrary value for a grid template; there's no theme token that expresses `minmax(0,1fr)` directly, so the arbitrary is justified per review-prompt.md §"Tailwind v4" ("text-[10px] is acceptable if no theme token exists"). Same pass for `text-[10px]` on avatar fallback + pending-invite subtitle — no `text-2xs` or similar in the theme. Kept.
  10. The `text-xs capitalize` on a non-owner member's role displays "editor" / "viewer" — it relies solely on text, not color, so review-prompt.md §"No color-only indicators" is satisfied.

Library improvements made:
  - Re-used `authClient.useSession()` (Better Auth) instead of threading `userId` through props or reading a custom context. Already the established pattern in this codebase (dashboard-shell.tsx, layouts/dashboard.tsx). Avoided introducing a parallel "current user" context.
  - Re-used `toToastErrorMessage(err, fallback)` for the new catch blocks — the helper already handles `Error`, `string`, `{ message }`, `{ error }`, URL scrubbing, length clipping (api.ts:10-33). No need to duplicate that logic per handler.
  - Extracted `REVEAL_ON_HOVER_FOCUS` as a single string constant at module scope. This is a local convenience (three call sites in one file) — didn't promote it to a shared `utils.ts` since only this file uses the exact combination and the comment-card / thread-sidebar variants use `md:` prefixes that this row doesn't need.
  - Did NOT introduce a new "useCurrentMember" hook. The `members.some(...)` line is 2 lines and only used in one place; a hook would be premature abstraction per review-prompt.md §"Don't add features… beyond what the task requires".

Checklist verification (per review-prompt.md Frontend + Library Leverage + Common Patterns):
  - No redundant state — `isOwner` is derived from `members` + `currentUserId`, not stored. `inviting` / `removingUserId` / `revokingInviteId` are per-button in-flight flags, can't be derived.
  - Proper cleanup in useEffects — no useEffects in this task.
  - Unnecessary re-renders / useMemo — nothing to memoize. `isOwner` is O(members.length), typically < 20 rows, cheap. The handlers are closures over state that would need useCallback ONLY if passed to `React.memo`-wrapped children; they're not, so no memoization needed.
  - Stable keys — `m.userId` for members, `inv.id` for pending invites. Both are DB-generated IDs. Verified.
  - Guard browser-only APIs — no `window`/`document`/`navigator` access in the new collaborators-section. `share-dialog.tsx` uses `navigator.clipboard.writeText` inside an event handler (user-action scope, safe — runs post-mount) + `window.location.origin` inside `getShareUrl` which is guarded with `typeof window !== 'undefined'` (share-dialog.tsx:85). No new SSR risk.
  - No dangerouslySetInnerHTML — zero in both files. React auto-escape on all interpolations.
  - Remove dead code — `Trash2` import is still used twice; `Mail` still used. `Check, Copy, Link2, Share2` still used. No orphans.
  - No `any` — zero. `setRole(v as InviteRole)` cast is pre-existing and narrow (Radix gives a string from `onValueChange`, and the only two `<SelectItem>` values are `'editor'` and `'viewer'`). Could be tightened with a type guard but it's a 1-liner with no bug surface; out of scope.
  - No unjustified `!` — zero in either file.
  - Exhaustive switches — no switch in either file.
  - Tailwind — `size-*` used (lines 170, 192, 197, 243, 253). No separate `w-8 h-8`. `inset-*` not applicable. Arbitrary values (`text-[10px]`, `grid-cols-[minmax(0,1fr)]`) justified in notes (9-10) above.
  - ARIA / semantic HTML — Every icon-only button has `aria-label`. Buttons all carry `type="button"`. Email input has `aria-label`. Role select has `aria-label`. Expires select associated to its label via id+htmlFor. No role="radiogroup" needed (Radix handles Select internals). Verified.
  - Keyboard accessibility — all `opacity-0 group-hover:opacity-100` now carry `group-focus-within:opacity-100`. Focus styles inherit from the Button component's focus-visible ring. Verified.
  - No color-only indicators — role shown as capitalized text ("Editor" / "Viewer" / "Owner"), not a color pill. Pending invite labeled "Pending invite · {role}", not just a mail icon.
  - No console.log — zero new. `rg 'console\.' apps/web/src/components/share` → zero hits in both files.
  - Proper error handling — every catch calls `toast.error(...)`. Every Eden `error` branch calls `toast.error(...)`. No empty catch. No "handled by global handler" comment without a global handler.
  - Handle loading / error / empty / populated — loading: `isLoading` → spinner. Error: queryCache.onError at api.ts:45 shows a retry toast (the queryFn throws, which React Query catches and routes through the cache onError). Empty: the `isOwner && members.length === 1 && pendingInvites.length === 0` branch shows the invite nudge. Populated: member + pending rows render. Verified all four states.
  - No secrets in client code — none added. `authClient` only holds the server URL (public) and session cookie (client-managed).
  - Common Pattern #1 (full-error logging) — no `console.error` added; the one we'd have needed is handled by `toToastErrorMessage` which scrubs URLs and clips length.
  - Common Pattern #2 (missing timeouts) — no new fetches added; share-dialog.tsx's fetchShareLinks + `api.api.*.delete()` are inherited from pre-existing code. The Eden treaty client handles network defaults; timeouts on the server side (Task 17 pattern) are separately owned.
  - Common Pattern #3 (rate limiter) — no new routes added. /api/assets/* is already in the crud bucket (Task 13/15).
  - Common Pattern #4 (keyboard-invisible interactive elements) — FIXED on the three reveal-on-hover buttons.
  - Common Pattern #5 (silent error swallowing) — FIXED on the three async handlers in collaborators-section.
  - Common Pattern #6 (duplicated logic) — extracted `REVEAL_ON_HOVER_FOCUS` locally; avoided over-extracting into a shared utility for one file.
  - Common Pattern #7 (exhaustive switch) — N/A.
  - Common Pattern #8 (useEffect async) — no useEffects touched.
  - SSR — `'use client'` directive present. `authClient.useSession()` is a client-only hook. `navigator.clipboard` only runs in the copy handler. Safe.
  - pnpm check-types: clean. 8/8 tasks green; 7 cached, @milkpod/web cache-miss (source change) and compiled successfully. No regressions elsewhere.


---

## Task 20: Insert notifications inside the triggering transaction

Approach: pure verification task. The four files in scope were added entirely on this branch (all four register as `new file` in `git diff main`). Walked every INSERT-into-notifications site, cross-referenced its triggering mutation, and confirmed both land in the same `db().transaction(async (tx) => { ... })` block. No code change produced — Tasks 16 (invite flow) and 18 (removeMember) already landed the tx pattern; signup-hooks shipped with the tx pattern from day one. The audit is the deliverable.

Verification before touching any code:
  - `rg 'insert\(notifications\)|NotificationService\.record'` across the branch → exactly THREE insert sites: asset-members/service.ts:184 (invite, existingUser branch), asset-members/service.ts:334 (removeMember), signup-hooks.ts:92 (claimPendingInvitesOnSignup). Every other notifications touch is an UPDATE (markRead/markAllRead/markReadByResource/server-mutators) — no new-row side effects, so tx-with-trigger is moot for those.
  - Comments module (`packages/api/src/modules/comments/`): `rg -i 'notification'` → zero hits. Comments never insert notifications in v1. The plan's "Comment create + notification insert must share a transaction" is forward-looking (applies when/if a mentions feature lands), not an existing code path. Same for moments — `rg -i 'notification' packages/api/src/modules/moments/` → zero hits. So the comments/moments files listed in the task scope have nothing to fix today; they are listed defensively in case a mention/reply notification was inserted inline.
  - Read `packages/api/src/modules/notifications/service.ts:49-68` (`NotificationService.record`). Signature: `record(tx: DbTx, input: RecordInput): Promise<string>`. The `tx` is the REQUIRED first arg — the service has no other `insertNotification` helper that would bypass tx, so every caller must either be in an open tx or pass `db()` explicitly. Verified by grepping `NotificationService.record(` across the whole repo — only the two asset-members call sites, both pass the tx handle from the enclosing `db().transaction(async (tx) => ...)`.
  - Read `packages/auth/package.json` and `packages/api/package.json` — @milkpod/api depends on @milkpod/auth (api imports sendInviteEmail from auth), but @milkpod/auth does NOT depend on @milkpod/api. Confirmed the direction: signup-hooks CANNOT import NotificationService without creating a cycle. The direct `tx.insert(notifications).values({...})` at signup-hooks.ts:92-99 is therefore intentional — it mirrors NotificationService.record's shape rather than calling through it. Documented this observation under Library Improvements.
  - Read `node_modules/.pnpm/drizzle-orm@0.44.2/node_modules/drizzle-orm/pg-core/db.d.ts` for `.transaction()` signature to confirm rollback semantics: the callback's thrown error aborts the tx and re-throws to the caller. Drizzle does NOT auto-commit on partial success — the tx commits only when the callback resolves without throwing. So if the notification insert throws (FK violation, connection loss, etc.), the asset_members INSERT also rolls back. This is the "no phantom notification" guarantee verbatim.
  - Read the `for await` loop at signup-hooks.ts:50-100: each pending invite gets its OWN `db().transaction(async (tx) => { ... })`. Per-invite tx isolation means a failure on invite #3 doesn't undo the successful commits of invites #1, #2. Good pattern — each invite is atomically claim-or-skip.

Site-by-site proof that notification + trigger share a tx:

  Site 1 — asset-members/service.ts:172-192 (invite, existingUser branch):
    ```ts
    const inserted = await db().transaction(async (tx) => {
      const [row] = await tx
        .insert(assetMembers)
        .values({ assetId, userId: existingUser.id, role, invitedBy })
        .onConflictDoNothing()
        .returning({ userId: assetMembers.userId });
      if (!row) return null;                                // already a member → no notification
      await NotificationService.record(tx, {                // passes the SAME tx handle
        type: 'asset.member.added',
        recipientId: existingUser.id,
        actorId: invitedBy,
        assetId,
        body: { role },
      });
      return row;
    });
    ```
    → Trigger (INSERT assetMembers) + notification (INSERT notifications) commit atomically. If the notification insert throws, the assetMembers insert rolls back. If assetMembers conflicts (`onConflictDoNothing` returns no row), the notification is correctly skipped — no duplicate bell entry for a re-invited existing member. The post-commit poke + email send happen OUTSIDE the tx (lines 195-202), which is correct per Task 10.

  Site 2 — asset-members/service.ts:308-342 (removeMember):
    ```ts
    const result = await db().transaction(
      async (tx): Promise<RemoveMemberResult> => {
        const [target] = await tx
          .select({ role: assetMembers.role })
          .from(assetMembers)
          .where(and(eq(assetMembers.assetId, assetId), eq(assetMembers.userId, userIdToRemove)))
          .for('update');                                   // Task 18 row lock
        if (!target) return { removed: false, reason: 'not_found' };
        if (target.role === 'owner') return { removed: false, reason: 'owner_protected' };
        await tx.delete(assetMembers).where(and(...));      // trigger: DELETE
        await NotificationService.record(tx, {              // SAME tx — notification
          type: 'asset.member.removed',
          recipientId: userIdToRemove,
          actorId: removedBy,
          assetId,
        });
        return { removed: true };
      },
    );
    ```
    → SELECT-FOR-UPDATE + DELETE + notification INSERT in one tx. If the notification insert throws, the delete rolls back — the removed user stays a member, which is correct (if we can't tell them they were kicked, don't kick them). Post-commit poke at lines 352-359 runs AFTER `await db().transaction(...)` resolves, so clients never pull pre-commit state.

  Site 3 — auth/src/signup-hooks.ts:56-100 (claimPendingInvitesOnSignup):
    ```ts
    await db().transaction(async (tx) => {
      const [claimed] = await tx
        .delete(assetInvites)
        .where(and(eq(assetInvites.id, invite.id), or(isNull(expiresAt), gt(expiresAt, new Date()))))
        .returning({ id: assetInvites.id });               // trigger: DELETE invite
      if (!claimed) return;
      const [inserted] = await tx
        .insert(assetMembers)
        .values({ assetId, userId: newUserId, role: invite.role, invitedBy: invite.invitedBy })
        .onConflictDoNothing()
        .returning({ userId: assetMembers.userId });        // trigger: INSERT member
      if (!inserted) return;                                // concurrent direct-add → skip notification
      await tx.insert(notifications).values({               // SAME tx — notification
        recipientId: newUserId,
        type: 'asset.member.added',
        actorId: invite.invitedBy,
        resourceType: 'asset',
        resourceId: invite.assetId,
        body: { role: invite.role },
      });
    });
    ```
    → Invite DELETE + member INSERT + notification INSERT share one tx. If any step throws, all three roll back. If assetMembers conflicts (user was concurrently added by the invite flow's existingUser branch between these two hooks — edge case, but possible), notification is skipped. No poke is emitted (the new user's client hasn't initialized yet — its first pull picks everything up naturally), per the top-of-file comment.

Issues found and fixed:
  (none — all three notification-insert sites were already inside their triggering transaction before Task 20 started. Tasks 16 and 18 closed the last two tx gaps; Task 20 is the post-facto verification that ratifies those fixes plus the signup-hooks pattern that shipped correctly from the start.)

Issues noted (no fix needed):
  1. packages/auth/src/signup-hooks.ts:92-99 — the direct `tx.insert(notifications).values({...})` duplicates the shape encoded in `NotificationService.record` (recipientId, type, actorId, resourceType='asset', resourceId, body). If NotificationService.record ever tightens its input shape (e.g. adds a denormalized column, validates body against type discriminant), this mirror will drift. BUT the fix — `signup-hooks → NotificationService.record` — creates a package cycle (@milkpod/auth → @milkpod/api → @milkpod/auth), which is worse than the duplication. Three alternatives considered and rejected:
     (a) Move `NotificationService.record` into `@milkpod/db` as a helper — forces the db package to know about notification shapes and types, violating layering.
     (b) Move `claimPendingInvitesOnSignup` into `@milkpod/api` and have `@milkpod/auth` call it via a function-injection hook — better-auth's `databaseHooks.user.create.after` is registered at auth() construction in packages/auth/src/index.ts, so the hook body must live in @milkpod/auth OR the auth plugin definition moves to @milkpod/api. Non-trivial refactor, out of scope for a verify task.
     (c) Accept the duplication, document the mirror. ← chosen. The only-two-fields-that-could-drift (resourceType, body shape) are already type-narrowed by `NotificationBody` in `@milkpod/db/schema/notifications.ts`, which BOTH call sites import transitively. A future migration that adds a new notification type with a stricter body shape would force both sites to update (via the TypeScript `NotificationBody` union). So the duplication is shape-safe against additive changes even without the service call.
  2. packages/auth/src/signup-hooks.ts:50-100 — the `for (const invite of pending)` loop does one tx per invite. If invite #3's tx throws mid-loop, the outer `try { ... } catch (err) { ... }` at line 102-111 catches it and EXITS the loop — invites #4, #5, etc. never get processed (silently skipped). Each completed invite commits atomically (correct), but the user may end up with a partial set of memberships. Task 20 scope is "notification inside triggering tx", which is satisfied — the phantom-notification class of bug doesn't apply. Whether to isolate per-invite failures is a separate robustness question; a future hardening pass could wrap each tx in its own try/catch so invite #3's failure logs but doesn't abandon #4/#5. Not changed in Task 20 to keep the scope narrow and because the partial-success window is tiny (realistically one user has <5 pending invites, and transient DB errors usually hit all or none).
  3. packages/api/src/modules/notifications/service.ts:49-68 — `NotificationService.record` returns `Promise<string>` (the inserted id), but BOTH call sites ignore the return (asset-members/service.ts:184, :334). Could be simplified to `Promise<void>` plus dropping the `.returning({ id: notifications.id })` clause (saves a trivial round-trip of bytes). Left as-is to keep the API open for a future caller who wants the id — e.g. a "send email with deep link to this exact notification" flow. Micro-optimization not worth taking.
  4. packages/api/src/modules/comments/service.ts and packages/api/src/modules/moments/service.ts — no notification insert exists (v1 has no mentions, replies, or comment-resolved events). The plan lists these as scope-in files for Task 20 forward-lookingly. Any future mention/reply notification MUST be added inside the `CommentService.insertMany` / `MomentService.insertMany` path as a `db().transaction(...)` wrapping both the content INSERT and the notification INSERT, with the poke+email side effects moved outside. Documented here so the convention is captured for the future author.
  5. packages/api/src/modules/replicache/server-mutators.ts:125-195 — the commentCreate/commentUpdate/commentDelete and momentCreate/momentUpdate/momentDelete mutators run inside the push tx (see push.ts for the outer `db().transaction()`). They do NOT insert notifications today. If a comment-create mutator is extended to emit a mention notification, the existing push-tx already guarantees tx containment — no code change needed at the server-mutator level. Verified push.ts wraps the mutator dispatch in a transaction at the outer scope.
  6. packages/auth/src/signup-hooks.ts:106-110 — the error log includes `email: normalized`. Email is PII per review-prompt.md §"No sensitive data in logs". However, this is diagnostic context for a failed signup-invite claim, mirrored by the same pattern in other server logs (invite-email.ts:58 logs `to` for send failures — pre-existing pattern). Masking would require project-wide convention alignment. Out of scope for Task 20. A future log-hygiene pass could hash or truncate the email; not blocking merge.

Library improvements made:
  - None. The existing code already uses the intended library primitive: Drizzle's `db().transaction(async (tx) => ...)` for multi-statement atomicity, with the `tx` handle threaded into `NotificationService.record(tx, ...)`. No hand-rolled savepoint, no manual BEGIN/COMMIT, no advisory-lock helper. Task 18 previously replaced the earlier check-then-tx pattern with Drizzle's `.for('update')` row lock inside the removeMember tx, which is the natural composition of `transaction` + `for('update')` that the library provides. The signup-hooks direct `tx.insert(notifications)` is the only duplication, and the cross-package cycle blocks consolidating it (see Issues Noted #1).

Checklist verification (per review-prompt.md Backend + Library Leverage + Common Patterns):
  - No N+1 queries — verified. Each of the three notification-insert sites is exactly one statement inside its tx. The signup-hooks loop issues N transactions for N pending invites, which is correct (each is a distinct trigger with a distinct recipient); batching into one tx would make a single failure abort all claims, which is the worse tradeoff.
  - Appropriate database indexes — verified at Task 4. `notification_recipient_created_idx`, `notification_recipient_unread_idx` (partial), `notification_recipient_row_version_idx` all live on the recipient-keyed queries that `listForUser` / `markRead` / Replicache pull perform. Insert path uses the primary key (id via $defaultFn).
  - Pagination on list endpoints — N/A (Task 21 scope).
  - Transactions for atomic operations — VERIFIED for all three notification-insert sites. Every INSERT INTO notifications is bundled with its triggering mutation in one `db().transaction()` scope. No phantom-notification class bug.
  - Use parameterized queries or ORM methods — all three call sites use Drizzle's `.insert(table).values({...})` builder — no raw SQL, no string concatenation. `NotificationService.record` + `signup-hooks` direct insert both.
  - Appropriate data types in schemas — `notifications.body` is `jsonb().$type<NotificationBody>()`, `notifications.type` is `text().$type<NotificationType>()` — both narrowed at the TS layer. `rowVersion` is `integer`, `readAt` is `timestamp`. Verified at Task 5.
  - Input validation on every route — N/A (this task audits service-layer tx structure; router-layer validation covered by Tasks 12, 15, 16).
  - Auth and authorization checks on every endpoint — the two service-layer insert paths (invite, removeMember) each enforce owner-role callerRole at line 157, 294 BEFORE opening the tx. The signup-hooks path is invoked from better-auth's `databaseHooks.user.create.after` — authority is the signup mechanism itself (the user just proved email ownership via OTP). Verified.
  - Rate limiting on sensitive endpoints — Task 13/15/16 already registered /api/asset-members in the crud bucket. Signup-hooks is invoked on signup (ratelimited by /api/auth/sign-up/email-otp in the auth bucket).
  - No sensitive data in logs — all three notification-adjacent catch blocks use the safe `err instanceof Error ? err.message : String(err)` form: asset-members/service.ts:106, 282, 357; notifications/service.ts:80; signup-hooks.ts:109. Email is logged in signup-hooks error log — noted as Issues Noted #6, not fixed in Task 20.
  - No secrets in error responses — N/A (Task 20 touches internal service code; router-layer responses verified in Task 15).
  - Proper HTTP status codes — N/A (router already uses the discriminated-union `result.kind` / `result.reason` mapping; Task 15/18 verified).
  - Webhook endpoints — N/A.
  - Timeouts on external API calls — `NotificationService.poke` calls `emitReplicachePokes` which is an in-process event-bus emit (no network). `sendInviteEmail` (called from asset-members, NOT from notification path) has its 30s `Promise.race` timeout from Task 17.
  - Idempotency — the invite flow's `onConflictDoNothing` on (assetId, userId) PK prevents duplicate member rows. The signup-hooks `onConflictDoNothing` on the same PK prevents duplicate member rows on concurrent signup hooks. Re-running `claimPendingInvitesOnSignup` on the same user is a no-op (the invite DELETE is gone after the first run). All three notification inserts are not idempotent by row (no natural unique constraint prevents two `asset.member.added` notifications for the same (recipient, actor, asset)), but callers never re-invoke record for the same logical event: invite's tx body early-returns when the member row already existed; removeMember's row lock prevents duplicate delete; signup-hooks' DELETE ... RETURNING ensures the invite is claimed exactly once. So duplicates can't happen at the service boundary.
  - Connection pooling — every tx uses `db().transaction()`, which checks out one connection for the duration. No connection leak on thrown path (Drizzle's tx wrapper rolls back and releases on error).
  - Environment variables — unchanged.
  - Reuse existing utilities and middleware — `NotificationService.record`, `db().transaction`, `onConflictDoNothing`, `.for('update')`, `emitReplicachePokes` all library-native primitives reused. The signup-hooks direct `tx.insert(notifications)` is the only divergence — documented cross-cycle reason above.
  - Exhaustive switch statements — `NotificationService.serialize` at line 128-148 switches on `row.type` (NotificationType union). Three cases + default → null. `default` returns null rather than throwing, which is the intentional "skip rows from a newer server" pattern documented at line 105-106. If the union grew without updating the switch, the new variant would return null instead of the correct branch, but since serialize receives only v1 `asset.member.*` rows today, this is safe. When adding a fourth notification type, MUST update this switch. A `default: const _exhaustive: never = row.type; return null;` with a runtime fallback would be stricter and forward-compatible — noted as a future hardening, not fixed (serialize is not in Task 20's file list).
  - No magic numbers — N/A. `INVITE_EXPIRY_MS = 14 * 24 * 60 * 60 * 1000` has a full comment at asset-members/service.ts:17-25 explaining the 14-day choice.
  - Proper error handling — verified. Every catch uses the safe error-message form. No empty catches. No silent swallows.
  - Database migrations are safe — migration 0037 (notification row_version trigger) covered at Task 1. Migration 0038+ (notification table + index) covered at Tasks 4, 5.
  - TypeScript: NotificationService.record's `tx: DbTx` is `any` (eslint-disabled) at line 12 — same pattern as server-mutators.ts. Known pre-existing any, tracked as a tech-debt item in serviceworker-style helpers. Not a Task 20 regression. `NotificationType` and `NotificationBody` are properly discriminated. Signup-hooks' `invite.role` is `'editor' | 'viewer'` via $type<>() narrowing.
  - Common Patterns #1 (full-error logging) — verified clean across all three sites.
  - Common Patterns #2 (missing timeouts) — N/A (no external API in the tx path).
  - Common Patterns #3 (rate limiter) — N/A (no new routes).
  - Common Patterns #4 (keyboard-invisible) — N/A (backend only).
  - Common Patterns #5 (silent error swallowing) — the signup-hooks outer catch at line 102-111 logs but doesn't re-throw. Intentional: signup itself must not fail because an invite claim hit a transient error. Users get a signed-up account; invites stay in pending for manual re-processing. Documented at line 103-105.
  - Common Patterns #6 (duplicated logic) — the signup-hooks direct `tx.insert(notifications)` duplicates NotificationService.record's shape. Blocked by package cycle. Documented.
  - Common Patterns #7 (exhaustive switches) — see note above on `NotificationService.serialize`. Not a Task 20 file.
  - Common Patterns #8 (useEffect async) — N/A.
  - Library Leverage / Drizzle — `db().transaction()`, `.onConflictDoNothing()`, `.for('update')`, `.returning()` are all native Drizzle primitives used correctly in the three sites. No hand-rolled BEGIN/COMMIT/SAVEPOINT SQL anywhere.
  - pnpm check-types: clean. 8/8 tasks green; all cached (no code change produced by Task 20 — pure verification).
