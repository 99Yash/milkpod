# UI Polish Progress

## Task 1 — Global Text & Font Baseline

### Changes made
- `globals.css`: Added `text-wrap: balance` to `.heading-xl`, `.heading-lg`, `.heading-md` utility classes
- `daily-quota.tsx`: Added `tabular-nums` to the remaining-words counter `<span>`
- `word-limit-picker.tsx`: Wrapped trigger label in `<span className="tabular-nums">`
- `pricing-grid.tsx`: Added `tabular-nums` to the price `<span>`, added `text-balance` to plan `<h3>` headings
- `timestamp-link.tsx`: Added `tabular-nums` to both the non-clickable `<span>` and the clickable `<button>`

### Already applied (no changes needed)
- `layout.tsx`: `antialiased` already on `<body>` (pattern 4)
- `globals.css`: `.text-balance` and `.text-pretty` utility classes already defined

### No changes needed
- `chapter-progress-bar.tsx`: No visible numeric text rendered (timestamps only in `aria-label`)

## Task 2 — Card Shadows & Image Outlines

### Changes made
- `globals.css`: Added `--shadow-card` and `--shadow-card-hover` CSS custom properties in both `:root` (light) and `.dark` scopes. Added `.card-elevated` (base multi-layer shadow), `.card-hover:hover` (elevated hover shadow), and `.image-inset-outline` (1px inset outline with dark mode variant) utility classes
- `dashboard-panel.tsx`: Replaced `border-border/60 shadow-none` with `border-0 shadow-none card-elevated` — removes solid border, replaces with multi-layer shadow (affects asset-card and collection-card)
- `asset-card.tsx`: Replaced `hover:shadow-md` with `card-hover` for consistent shadow hover. Added `image-inset-outline` to thumbnail `<img>` for subtle depth on variable backgrounds
- `collection-card.tsx`: Replaced `hover:shadow-md` with `card-hover` for consistent shadow hover
- `moment-card.tsx`: Replaced `border border-border/60` and `hover:border-border` with `card-elevated card-hover`. Changed `transition-colors` to `transition-shadow`
- `comment-card.tsx`: Same treatment as moment-card — replaced border with `card-elevated card-hover`, `transition-shadow`
- `video-moment-dialog.tsx`: Added `image-inset-outline` to iframe for subtle depth on video preview

### No changes needed
- `ai-avatar.tsx`: Already well-styled with gradient background, subtle `border-border/30`, and `shadow-sm` — not a content image, so pattern 11 doesn't apply. Existing styling is appropriate for an icon avatar

### Decisions
- Used CSS custom properties (`--shadow-card`, `--shadow-card-hover`) with separate light/dark values to avoid verbose inline Tailwind arbitrary values
- Dark mode shadows use white ring border (`rgba(255,255,255,0.08)`) with stronger dark drop shadows to maintain depth perception
- Kept `shadow-none` on DashboardPanel to override Card's default `shadow-sm`, then `card-elevated` re-applies the multi-layer shadow

## Task 3 — Concentric Border Radii

### Changes made
- `pricing-grid.tsx`: Changed interval toggle inner buttons from `rounded-md` (8px) to `rounded-sm` (6px). Outer container is `rounded-lg` (10px) with `p-1` (4px), so concentric formula gives 10-4=6px = `rounded-sm`

### Already applied (no changes needed)
- `asset-card.tsx`: Card (`rounded-xl`) > thumbnail wrapper (`rounded-t-xl`) with 0 padding between them — matching radii is correct when padding=0
- `collection-card.tsx`: No nested containers with visible rounded borders — DashboardPanelContent has no border-radius
- `moment-card.tsx`: Outer `rounded-lg p-4` — inner action buttons are small and far from edges (padding 16px > radius 10px), no visual pinch at corners
- `chat-panel.tsx`: Form uses `overflow-hidden rounded-2xl` which clips children to parent radius. Textarea is `border-0` (invisible). Send button is small/standalone, not adjacent to form corners
- `card.tsx` (base): Inner content areas (CardContent, CardHeader) have no visible border-radius — no nested radius issue
- `dialog.tsx`: DialogContent `rounded-lg p-6` — inner elements (header, footer) have no visible rounded borders

### Decisions
- Only the pricing interval toggle had a true concentric radius issue — two visually stacked rounded containers (toggle background + active button highlight) where both borders/shadows are visible at corners simultaneously
- Other files either have 0 padding between nested radii (correct to match), use `overflow-hidden` (correct — clips children), or have inner elements too small/distant from outer corners to create visible pinch

## Task 4 — Enter/Exit Animations & Staggering

### Changes made
- `globals.css`: Added `@keyframes enter-up` (opacity 0→1, blur 4px→0, translateY 8px→0, 500ms ease-out) and `@keyframes exit-up` (opacity 1→0, blur 0→4px, translateY 0→-8px, 350ms). Added `.animate-enter` and `.animate-exit` utility classes. Added `prefers-reduced-motion` override to disable both animations.
- `asset-list.tsx`: Wrapped each `AssetCard` in `<div className="animate-enter">` with staggered `animationDelay` (60ms/item, capped at 8 = 480ms max). Cards cascade in on load/filter change.
- `collection-list.tsx`: Same stagger — wrapped each `CollectionCard` with `animate-enter` + 60ms stagger capped at 8.
- `moments-tab.tsx`: Same stagger — wrapped each `MomentCard` with `animate-enter` + 60ms stagger capped at 8.
- `tool-result.tsx`: Replaced container-level `animate-in fade-in-0 duration-300` on source pills with per-pill `animate-enter` + 40ms stagger. Each timestamp pill cascades in individually. The "+N more" label animates after the last pill.
- `thread-sidebar.tsx`: Wrapped each `ThreadItem` in `animate-enter` + 40ms stagger (capped at 10). Thread items cascade in when the desktop sidebar opens.

### Already applied (no changes needed)
- `message.tsx`: Already has `animate-in fade-in-0 slide-in-from-bottom-1 duration-300` on reasoning parts and initial thinking state, plus `@formkit/auto-animate` on the parts container. Adding blur to streaming content would feel sluggish — the existing fast fade+slide is appropriate for real-time streaming.
- `shimmer-text.tsx`: Dedicated shimmer gradient animation for loading states — a loading indicator, not an enter/exit animation.
- `dashboard-content.tsx`: Thin composition wrapper that passes children to `DashboardTabsClient`. No direct elements to animate.

### Decisions
- Used standalone CSS `@keyframes` + utility classes rather than extending tw-animate-css, since tw-animate-css's `animate-in` doesn't support blur
- Entrance: 500ms (snappier than pattern's 800ms), exit: 350ms (shorter per pattern 8)
- Stagger via inline `animationDelay` style, avoiding TypeScript issues with CSS custom properties
- Capped stagger at 8 items (grids) / 10 items (thread list) to prevent long delays on large lists
- Did not add exit animations to conditionally-rendered components (sidebar, dismissed cards) — would require AnimatePresence or similar for proper exit

## Task 5 — Interruptible Animations & Contextual Icons

### Changes made
- `sidebar-toggle.tsx`: Added `transition-transform duration-200` so the `rotate-180` on collapse/expand animates smoothly instead of snapping (pattern 6)
- `model-picker.tsx`: Added `transition-transform duration-200` and conditional `rotate-180` to the ChevronDown icon so it rotates smoothly when the popover opens/closes (pattern 6)
- `chat-panel.tsx`: Changed form `transition-shadow` to `transition-[box-shadow,border-color] duration-200` so the `focus-within:border-border` transition is smooth, not instant (pattern 6)
- `search-filter-bar.tsx`: Changed the X clear button from conditional render (`{localQuery && ...}`) to always-rendered with `transition-[opacity,transform] duration-150`. When hidden: `opacity-0 scale-75 pointer-events-none tabIndex=-1`. When visible: `opacity-100 scale-100`. Button now fades+scales in/out smoothly (pattern 3)
- `share-dialog.tsx`: Added `animate-in zoom-in-50 duration-150` to the Check icon that appears after copying a share link. The confirmation icon now scales in smoothly instead of popping (pattern 3)

### Already applied (no changes needed)
- `asset-tab-bar.tsx`: Tab switching uses `NavTabs`/`NavTabIndicator`/`navTabVariants` component system with link-based navigation. The indicator and tab variants handle transitions internally — no interactive keyframe animations to fix
- `asset-card.tsx`: Card already has `transition` covering transform and shadow for hover states (pattern 6). Action icons (FolderPlus, Retry) are always visible for their respective states, not contextually appearing/disappearing on hover
- `moment-card.tsx`: Already has `transition-shadow` for card hover. Action buttons (Save, Dismiss) are always visible with `transition-colors` on hover states. No contextually appearing/disappearing icons

### Decisions
- For the search X button, chose always-render + CSS opacity/scale transition over conditional render + keyframe animation. CSS transitions are interruptible (pattern 6) — if the user types and deletes quickly, the button smoothly reverses mid-animation
- For the share dialog Check icon, used `animate-in zoom-in-50` (keyframe) rather than a CSS transition because it's a one-shot feedback animation, not a reversible interactive state (appropriate per pattern 6's distinction)
- Did not add hover-reveal behavior to asset-card or moment-card action icons — that would be a UX change beyond the scope of ensuring existing animations are interruptible

## Task 6 — Optical Alignment Sweep

### Changes made
- `chat-panel.tsx`: Changed suggestion buttons (Sparkles icon + text) from symmetric `px-3.5` to `pl-3 pr-3.5` — tighter padding on the icon side (left) per pattern 9
- `chat-panel.tsx`: Added `translate-x-px` to `SendHorizonal` icon in the send button — right-pointing arrows appear left-shifted in square containers; 1px nudge optically centers it
- `moment-preset-switcher.tsx`: Changed preset buttons (icon + label) from symmetric `px-3` to `pl-2.5 pr-3` — tighter padding on the icon side (left)
- `url-input-form.tsx`: Changed mode toggle buttons (Link/Upload icon + text) from symmetric `px-2.5` to `pl-2 pr-2.5` — tighter padding on the icon side (left)

### Already applied (no changes needed)
- `pricing-grid.tsx`: Feature list Check icons already have `mt-0.5` optical offset to align with text baseline. CTA buttons ("Current plan", "Upgrade", etc.) are text-only — no icon+text alignment needed
- `timestamp-link.tsx`: No icon+text pairs — renders timestamp text only (children prop). No optical alignment issue
- `transcript-toolbar.tsx`: All buttons are icon-only (Search, ChevronUp/Down, X, List, BookOpen) with uniform `p-1`/`p-1.5` padding. No text+icon pairing to align
- `button.tsx` (base): Already has `has-[>svg]:px-*` size variants that symmetrically reduce padding when icons are present. Optical alignment (asymmetric reduction) is context-dependent on icon position, so applied at usage sites rather than the base component

### Decisions
- Applied asymmetric padding (2px less on icon side) only to buttons where the icon is consistently on the left side and the text is on the right. This matches pattern 9's recommendation of ~3-4px less padding on the icon side, scaled down for smaller button sizes
- For the SendHorizonal arrow icon, used `translate-x-px` (1px) rather than padding adjustment since it's an icon-only button — the goal is to shift the visual centroid of the right-pointing arrow to the geometric center of the container
- Did not modify `button.tsx` base component — asymmetric padding decisions depend on icon placement (left vs right), which the base component can't determine from `has-[>svg]` alone
