# Moments Feature Progress

## Task 1: Add moments DB schema and migration
- **Status**: Complete
- **Files changed**:
  - `packages/db/src/schema/moments.ts` (new) — `asset_moment` and `asset_moment_feedback` tables with enums, indexes, FKs
  - `packages/db/src/migrations/0014_omniscient_cassandra_nova.sql` (generated) — migration SQL
  - `packages/db/src/migrations/meta/0014_snapshot.json` (generated)
- **Outcome**: Schema created with `moment_preset` enum (default|hook|insight|quote|actionable|story), `moment_source` enum (hybrid|llm|qa), `asset_moment` table (15 columns, 2 indexes, 2 FKs), `asset_moment_feedback` table (6 columns, unique constraint, 1 index, 2 FKs). Migration generated and applied. Build and type-check pass.

## Task 2: Add moments API module + mount in packages/api/src/index.ts
- **Status**: Complete
- **Files changed**:
  - `packages/db/src/schemas.ts` (modified) — added `export * from './schema/moments'` to barrel export
  - `packages/api/src/types.ts` (modified) — added `Moment` and `MomentFeedback` row types from schema
  - `packages/api/src/modules/moments/model.ts` (new) — TypeBox validation schemas for generate, listQuery, feedback endpoints
  - `packages/api/src/modules/moments/service.ts` (new) — abstract static `MomentService` with list, getById, deleteByAssetAndPreset, insertMany, saveMoment, dismissMoment, addFeedback methods
  - `packages/api/src/modules/moments/index.ts` (new) — Elysia routes: `POST /api/moments/generate`, `GET /api/moments`, `POST /api/moments/:id/feedback`
  - `packages/api/src/index.ts` (modified) — mounted `moments` module
- **Outcome**: Full CRUD API module following project patterns (authMacro, abstract static service, TypeBox models). Generate endpoint validates asset ownership and readiness, returns cached moments or empty array (generation pipeline is a TODO for next task). Feedback endpoint handles save/dismiss state mutations + feedback recording. Build and type-check pass across all 7 packages.

## Task 3: Implement dynamic chunk config helper (2000 upper bound)
- **Status**: Complete
- **Files changed**:
  - `packages/api/src/modules/moments/chunking.ts` (new) — dynamic chunk config + segment chunker
- **Outcome**: Created `chunking.ts` with three exports:
  1. `getMomentChunkConfig(totalChars)` — returns `{ chunkSize, overlap, maxCandidatesPerChunk }` scaling across 5 tiers (900→2000 chars) per the plan's chunk policy table.
  2. `chunkSegmentsForMoments(segments, config)` — groups transcript segments into character-bounded `MomentChunk` windows with overlap, preserving `startTime`/`endTime` and `segmentIds` for traceability. Operates on full segment boundaries (never splits mid-segment), unlike the embedding chunker.
  3. Types: `MomentChunkConfig`, `MomentChunk`.
  Overlap is implemented by rewinding trailing segments into the next chunk. Build and type-check pass across all 7 packages.

## Task 4: Implement candidate generation + merge/rerank
- **Status**: Complete
- **Files changed**:
  - `packages/api/src/modules/moments/generate.ts` (new) — full generation pipeline
  - `packages/api/src/modules/moments/index.ts` (modified) — wired `generateMoments()` into POST /generate route, replacing TODO placeholder
  - `packages/api/package.json` (modified) — added `zod` dependency for structured output schema
- **Outcome**: Created `generate.ts` implementing the complete moment generation pipeline:
  1. **LLM candidate extraction**: Uses `generateText` with `Output.array()` (AI SDK v6 structured output) and `fastModel` (gemini-2.5-flash-lite) for cost-efficient chunk-level extraction. Processes chunks in batches of 3 for bounded concurrency.
  2. **Preset-aware prompting**: 6 preset-specific system prompts (default, hook, insight, quote, actionable, story) guide the LLM toward different extraction goals.
  3. **Merge/dedup**: Overlapping candidates (>50% time overlap) are merged, keeping the higher-scoring candidate with extended time window.
  4. **Structural heuristics scoring**: Computes score from cue phrase density, lexical density, and sentence burst intensity (0-1 range).
  5. **Ask-AI evidence scoring**: Queries `qa_message_part` for `retrieve_segments` tool outputs referencing the asset's segments, normalizes reference counts to 0-1 signal.
  6. **Ranking formula**: `0.45 * llmScore + 0.35 * qaSignal + 0.20 * structuralScore` with preset-specific boosts (e.g., hook boosts first 20% of timeline, actionable boosts imperative language).
  7. **Guardrails**: Caps total candidates at 80 before rerank, returns top 10 moments, persists with score breakdown in jsonb.
  Build and type-check pass across all 7 packages.

## Task 5: Integrate Ask-AI evidence scoring from qa_evidence
- **Status**: Complete
- **Files changed**:
  - `packages/api/src/modules/chat/service.ts` (modified) — added `qa_evidence` population on message save
  - `packages/api/src/modules/moments/generate.ts` (modified) — rewrote `getQAEvidenceSignals()` to query `qa_evidence` directly
- **Outcome**: Implemented the v1.1 recommended approach from the plan, putting `qa_evidence` to active use:
  1. **Evidence population**: `ChatService.saveMessages()` now extracts segment references from `retrieve_segments` tool outputs (both dynamic-tool and static tool parts) and inserts into `qa_evidence` with `relevanceScore` from cosine similarity. Deduplicates by messageId + segmentId within each save call.
  2. **Evidence querying**: `getQAEvidenceSignals()` replaced the old approach (scanning all `qa_message_part` tool outputs in JS) with a single SQL query joining `qa_evidence → transcript_segments → transcripts` filtered by assetId. Groups by segment, aggregates reference counts + max relevance score, normalizes to 0-1.
  3. **Blended signal**: When relevance scores are available, the qa signal blends reference frequency (60%) with retrieval similarity (40%). Falls back to pure frequency when no relevance scores exist.
  4. Removed unused `and` import from drizzle-orm in generate.ts. Replaced `qaMessageParts` import with `qaEvidence`.
  Build and type-check pass across all 7 packages.

## Task 6: Add Moments tab route and UI cards
- **Status**: Complete
- **Files changed**:
  - `apps/web/src/components/asset/asset-tab-bar.tsx` (modified) — added Moments tab with Sparkles icon, refactored active detection from binary isChat check to general getActiveTab() for 3-tab support
  - `apps/web/src/app/asset/[id]/moments/page.tsx` (new) — client route page rendering MomentsTab component, follows same pattern as transcript page
  - `apps/web/src/components/moments/moments-tab.tsx` (new) — main orchestrator: preset state, fetch/generate/save/dismiss via Eden treaty API, loading/empty/content states
  - `apps/web/src/components/moments/moment-card.tsx` (new) — individual moment card with title, timestamp range (clickable via useTimestampAction), rationale, source badge, save/dismiss actions, VideoMomentDialog for preview
  - `apps/web/src/components/moments/moment-preset-switcher.tsx` (new) — pill-style preset buttons (All, Hooks, Insights, Quotes, How-to, Story) with icons
- **Outcome**: Full Moments tab UI following existing patterns:
  1. **Tab bar**: Added third "Moments" tab with Sparkles icon, active state detection generalized to support 3 tabs.
  2. **Route**: `/asset/[id]/moments` client page matching transcript page pattern (border wrapper + component).
  3. **Preset switcher**: 6 preset pill buttons with distinct icons, disabled during generation.
  4. **Moment cards**: 2-column responsive grid. Each card shows title, source badge (Hybrid/AI/Ask AI), clickable timestamp range with Play icon (reuses `useTimestampAction` + `VideoMomentDialog`), rationale text, save/dismiss actions with optimistic UI updates.
  5. **States**: Loading spinner, empty state with generate CTA, and regenerate button in header when moments exist.
  6. **API integration**: Eden treaty calls to `GET /api/moments`, `POST /api/moments/generate`, `POST /api/moments/:id/feedback`.
  Build and type-check pass across all 7 packages.

## Task 7: Reuse timestamp preview interaction
- **Status**: Complete (verified — implementation was done as part of Task 6)
- **Files changed**: None (no new changes needed)
- **Outcome**: Verified that `moment-card.tsx` already correctly reuses the existing timestamp preview pattern:
  1. Imports `useTimestampAction` from `~/components/chat/use-timestamp-action` and `VideoMomentDialog` from `~/components/chat/video-moment-dialog`.
  2. Uses `isClickable` to conditionally render clickable timestamp range (Play icon button) or plain text fallback.
  3. Calls `handleClick(moment.startTime)` which generates embed URL via `getEmbedUrl()` and opens either an in-app `VideoMomentDialog` (for YouTube/Vimeo/etc.) or external link (for podcasts).
  4. `VideoMomentDialog` renders conditionally when `momentDialog` state is set, with proper close handling via `clearDialog()`.
  5. `AssetSourceContext` is provided by `AssetShell` layout wrapping all `/asset/[id]/*` routes, so the hook works correctly in the moments route.
  Acceptance criterion met: "Timestamp click opens playable preview via existing moment dialog." Type-check passes across all 7 packages.

## Task 8: Add save/dismiss feedback endpoint and UI actions
- **Status**: Complete (verified — implementation was done as part of Tasks 2 and 6)
- **Files changed**: None (no new changes needed)
- **Outcome**: Verified that save/dismiss feedback is fully implemented:
  1. **Backend**: `POST /api/moments/:id/feedback` endpoint in `index.ts` handles save/dismiss/upvote/downvote actions. `MomentService.saveMoment()` sets `isSaved: true`, `MomentService.dismissMoment()` sets `dismissedAt` timestamp, `MomentService.addFeedback()` records in `assetMomentFeedback` table with `onConflictDoNothing()` dedup.
  2. **Frontend**: `handleSave()` and `handleDismiss()` in `moments-tab.tsx` call the feedback endpoint via Eden treaty and apply optimistic UI updates (save toggles `isSaved`, dismiss filters moment out). `moment-card.tsx` renders Save/Dismiss buttons with proper disabled states and visual feedback (filled bookmark icon when saved).

## Task 9: Add basic tests for chunk config and merge/ranking logic
- **Status**: Complete
- **Files changed**:
  - `packages/api/package.json` (modified) — added `vitest` dev dependency and `test` script
  - `packages/api/vitest.config.ts` (new) — Vitest configuration targeting `src/**/*.test.ts`
  - `packages/api/src/modules/moments/chunking.test.ts` (new) — 14 tests for `getMomentChunkConfig` and `chunkSegmentsForMoments`
  - `packages/api/src/modules/moments/ranking.test.ts` (new) — 32 tests for `mergeOverlapping`, `computeStructuralScore`, `lookupQASignal`, `applyPresetBoost`, `rankCandidates`
  - `packages/api/src/modules/moments/generate.ts` (modified) — exported pure functions (`mergeOverlapping`, `computeStructuralScore`, `lookupQASignal`, `applyPresetBoost`, `rankCandidates`, `clamp01`) and types (`LLMCandidate`, `ScoredCandidate`) for testability
- **Outcome**: First test infrastructure for the monorepo. 46 tests across 2 test files:
  1. **Chunk config tests** (6): Verifies all 5 tier boundaries map to correct `{ chunkSize, overlap, maxCandidatesPerChunk }` values, and that chunkSize never exceeds 2000.
  2. **Chunk segmentation tests** (8): Empty input, single-chunk fit, multi-chunk splitting, time boundary preservation, oversized segment handling, overlap rewind behavior, newline-joined text output.
  3. **Merge/overlap tests** (6): Empty input, single candidate, non-overlapping preservation, >50% overlap merge with extended time window, higher-scoring winner selection, unsorted input handling.
  4. **Structural scoring tests** (4): Empty text, 0-1 range, cue phrase boost, sentence density boost, upper bound clamp.
  5. **QA signal lookup tests** (5): Empty map, range overlap, miss detection, max signal selection, boundary checking.
  6. **Preset boost tests** (9): Default returns 0, hook boosts early timeline + surprise language, actionable boosts imperative cues, quote boosts short duration, story boosts emotional language, insight boosts conceptual language.
  7. **Ranking tests** (8): Empty input, descending score order, topN limit, QA signal impact on ranking, score breakdown presence, structural score from segment texts.
  All 46 tests pass. Type-check passes across all 7 packages.
